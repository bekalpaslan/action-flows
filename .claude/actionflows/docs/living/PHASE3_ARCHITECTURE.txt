# Phase 3: Evidence Validation — Architecture Diagram

================================================================================
                        PHASE 3 EVIDENCE VALIDATION
                     Bidirectional Hook + Service Pattern
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│                         ORCHESTRATOR (Decision Layer)                        │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │ Post-Chain Completion Protocol                                     │    │
│  │                                                                     │    │
│  │  IF: LEARNINGS.md modified with closed learnings                   │    │
│  │  THEN: Trigger Gate 13 validation                                  │    │
│  │  OUTPUT: Surface warnings (non-blocking)                           │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │
                                   │ Trigger validation
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│                    GATE 13 VALIDATOR (Enhanced)                              │
│        packages/backend/src/services/checkpoints/gate13-learning-surface.ts │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │ Step 1: Parse orchestrator output                                  │    │
│  │         Extract closed learning IDs (L001, L002, ...)              │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                   │                                          │
│                                   ▼                                          │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │ Step 2: Extract evidence strings                                   │    │
│  │         For each closed learning:                                  │    │
│  │         - Match entry boundary (L00X to next L00X)                │    │
│  │         - Extract "Evidence: ..." line                            │    │
│  │         - Map: learningId -> evidenceString                       │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                   │                                          │
│                                   ▼                                          │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │ Step 3: Validate each evidence                                     │    │
│  │         Call EvidenceVerifier.validateAll(evidenceString)         │    │
│  │         Collect results: Map<learningId, ValidationResult>        │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                   │                                          │
│                                   ▼                                          │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │ Step 4: Calculate harmony score                                    │    │
│  │         Base: 100                                                  │    │
│  │         For each invalid evidence:                                 │    │
│  │           - Malformed: -25 points                                 │    │
│  │           - Not found: -20 points                                 │    │
│  │           - Unverified: -10 points                                │    │
│  │         Final: max(0, 100 - penalties)                            │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                   │                                          │
│                                   ▼                                          │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │ Step 5: Record checkpoint trace                                    │    │
│  │         Store in Redis/Memory (7-day TTL)                         │    │
│  │         Metadata includes:                                        │    │
│  │         - evidenceValidated: count                                │    │
│  │         - evidenceInvalidCount: count                             │    │
│  │         - evidenceResults: array of {id, type, valid, reason}    │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │
                                   │ Call validators
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│                     EVIDENCE VERIFIER SERVICE (NEW)                          │
│              packages/backend/src/services/evidenceVerifier.ts              │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐      │
│  │                      validateAll(evidenceString)                 │      │
│  │                                                                  │      │
│  │  Parse evidence string → Route to specific validator             │      │
│  └──────────────────────────────────────────────────────────────────┘      │
│                                   │                                          │
│     ┌──────────────┬──────────────┼──────────────┬──────────────┐          │
│     │              │               │              │              │          │
│     ▼              ▼               ▼              ▼              ▼          │
│  ┌────────┐   ┌─────────┐   ┌─────────┐   ┌──────────┐   ┌──────────┐    │
│  │ Commit │   │  File   │   │ Reason  │   │Escalated│   │Dissolu-  │    │
│  │  Hash  │   │Reference│   │  Text   │   │ Status  │   │tion      │    │
│  └────────┘   └─────────┘   └─────────┘   └──────────┘   └──────────┘    │
│      │             │              │              │              │          │
│      ▼             ▼              ▼              ▼              ▼          │
│  ┌────────────────────────────────────────────────────────────────────┐   │
│  │                         CACHING LAYER                              │   │
│  │                                                                    │   │
│  │  Key: "{type}:{value}" (e.g., "commit:c8a059b")                  │   │
│  │  TTL: 60 seconds (1 minute)                                      │   │
│  │  Hit Rate: >80% expected                                         │   │
│  │                                                                   │   │
│  │  Cache Miss → Execute validation → Store result                  │   │
│  │  Cache Hit → Return cached result (mark cached: true)           │   │
│  └────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐      │
│  │              VALIDATION IMPLEMENTATIONS                          │      │
│  │                                                                  │      │
│  │  validateCommitHash(hash)                                       │      │
│  │    → execSync(`git rev-parse --verify ${hash}`)                │      │
│  │    → Timeout: 5 seconds                                         │      │
│  │    → Accept: 7-40 hex characters                                │      │
│  │    → Error: Malformed / Not found / Git unavailable             │      │
│  │                                                                  │      │
│  │  validateFile(path)                                             │      │
│  │    → path.resolve(workingDir, path)                            │      │
│  │    → path.normalize() for cross-platform                        │      │
│  │    → fs.existsSync(resolvedPath)                               │      │
│  │    → Error: File not found (may have moved/deleted)            │      │
│  │                                                                  │      │
│  │  validateReason(text)                                           │      │
│  │    → Check length >10 characters                                │      │
│  │    → Always valid if sufficient length                          │      │
│  │    → Error: Reason too short                                    │      │
│  │                                                                  │      │
│  │  validateEscalated(reason)                                      │      │
│  │    → Always valid (architectural gaps are legitimate)           │      │
│  │    → Return: isValid=true                                       │      │
│  └──────────────────────────────────────────────────────────────────┘      │
│                                                                              │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │
                                   │ Return validation results
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│                       VALIDATION RESULT STRUCTURE                            │
│                                                                              │
│  interface EvidenceValidationResult {                                       │
│    evidenceType: 'commit' | 'file' | 'reason' | 'escalated' | 'unknown';  │
│    isValid: boolean;                                                        │
│    reason: string;                                                          │
│    timestamp: Date;                                                         │
│    sourceHash?: string;        // For commit evidence                       │
│    sourceFile?: string;        // For file evidence                         │
│    cached?: boolean;           // True if from cache                        │
│  }                                                                          │
│                                                                              │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │
                                   │ Aggregate results
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│                      OUTPUT: WARNING SURFACE                                 │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │ ## Evidence Validation Summary                                     │    │
│  │                                                                     │    │
│  │ **Learnings Validated:** 5 closed                                  │    │
│  │ **Evidence Status:**                                               │    │
│  │ - ✅ Valid: 3 learnings                                            │    │
│  │ - ⚠️ Invalid: 1 learning                                           │    │
│  │ - 🔍 Unverified: 1 learning (git unavailable)                     │    │
│  │                                                                     │    │
│  │ **Warnings:**                                                      │    │
│  │ - L012: File evidence invalid — docs/old.md not found             │    │
│  │                                                                     │    │
│  │ **Harmony Impact:** -20 points (1 invalid evidence)               │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │
                                   │ Non-blocking warnings
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│                        HEALTH SCORE INTEGRATION                              │
│                                                                              │
│  packages/backend/src/services/healthScoreCalculator.ts                     │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │ calculateGate13Health(chainId)                                     │    │
│  │   → Fetch latest Gate 13 trace                                     │    │
│  │   → Extract harmonyScore (includes evidence penalties)             │    │
│  │   → Return: 0-100 (15% weight in overall health)                   │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

================================================================================
                              DATA FLOW EXAMPLE
================================================================================

INPUT: Orchestrator outputs learning closure
───────────────────────────────────────────────────────────────────────────────

### L025: New Learning Entry
- **Date:** 2026-02-13
- **From:** review/ (sonnet)
- **Issue:** Something went wrong
- **Root Cause:** Bad implementation
- **Fix:** Fixed the code
- **Status:** Closed (dissolved) — Evidence: commit abc1234


PROCESSING: Gate 13 Enhanced Validation
───────────────────────────────────────────────────────────────────────────────

Step 1: Extract learning ID → "L025"
Step 2: Extract evidence → "Evidence: commit abc1234"
Step 3: Validate evidence:
        - Call EvidenceVerifier.validateAll("Evidence: commit abc1234")
        - Route to validateCommitHash("abc1234")
        - Check cache: MISS
        - Execute: git rev-parse --verify abc1234
        - Result: Not found (error code 128)
        - Cache result: "commit:abc1234" → invalid
Step 4: Calculate harmony:
        - Base: 100
        - Invalid evidence (not found): -20
        - Final: 80
Step 5: Record trace:
        - validationResult.harmonyScore = 80
        - metadata.evidenceResults = [{
            learningId: "L025",
            evidenceType: "commit",
            isValid: false,
            reason: "Commit abc1234 not found in repository"
          }]


OUTPUT: Warning Surface (Non-Blocking)
───────────────────────────────────────────────────────────────────────────────

⚠️ Evidence Validation Warnings:
- L025: Commit hash not found (abc1234 does not exist in repository)

Harmony Score: 80/100 (-20 points for invalid evidence)

[Chain continues — warnings don't block commit]


================================================================================
                           REGEX PATTERN REFERENCE
================================================================================

1. Commit Hash Pattern
   /Evidence:\s+commit\s+([a-f0-9]{7,40})/i
   Example: "Evidence: commit c8a059b" → captures "c8a059b"

2. File Reference Pattern
   /Evidence:\s+file\s+([^\s]+)/i
   Example: "Evidence: file docs/MEMORY.md" → captures "docs/MEMORY.md"

3. Documented Reason Pattern
   /Evidence:\s+documented\s+(?:in|as)\s+([^\n]+)/i
   Example: "Evidence: documented in MEMORY.md" → captures "MEMORY.md"

4. Escalated Status Pattern
   /Status:\s+Escalated\s+\(([^)]+)\)/i
   Example: "Status: Escalated (architectural gap)" → captures "architectural gap"

5. Dissolution Pattern
   /Status:\s+Closed.*?Evidence:\s+commit\s+([a-f0-9]{7,40})/is
   Example: "Status: Closed — Evidence: commit abc123" → captures "abc123"

6. Learning Entry Boundary
   /(L\d{3}[\s\S]*?)(?=\n###\s+L\d{3}|$)/i
   Example: Extracts full learning entry from L001 to L002 (or EOF)

7. Closure Status Check
   /Status:\s+Closed/i
   Example: Matches any "Status: Closed" line (case-insensitive)


================================================================================
                            PERFORMANCE PROFILE
================================================================================

Target Metrics (5 learnings):
────────────────────────────────────────────────────────────────────────────

  Operation                    Time (ms)      Cache Hit Rate
  ─────────────────────────────────────────────────────────────────────────
  Parse orchestrator output         5-10           N/A
  Extract evidence strings         10-20           N/A
  Validate commit hash (cache)      <1            >80%
  Validate commit hash (git)       30-50          <20%
  Validate file reference           5-10           50%
  Calculate harmony score           <5             N/A
  Record checkpoint trace          10-20           N/A
  ─────────────────────────────────────────────────────────────────────────
  TOTAL (5 learnings)             <100ms         Cache hit: ~60ms
                                                 Cache miss: ~150ms

Cache Strategy:
  - TTL: 60 seconds (1 minute)
  - Expected hit rate: >80% in production
  - Memory footprint: <5MB (typical workload)

Git Performance:
  - Timeout: 5 seconds (safety limit)
  - Typical: 30-50ms per commit verification
  - Cached: <1ms (near-instant)


================================================================================
                          ERROR HANDLING STRATEGY
================================================================================

┌────────────────────────────────────────────────────────────────────────────┐
│ Error Type           │ Action                     │ Harmony Impact         │
├────────────────────────────────────────────────────────────────────────────┤
│ Malformed hash       │ Reject immediately         │ -25 points (critical)  │
│ Commit not found     │ Warn (might be rebased)    │ -20 points (standard)  │
│ File not found       │ Warn (might be moved)      │ -20 points (standard)  │
│ Git unavailable      │ Graceful degradation       │ -10 points (unverified)│
│ Git timeout          │ Graceful degradation       │ -10 points (unverified)│
│ Filesystem error     │ Warn with error details    │ -20 points (standard)  │
│ Unknown format       │ Reject (not recognized)    │ -20 points (standard)  │
│ Reason too short     │ Reject (insufficient)      │ -20 points (standard)  │
└────────────────────────────────────────────────────────────────────────────┘

Graceful Degradation Examples:
  - Git not installed → Mark as "unverified" (not invalid)
  - Git repository error → Mark as "unverified" (not invalid)
  - Timeout expired → Mark as "unverified" (not invalid)

All errors are NON-BLOCKING. Validation warnings don't prevent commits.


================================================================================
                              TESTING MATRIX
================================================================================

Unit Tests (evidenceVerifier.test.ts):
┌────────────────────────────────────────────────────────────────────────────┐
│ Test Case                      │ Input                 │ Expected Output   │
├────────────────────────────────────────────────────────────────────────────┤
│ Valid commit hash              │ "333d065"            │ isValid: true     │
│ Malformed commit hash          │ "zzz123"             │ isValid: false    │
│ Non-existent commit            │ "abc1234"            │ isValid: false    │
│ Long commit hash               │ "333d065..." (40)    │ isValid: true     │
│ Existing file                  │ "README.md"          │ isValid: true     │
│ Non-existent file              │ "missing.md"         │ isValid: false    │
│ Nested file path               │ ".claude/..."        │ isValid: true     │
│ Valid documented reason        │ "documented in..."   │ isValid: true     │
│ Short documented reason        │ "fix"                │ isValid: false    │
│ Escalated status               │ "architectural gap"  │ isValid: true     │
│ Dissolution pattern            │ "Closed — commit..." │ isValid: true     │
│ Unknown format                 │ "random text"        │ isValid: false    │
│ Cache hit                      │ Same hash twice      │ cached: true      │
└────────────────────────────────────────────────────────────────────────────┘

Integration Tests (gate13 scenarios):
┌────────────────────────────────────────────────────────────────────────────┐
│ Scenario                       │ Learnings    │ Expected Harmony Score    │
├────────────────────────────────────────────────────────────────────────────┤
│ All valid evidence             │ 5 closed     │ 100 (no penalties)        │
│ 1 invalid commit               │ 5 closed     │ 80 (-20 for invalid)      │
│ 2 malformed + 1 not found      │ 5 closed     │ 30 (-25-25-20)           │
│ Git unavailable                │ 5 closed     │ 50 (-10×5 unverified)    │
│ Mixed valid/invalid            │ 3 valid, 2   │ 60 (-20×2)               │
│                                │ invalid      │                           │
└────────────────────────────────────────────────────────────────────────────┘


================================================================================
                            DEPLOYMENT SEQUENCE
================================================================================

Phase 3A: Evidence Verifier Service (3-4 hours)
────────────────────────────────────────────────────────────────────────────
  1. Create evidenceVerifier.ts
  2. Implement 5 validators (commit, file, reason, escalated, dissolution)
  3. Add caching layer (1-minute TTL)
  4. Write unit tests (>95% coverage)
  5. Test with real Git repository

  Commit: feat: add evidence verifier service (Phase 3A)

Phase 3B: Gate 13 Enhancement (2-3 hours)
────────────────────────────────────────────────────────────────────────────
  1. Import EvidenceVerifier into gate13-learning-surface.ts
  2. Add extractEvidenceStrings() helper
  3. Add validateClosureEvidence() async function
  4. Update calculateHarmonyScore() with penalties
  5. Enhance metadata with validation results
  6. Test with LEARNINGS.md (24 closed entries)

  Commit: feat: enhance Gate 13 with evidence validation (Phase 3B)

Phase 3C: Orchestrator Integration (2-3 hours)
────────────────────────────────────────────────────────────────────────────
  1. Update ORCHESTRATOR.md with validation trigger
  2. Define warning output format
  3. Integrate Gate 13 score into healthScoreCalculator.ts
  4. Test end-to-end flow

  Commit: feat: integrate evidence validation into orchestrator (Phase 3C)


================================================================================
                              SUCCESS METRICS
================================================================================

After Phase 3 implementation:
  ✅ All closed learnings (24 entries) have validated evidence
  ✅ Git commit hashes verified via `git rev-parse`
  ✅ File references checked against filesystem
  ✅ Gate 13 harmony score reflects evidence validity
  ✅ Orchestrator surfaces clear warnings for invalid evidence
  ✅ Zero false positives (valid evidence not marked invalid)
  ✅ Graceful degradation when git unavailable
  ✅ Performance: <100ms for 5 validations (with cache)
  ✅ Cache hit rate: >80% in production workload
  ✅ Cross-platform support (Windows/Linux/macOS)

Health Score Impact:
  - Gate 13 weight: 15% of overall health
  - Harmony score range: 0-100 (evidence-adjusted)
  - Dashboard integration: Gate 13 trace visible in workbench


================================================================================
END OF ARCHITECTURE DIAGRAM
================================================================================
