---
phase: 02-frontend-scaffold-websocket
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/src/main.tsx
  - packages/app/src/App.tsx
  - packages/app/src/App.css
  - packages/app/src/index.css
  - packages/app/vite.config.ts
  - packages/app/package.json
  - packages/app/src/workbenches/shell/AppShell.tsx
  - packages/app/src/workbenches/shell/AppShell.css
  - packages/app/src/workbenches/sidebar/SidebarPlaceholder.tsx
  - packages/app/src/workbenches/sidebar/SidebarPlaceholder.css
  - packages/app/src/workbenches/workspace/WorkspaceArea.tsx
  - packages/app/src/workbenches/workspace/WorkspaceArea.css
  - packages/app/src/workbenches/chat/ChatPlaceholder.tsx
  - packages/app/src/workbenches/chat/ChatPlaceholder.css
  - packages/app/src/workbenches/pages/WorkPage.tsx
  - packages/app/src/workbenches/pages/ExplorePage.tsx
  - packages/app/src/workbenches/pages/ReviewPage.tsx
  - packages/app/src/workbenches/pages/PMPage.tsx
  - packages/app/src/workbenches/pages/SettingsPage.tsx
  - packages/app/src/workbenches/pages/ArchivePage.tsx
  - packages/app/src/workbenches/pages/StudioPage.tsx
  - packages/app/src/stores/uiStore.ts
  - packages/app/src/lib/types.ts
autonomous: true
requirements: [FOUND-04]

must_haves:
  truths:
    - "The app renders a 3-region shell (sidebar, workspace, chat placeholder) instead of the old cosmic map"
    - "Clicking a workbench name in the sidebar switches the workspace content to that workbench's placeholder page"
    - "All 7 default workbenches (Work, Explore, Review, PM, Settings, Archive, Studio) appear in the sidebar"
    - "pnpm type-check passes with zero errors in packages/app"
    - "No imports from deleted directories (components/, hooks/, contexts/) exist anywhere in src/"
  artifacts:
    - path: "packages/app/src/workbenches/shell/AppShell.tsx"
      provides: "Root 3-region layout container"
      contains: "app-shell"
    - path: "packages/app/src/workbenches/sidebar/SidebarPlaceholder.tsx"
      provides: "7-workbench navigation list"
      contains: "SidebarPlaceholder"
    - path: "packages/app/src/workbenches/workspace/WorkspaceArea.tsx"
      provides: "Active workbench content renderer"
      contains: "WorkspaceArea"
    - path: "packages/app/src/stores/uiStore.ts"
      provides: "Active workbench state via zustand"
      contains: "useUIStore"
    - path: "packages/app/src/lib/types.ts"
      provides: "WorkbenchId union type definition"
      contains: "WorkbenchId"
  key_links:
    - from: "packages/app/src/workbenches/sidebar/SidebarPlaceholder.tsx"
      to: "packages/app/src/stores/uiStore.ts"
      via: "useUIStore selector for activeWorkbench + setActiveWorkbench"
      pattern: "useUIStore.*activeWorkbench|setActiveWorkbench"
    - from: "packages/app/src/workbenches/shell/AppShell.tsx"
      to: "packages/app/src/stores/uiStore.ts"
      via: "useUIStore selector to read activeWorkbench for WorkspaceArea key"
      pattern: "useUIStore.*activeWorkbench"
    - from: "packages/app/src/App.tsx"
      to: "packages/app/src/workbenches/shell/AppShell.tsx"
      via: "direct import and render"
      pattern: "import.*AppShell"
---

<objective>
Delete the entire cosmic-themed frontend and build a clean workbench shell with sidebar navigation, workspace area, and chat placeholder. Install zustand for state management and create the UI store for workbench switching.

Purpose: Per D-01 and D-02, this is a hard cutover -- the old cosmic UI is removed entirely and replaced with a new workbench-based architecture. This gives Phase 3 (design system) and Phase 4 (layout) a clean foundation to build on.
Output: A working React app that renders a 3-region shell with 7 navigable workbench placeholders, no cosmic code remaining, and zero TypeScript errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-scaffold-websocket/02-CONTEXT.md
@.planning/phases/02-frontend-scaffold-websocket/02-RESEARCH.md
@.planning/phases/02-frontend-scaffold-websocket/02-UI-SPEC.md

<interfaces>
<!-- WorkbenchId type used by all shell components and stores -->
<!-- Define this FIRST in lib/types.ts, then import everywhere -->

type WorkbenchId = 'work' | 'explore' | 'review' | 'pm' | 'settings' | 'archive' | 'studio';

<!-- Zustand store shape for UI state -->
interface UIState {
  activeWorkbench: WorkbenchId;
  setActiveWorkbench: (id: WorkbenchId) => void;
}

<!-- Existing design tokens consumed (from design-tokens.css) -->
<!-- --app-bg-primary: #0c1425 -->
<!-- --app-bg-secondary: #101b31 -->
<!-- --app-bg-tertiary: (derived, lighter panel bg) -->
<!-- --text-primary, --text-secondary -->
<!-- --system-blue: #3e67bf -->
<!-- --font-sans: system stack -->
<!-- --space-2: 8px, --space-4: 16px, --space-6: 24px -->
<!-- --gap: 20px -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete cosmic frontend, install zustand, rewrite entry points and vite config</name>
  <files>
    packages/app/src/components/ (DELETE)
    packages/app/src/hooks/ (DELETE)
    packages/app/src/contexts/ (DELETE)
    packages/app/src/store/ (DELETE)
    packages/app/src/contracts/ (DELETE)
    packages/app/src/capabilities/ (DELETE)
    packages/app/src/systems/ (DELETE)
    packages/app/src/services/ (DELETE)
    packages/app/src/config/ (DELETE)
    packages/app/src/data/ (DELETE)
    packages/app/src/types/ (DELETE)
    packages/app/src/utils/ (DELETE)
    packages/app/src/monaco-config.ts (DELETE)
    packages/app/src/App.css (DELETE)
    packages/app/src/styles/cosmic-tokens.css (DELETE)
    packages/app/src/styles/region-themes.css (DELETE)
    packages/app/src/styles/ChainDemo.css (DELETE)
    packages/app/src/styles/ChainLiveMonitor.css (DELETE)
    packages/app/src/styles/history-browser.css (DELETE)
    packages/app/src/styles/animations/ (DELETE)
    packages/app/src/main.tsx (REWRITE)
    packages/app/src/App.tsx (REWRITE)
    packages/app/src/index.css (MODIFY)
    packages/app/vite.config.ts (MODIFY)
    packages/app/package.json (MODIFY via pnpm add)
    packages/app/src/lib/types.ts (CREATE)
    packages/app/src/stores/uiStore.ts (CREATE)
  </files>
  <read_first>
    packages/app/src/main.tsx
    packages/app/src/App.tsx
    packages/app/src/index.css
    packages/app/vite.config.ts
    packages/app/src/styles/design-tokens.css (lines 1-30 to confirm it exists and is token-only)
    packages/app/src/styles/themes/index.css (check what it imports -- keep if it loads design-tokens)
  </read_first>
  <action>
    **Step 1: Delete cosmic directories and files (per D-02, D-03)**

    Delete these directories entirely (rm -rf):
    - packages/app/src/components/
    - packages/app/src/hooks/
    - packages/app/src/contexts/
    - packages/app/src/store/
    - packages/app/src/contracts/
    - packages/app/src/capabilities/
    - packages/app/src/systems/
    - packages/app/src/services/
    - packages/app/src/config/
    - packages/app/src/data/
    - packages/app/src/types/
    - packages/app/src/utils/
    - packages/app/src/__tests__/ (cosmic test files)

    Delete these specific files:
    - packages/app/src/monaco-config.ts
    - packages/app/src/App.css
    - packages/app/src/styles/cosmic-tokens.css
    - packages/app/src/styles/region-themes.css
    - packages/app/src/styles/ChainDemo.css
    - packages/app/src/styles/ChainLiveMonitor.css
    - packages/app/src/styles/history-browser.css
    - packages/app/src/styles/animations/ (entire directory)

    PRESERVE (do NOT delete):
    - packages/app/src/styles/design-tokens.css
    - packages/app/src/styles/design-tokens-guide.css
    - packages/app/src/styles/themes/ (if it imports design-tokens.css -- inspect first)
    - packages/app/src/index.css (will be modified)
    - packages/app/src/vite-env.d.ts
    - packages/app/electron/ (entire directory)

    **Step 2: Install zustand**

    ```bash
    cd packages/app && pnpm add zustand@^5.0.12
    ```

    **Step 3: Clean vite.config.ts**

    In packages/app/vite.config.ts:
    - Remove `reactflow` from the `optimizeDeps.include` array (line 93). Keep `react` and `react-dom`.
    - Remove the `manualChunks` entries that reference deleted code:
      - Remove the reactflow chunk (line 58-60: `if (id.includes('node_modules/@react-flow') || id.includes('node_modules/reactflow'))`)
      - Remove the cosmic-map chunk (line 73-75: `if (id.includes('/CosmicMap/') || id.includes('cosmic'))`)
      - Remove the flow-viz chunk (line 76-78: `if (id.includes('/FlowVisualization/'))`)
    - KEEP: react plugin, electron plugin, proxy config (`/api` and `/ws`), path alias `@`, build output config, monaco/xterm chunks (they may return in later phases), worker config.

    **Step 4: Update index.css**

    Current line 1 is `@import './styles/themes/index.css';` -- keep this if the themes/index.css loads design-tokens.css. Remove the `@import './styles/animations/index.css'` from main.tsx (it's being deleted).

    **Step 5: Create lib/types.ts**

    Create `packages/app/src/lib/types.ts`:
    ```typescript
    /** The 7 default workbench identifiers */
    export type WorkbenchId = 'work' | 'explore' | 'review' | 'pm' | 'settings' | 'archive' | 'studio';

    /** Workbench metadata for sidebar rendering */
    export interface WorkbenchMeta {
      id: WorkbenchId;
      label: string;
    }

    /** All default workbenches in sidebar order */
    export const WORKBENCHES: readonly WorkbenchMeta[] = [
      { id: 'work', label: 'Work' },
      { id: 'explore', label: 'Explore' },
      { id: 'review', label: 'Review' },
      { id: 'pm', label: 'PM' },
      { id: 'settings', label: 'Settings' },
      { id: 'archive', label: 'Archive' },
      { id: 'studio', label: 'Studio' },
    ] as const;
    ```

    **Step 6: Create stores/uiStore.ts**

    Create `packages/app/src/stores/uiStore.ts`:
    ```typescript
    import { create } from 'zustand';
    import type { WorkbenchId } from '../lib/types';

    interface UIState {
      activeWorkbench: WorkbenchId;
      setActiveWorkbench: (id: WorkbenchId) => void;
    }

    export const useUIStore = create<UIState>((set) => ({
      activeWorkbench: 'work',
      setActiveWorkbench: (id) => set({ activeWorkbench: id }),
    }));
    ```

    **Step 7: Rewrite main.tsx**

    Replace packages/app/src/main.tsx entirely:
    ```typescript
    import React from 'react';
    import ReactDOM from 'react-dom/client';
    import App from './App';
    import './index.css';
    import './styles/design-tokens.css';

    ReactDOM.createRoot(document.getElementById('root')!).render(
      <React.StrictMode>
        <App />
      </React.StrictMode>,
    );
    ```

    Note: No animation import (deleted), no cosmic styles. The `index.css` import pulls in themes (which load design tokens).

    **Step 8: Rewrite App.tsx**

    Replace packages/app/src/App.tsx entirely:
    ```typescript
    import { AppShell } from './workbenches/shell/AppShell';

    function App() {
      return <AppShell />;
    }

    export default App;
    ```

    No provider pyramid (per D-03). Zero context providers. Zustand stores are module singletons.

    **Step 9: Remove unused dependencies from package.json**

    ```bash
    cd packages/app && pnpm remove reactflow @reactflow/core
    ```

    These are replaced by @xyflow/react in Phase 5. Leave monaco-editor, xterm, d3, dagre for later phases.
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit 2>&1 | head -5</automated>
    Verify: No imports reference deleted directories. Run:
    grep -r "from.*components/" packages/app/src/main.tsx packages/app/src/App.tsx 2>/dev/null | wc -l should be 0
    grep -r "from.*contexts/" packages/app/src/main.tsx packages/app/src/App.tsx 2>/dev/null | wc -l should be 0
    Verify: zustand is in package.json dependencies
    grep "zustand" packages/app/package.json
  </verify>
  <acceptance_criteria>
    - grep -r "from.*'/components" packages/app/src/main.tsx packages/app/src/App.tsx returns 0 results
    - grep -r "from.*'/contexts" packages/app/src/main.tsx packages/app/src/App.tsx returns 0 results
    - grep -r "from.*'/hooks'" packages/app/src/main.tsx packages/app/src/App.tsx returns 0 results
    - ls packages/app/src/components/ fails (directory deleted)
    - ls packages/app/src/contexts/ fails (directory deleted)
    - ls packages/app/src/hooks/ fails (directory deleted)
    - grep "zustand" packages/app/package.json returns a match
    - cat packages/app/src/lib/types.ts | grep "WorkbenchId" returns a match
    - cat packages/app/src/stores/uiStore.ts | grep "useUIStore" returns a match
    - cat packages/app/src/App.tsx | grep "AppShell" returns a match
    - grep "reactflow" packages/app/vite.config.ts returns 0 results (removed from optimizeDeps and manualChunks)
    - grep "CosmicMap" packages/app/vite.config.ts returns 0 results
  </acceptance_criteria>
  <done>
    All cosmic directories and files deleted. zustand installed. main.tsx, App.tsx, vite.config.ts, index.css rewritten/cleaned. lib/types.ts with WorkbenchId union and WORKBENCHES array created. stores/uiStore.ts with activeWorkbench state created. No references to deleted code remain in entry points.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build workbench shell components (AppShell, Sidebar, Workspace, Chat, 7 pages)</name>
  <files>
    packages/app/src/workbenches/shell/AppShell.tsx (CREATE)
    packages/app/src/workbenches/shell/AppShell.css (CREATE)
    packages/app/src/workbenches/sidebar/SidebarPlaceholder.tsx (CREATE)
    packages/app/src/workbenches/sidebar/SidebarPlaceholder.css (CREATE)
    packages/app/src/workbenches/workspace/WorkspaceArea.tsx (CREATE)
    packages/app/src/workbenches/workspace/WorkspaceArea.css (CREATE)
    packages/app/src/workbenches/chat/ChatPlaceholder.tsx (CREATE)
    packages/app/src/workbenches/chat/ChatPlaceholder.css (CREATE)
    packages/app/src/workbenches/pages/WorkPage.tsx (CREATE)
    packages/app/src/workbenches/pages/ExplorePage.tsx (CREATE)
    packages/app/src/workbenches/pages/ReviewPage.tsx (CREATE)
    packages/app/src/workbenches/pages/PMPage.tsx (CREATE)
    packages/app/src/workbenches/pages/SettingsPage.tsx (CREATE)
    packages/app/src/workbenches/pages/ArchivePage.tsx (CREATE)
    packages/app/src/workbenches/pages/StudioPage.tsx (CREATE)
  </files>
  <read_first>
    packages/app/src/lib/types.ts (created in Task 1 -- WorkbenchId, WORKBENCHES)
    packages/app/src/stores/uiStore.ts (created in Task 1 -- useUIStore)
    packages/app/src/styles/design-tokens.css (lines 1-100 for token names: --app-bg-primary, --app-bg-secondary, --text-primary, --text-secondary, --system-blue, --font-sans, --space-*, --gap)
    .planning/phases/02-frontend-scaffold-websocket/02-UI-SPEC.md (visual contract)
  </read_first>
  <action>
    Build all shell components using ONLY design token CSS custom properties (no raw hex values, no Tailwind -- per UI-SPEC).

    **1. AppShell.tsx + AppShell.css**

    AppShell is a CSS Grid with 3 columns: sidebar (fixed 220px), workspace (fluid 1fr), chat placeholder (fixed 300px).

    CSS (`AppShell.css`):
    ```css
    .app-shell {
      display: grid;
      grid-template-columns: 220px 1fr 300px;
      height: 100vh;
      background: var(--app-bg-primary);
      color: var(--text-primary);
      font-family: var(--font-sans);
    }
    ```

    TSX (`AppShell.tsx`):
    - Import `useUIStore` from `@/stores/uiStore`
    - Import `SidebarPlaceholder`, `WorkspaceArea`, `ChatPlaceholder`
    - Read `activeWorkbench` from store
    - Render: `<div className="app-shell">` containing the 3 children
    - Pass `key={activeWorkbench}` to `WorkspaceArea` (per PITFALLS P13 -- forces clean unmount/remount on workbench switch)

    **2. SidebarPlaceholder.tsx + SidebarPlaceholder.css**

    Sidebar is a `<nav>` with a `<ul>` listing all 7 workbenches.

    CSS (`SidebarPlaceholder.css`):
    ```css
    .sidebar {
      background: var(--app-bg-secondary);
      padding: var(--space-4);
      display: flex;
      flex-direction: column;
      overflow-y: auto;
    }

    .sidebar__title {
      font-size: var(--text-xl);
      font-weight: var(--font-semibold);
      line-height: var(--leading-tight);
      color: var(--text-primary);
      margin-bottom: var(--space-6);
    }

    .sidebar__list {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: var(--space-2);
    }

    .sidebar__item {
      padding: var(--space-2) var(--space-4);
      color: var(--text-secondary);
      cursor: pointer;
      border-left: 3px solid transparent;
      transition: background-color 150ms ease;
      font-size: var(--text-sm);
      font-weight: var(--font-semibold);
      line-height: var(--leading-snug);
    }

    .sidebar__item:hover {
      background: var(--app-bg-tertiary);
      color: var(--text-primary);
    }

    .sidebar__item--active {
      border-left-color: var(--system-blue);
      color: var(--text-primary);
      background: var(--app-bg-tertiary);
    }
    ```

    TSX (`SidebarPlaceholder.tsx`):
    - Import `useUIStore` from `@/stores/uiStore`
    - Import `WORKBENCHES` from `@/lib/types`
    - Read `activeWorkbench` and `setActiveWorkbench` from store
    - Map over `WORKBENCHES` to render `<li>` elements
    - Each item: `className` conditionally includes `sidebar__item--active` when `id === activeWorkbench`
    - Each item: `onClick={() => setActiveWorkbench(id)}`
    - Title text: "Workbenches" as heading

    **3. WorkspaceArea.tsx + WorkspaceArea.css**

    Workspace renders the active workbench's placeholder page.

    CSS (`WorkspaceArea.css`):
    ```css
    .workspace {
      background: var(--app-bg-primary);
      padding: var(--space-6);
      overflow-y: auto;
      display: flex;
      flex-direction: column;
    }
    ```

    TSX (`WorkspaceArea.tsx`):
    - Accept prop `workbenchId: WorkbenchId`
    - Use a switch/map to render the correct page component for the workbenchId
    - Import all 7 page components lazily or directly (direct import is fine for 7 small placeholders)

    **4. ChatPlaceholder.tsx + ChatPlaceholder.css**

    Right panel placeholder showing "Chat panel -- Phase 7" text.

    CSS (`ChatPlaceholder.css`):
    ```css
    .chat-placeholder {
      background: var(--app-bg-secondary);
      padding: var(--space-4);
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-secondary);
      font-size: var(--text-sm);
      font-weight: var(--font-semibold);
    }
    ```

    TSX (`ChatPlaceholder.tsx`):
    - Simple component rendering the placeholder text: "Chat panel -- Phase 7"

    **5. Seven workbench page placeholders**

    Each page in `workbenches/pages/`:
    - WorkPage.tsx: heading "Work", body "Active sessions and ongoing chains"
    - ExplorePage.tsx: heading "Explore", body "Navigate and understand the codebase"
    - ReviewPage.tsx: heading "Review", body "Quality gates, approvals, and audits"
    - PMPage.tsx: heading "PM", body "Planning, roadmaps, and task tracking"
    - SettingsPage.tsx: heading "Settings", body "Configuration, preferences, and system health"
    - ArchivePage.tsx: heading "Archive", body "Historical sessions and searchable memory"
    - StudioPage.tsx: heading "Studio", body "Preview components, test layouts, live renders"

    Each page follows the same pattern:
    ```tsx
    export function WorkPage() {
      return (
        <div className="workbench-page">
          <h1 className="workbench-page__heading">Work</h1>
          <p className="workbench-page__body">Active sessions and ongoing chains</p>
        </div>
      );
    }
    ```

    Shared page styles can go in `WorkspaceArea.css` or a shared `workbenches/pages/pages.css`:
    ```css
    .workbench-page__heading {
      font-size: var(--text-xl);
      font-weight: var(--font-semibold);
      line-height: var(--leading-tight);
      color: var(--text-primary);
      margin-bottom: var(--space-4);
    }

    .workbench-page__body {
      font-size: var(--text-base);
      font-weight: var(--font-normal);
      line-height: var(--leading-normal);
      color: var(--text-secondary);
    }
    ```

    **6. Verify type-check passes**

    After all files created, run `cd packages/app && npx tsc --noEmit` to confirm zero errors.
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit && echo "TYPE_CHECK_PASS"</automated>
    Also verify:
    - pnpm dev:app starts without errors (Vite dev server on port 5173)
    - The shell renders 3 regions (check by visiting http://localhost:5173 if dev server running)
  </verify>
  <acceptance_criteria>
    - cat packages/app/src/workbenches/shell/AppShell.tsx | grep "app-shell" returns a match
    - cat packages/app/src/workbenches/shell/AppShell.css | grep "grid-template-columns: 220px 1fr 300px" returns a match
    - cat packages/app/src/workbenches/sidebar/SidebarPlaceholder.tsx | grep "WORKBENCHES" returns a match
    - cat packages/app/src/workbenches/sidebar/SidebarPlaceholder.tsx | grep "setActiveWorkbench" returns a match
    - cat packages/app/src/workbenches/sidebar/SidebarPlaceholder.css | grep "system-blue" returns a match
    - cat packages/app/src/workbenches/workspace/WorkspaceArea.tsx | grep "workbenchId" returns a match
    - cat packages/app/src/workbenches/chat/ChatPlaceholder.tsx | grep "Phase 7" returns a match
    - ls packages/app/src/workbenches/pages/WorkPage.tsx confirms file exists
    - ls packages/app/src/workbenches/pages/StudioPage.tsx confirms file exists
    - All 7 page files exist: WorkPage, ExplorePage, ReviewPage, PMPage, SettingsPage, ArchivePage, StudioPage
    - grep -r "#0c1425\|#101b31\|#3e67bf" packages/app/src/workbenches/ returns 0 results (no raw hex -- all token references)
    - cd packages/app && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    AppShell renders a 3-region CSS grid (220px sidebar, 1fr workspace, 300px chat). Sidebar lists 7 workbenches with active state highlighting using design tokens. Clicking a workbench name updates the workspace content via zustand store. All styling uses CSS custom properties from design-tokens.css (no raw hex values). TypeScript compiles with zero errors.
  </done>
</task>

</tasks>

<verification>
1. `cd packages/app && npx tsc --noEmit` exits 0 (zero TypeScript errors)
2. No cosmic imports remain: `grep -r "from.*components/\|from.*contexts/\|from.*hooks/" packages/app/src/main.tsx packages/app/src/App.tsx packages/app/src/workbenches/` returns 0 results
3. All 7 workbench pages exist in packages/app/src/workbenches/pages/
4. zustand is installed: `grep "zustand" packages/app/package.json`
5. No raw hex colors in shell CSS: `grep -r "#[0-9a-f]" packages/app/src/workbenches/` returns 0 results (all use var(--token))
</verification>

<success_criteria>
The app renders a 3-region workbench shell when loaded. The sidebar shows 7 workbench names. Clicking a workbench name switches the workspace content. The old cosmic UI is completely gone. TypeScript compiles cleanly. All styling uses design tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-scaffold-websocket/02-01-SUMMARY.md`
</output>
