---
phase: 999.1-history-and-memory-lifecycle-management
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/actionflows/actions/_abstract/create-log-folder/instructions.md
autonomous: false
requirements: [D-11, D-12]
must_haves:
  truths:
    - "Every new log session directory created via the create-log-folder abstract action contains an INDEX.md"
    - "INDEX.md contains: session dir name, action type, ISO timestamp, one-line description"
    - "Existing log dirs without INDEX.md are accepted as legacy (no backfill required)"
  artifacts:
    - path: .claude/actionflows/actions/_abstract/create-log-folder/instructions.md
      provides: "Updated instructions enforcing INDEX.md creation as step 5"
      contains: "INDEX.md"
  key_links:
    - from: .claude/actionflows/actions/_abstract/create-log-folder/instructions.md
      to: .claude/actionflows/logs/
      via: documented "Step 5: Create INDEX.md" instruction every action follows
      pattern: "Step 5"
---

<objective>
Update the `_abstract/create-log-folder` action instructions to require an `INDEX.md` file inside every new log session directory at creation time. The orchestrator and all agents that extend this abstract will then write the index automatically. Old log dirs are NOT backfilled (legacy accepted per D-11).

Purpose: Closes D-11 (enforce INDEX.md at log dir creation) and D-12 (minimum content fields).
Output: One framework instructions file updated. Includes a manual checkpoint to verify the next time any agent runs an action, an INDEX.md is produced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-history-and-memory-lifecycle-management/999.1-CONTEXT.md
@.planning/phases/999.1-history-and-memory-lifecycle-management/999.1-RESEARCH.md
@.claude/actionflows/actions/_abstract/create-log-folder/instructions.md
@.claude/actionflows/LOGGING_STANDARDS_CATALOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 5 (Create INDEX.md) to create-log-folder instructions</name>
  <files>.claude/actionflows/actions/_abstract/create-log-folder/instructions.md</files>
  <read_first>
    - .claude/actionflows/actions/_abstract/create-log-folder/instructions.md (FULL FILE — this is the file being edited; preserve existing structure including the Critical Execution Order section, the bash example with `description=` / `datetime=` / `folder=` variables, and the Contract Contributions section at the bottom)
    - .claude/actionflows/LOGGING_STANDARDS_CATALOG.md (logging standards the new INDEX.md must align with — read for tone/format consistency only)
  </read_first>
  <action>
    Edit `.claude/actionflows/actions/_abstract/create-log-folder/instructions.md` IN PLACE. Use LF line endings (UNIX) — `.gitattributes` line `*.md text eol=lf` should already cover this; if not, write with explicit `\n` only.

    Apply these specific changes (no other content should be modified):

    1. **In the "Critical Execution Order" numbered list**, add a new step 5:

       Existing list ends at "4. Fourth: Pass the completed string to mkdir -p". Add:

       ```
       5. **Fifth:** Immediately after `mkdir -p` succeeds, create an `INDEX.md` file inside the new folder with the required fields below (D-11, D-12).
       ```

    2. **After the existing CORRECT bash example block** (the one with `description=`, `datetime=`, `folder=`, `mkdir -p`), add a new sub-section directly below it:

       ```markdown
       ### Step 5: Create INDEX.md (REQUIRED)

       Per phase 999.1 (D-11, D-12), every new log session directory MUST contain an `INDEX.md` written immediately after the folder is created. This is non-optional. Old directories without an INDEX.md are accepted as legacy and are NOT backfilled.

       **Minimum required fields:**

       1. **Session dir name** — the folder name from step 3 (e.g., `auth-changes_2026-02-05-14-30-45`)
       2. **Action type** — the action being executed (e.g., `code`, `review`, `audit`, `analyze`, `plan`)
       3. **Timestamp** — ISO 8601 datetime (e.g., `2026-02-05T14:30:45Z`)
       4. **Description** — one-line summary derived from the task input (the same kebab-case text used in step 1, expanded into a sentence)

       **Bash recipe (extends the variables already defined above):**

       ```bash
       # Variables description, datetime, folder are already set from steps 1-3
       action_type="review"   # set per the action being executed
       iso_timestamp=$(date -u +%Y-%m-%dT%H:%M:%SZ)
       one_line_desc="Review of auth changes for login endpoint"

       cat > "$folder/INDEX.md" <<EOF
       # ${description}

       - **Session dir:** ${description}_${datetime}
       - **Action type:** ${action_type}
       - **Timestamp:** ${iso_timestamp}
       - **Description:** ${one_line_desc}
       EOF
       ```

       **Verification:** After writing, `ls "$folder/INDEX.md"` must succeed. If it does not, the action MUST abort and surface the failure — this is a contract gate, not a soft warning.
       ```

    3. **In the "Contract Contributions" section at the bottom**, append a new bullet under "Output Contract additions:":

       ```
       - Required artifact: `{folder}/INDEX.md` with fields {session-dir, action-type, ISO-timestamp, one-line description} per D-11/D-12
       ```

    DO NOT modify the "Critical Warnings" section. DO NOT change the existing CORRECT/WRONG bash examples. DO NOT delete any existing prose.
  </action>
  <verify>
    <automated>grep -n "Step 5: Create INDEX.md" .claude/actionflows/actions/_abstract/create-log-folder/instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal substring `Step 5: Create INDEX.md (REQUIRED)`.
    - File contains the literal substring `Fifth:** Immediately after` inside the numbered Critical Execution Order list.
    - File contains the literal substring `D-11, D-12` (traceability marker).
    - File contains all four required field labels: `Session dir`, `Action type`, `Timestamp`, `Description`.
    - File contains the bash literal `cat > "$folder/INDEX.md" <<EOF`.
    - The Contract Contributions section contains the literal substring `Required artifact:` and `INDEX.md`.
    - File line endings are LF only (verify: `file .claude/actionflows/actions/_abstract/create-log-folder/instructions.md` does NOT report `CRLF`; or `grep -c $'\r' file` returns 0).
    - Existing sections remain present (grep still matches `Critical Warnings`, `Shell Substitution Warning`, `Contract Contributions`, and the original CORRECT bash example with `description="auth-changes"`).
  </acceptance_criteria>
  <done>create-log-folder instructions now mandate INDEX.md creation with all four required fields, and the contract section reflects the new artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification — next action run produces INDEX.md</name>
  <what-built>
    The `_abstract/create-log-folder` instructions now include Step 5 (Create INDEX.md). Every action that extends this abstract (analyze, audit, code, review, plan, etc. — verified via grep that 20+ agent.md files reference it) will, the next time it runs, produce an INDEX.md inside its log folder.
  </what-built>
  <how-to-verify>
    1. Trigger any action that creates a log folder. Easiest path: run any quick `/gsd:quick` or `/gsd:debug` flow that includes a `code/` or `review/` step, OR manually invoke an agent that extends create-log-folder.
    2. Locate the newly created folder under `.claude/actionflows/logs/{action-type}/`.
    3. Confirm `INDEX.md` exists inside it: `ls .claude/actionflows/logs/{action-type}/{newest-folder}/INDEX.md`
    4. Open the file and verify all four required fields are present: session dir name, action type, ISO timestamp, one-line description.
    5. Confirm older log dirs (the existing 40+ session dirs) are LEFT UNTOUCHED — no backfill happened. (Per D-11.)
  </how-to-verify>
  <files>(none — verification only)</files>
  <action>This is a manual verification checkpoint. The orchestrator pauses after Task 1 lands and waits for the human to run any action that extends create-log-folder (e.g., a /gsd:quick or any agent invocation that creates a log session folder), then confirm the new folder contains an INDEX.md with the four required fields.</action>
  <verify>Manual: human inspects newest log folder under .claude/actionflows/logs/{action-type}/ and confirms INDEX.md exists with all four fields.</verify>
  <done>Human types "approved" after confirming a freshly created log session folder contains INDEX.md with session dir, action type, ISO timestamp, and one-line description.</done>
  <resume-signal>Type "approved" if INDEX.md was created with all four fields. Type "issues: ..." with details otherwise.</resume-signal>
</task>

</tasks>

<verification>
- Grep verification of the instructions file proves Step 5 is present.
- Manual checkpoint confirms the orchestrator actually honors the new step on its next action.
</verification>

<success_criteria>
D-11 and D-12 satisfied: new log session dirs include an INDEX.md with the required fields; legacy dirs are accepted as-is.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-history-and-memory-lifecycle-management/999.1-06-SUMMARY.md`
</output>
