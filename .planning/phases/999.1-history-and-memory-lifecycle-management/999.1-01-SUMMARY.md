---
phase: 999.1-history-and-memory-lifecycle-management
plan: 01
subsystem: history-and-memory-lifecycle
tags: [tdd, wave-0, types, test-scaffolding, shared-contract]
dependency_graph:
  requires: []
  provides:
    - "@afw/shared LedgerEntry type"
    - "@afw/shared LedgerFilter type"
    - "Wave 0 failing test harness for LedgerService, gateTraceSummarizer, historyPruner, learningsArchiver, history.ledger route"
  affects:
    - "Plans 02-05 of phase 999.1 (TDD green targets)"
tech-stack:
  added: []
  patterns: [tdd-red-first, shared-type-contract]
key-files:
  created:
    - packages/shared/src/ledger.ts
    - packages/backend/src/__tests__/ledger.test.ts
    - packages/backend/src/__tests__/gateTraceSummarizer.test.ts
    - packages/backend/src/__tests__/historyPruner.test.ts
    - packages/backend/src/__tests__/learningsArchiver.test.ts
    - packages/backend/src/__tests__/routes/history.ledger.test.ts
  modified:
    - packages/shared/src/index.ts
decisions:
  - "LedgerEntry.outcome uses string literal union 'pass' | 'fail' | 'unknown' (not boolean) for tri-state summarization"
  - "LedgerFilter.outcome restricted to 'pass' | 'fail' — 'unknown' is an implementation detail, not a query axis"
  - "reason field cap (200 chars) enforced at call site, not at type level, to keep the type structural"
  - "Wave 0 stubs intentionally import missing modules to force RED state — first run MUST fail"
metrics:
  duration: "~8min"
  tasks: 2
  files: 7
  completed: "2026-04-10"
requirements: [D-03, D-04]
---

# Phase 999.1 Plan 01: Ledger Contract & Wave 0 Test Scaffolds Summary

**One-liner:** Established the `LedgerEntry`/`LedgerFilter` shared type contract and five failing Vitest stub files that later waves must turn green.

## What Was Built

### Task 1 — Shared Ledger Contract
- Created `packages/shared/src/ledger.ts` exporting `LedgerEntry` (timestamp, sessionId?, chainId, gateId, gateName, outcome, healthScoreDelta?, reason, sourceTraceTimestamp) and `LedgerFilter` (fromTimestamp, toTimestamp, gateId, outcome, limit).
- Wired barrel re-export in `packages/shared/src/index.ts` via `export * from './ledger.js';`.
- `pnpm --filter @afw/shared build` passes.
- Commit: `3196576`

### Task 2 — Wave 0 Test Scaffolds
Five Vitest test files created, each importing a not-yet-existent target module to enforce TDD RED state:

| Test file | Target module | Requirements covered |
|-----------|---------------|----------------------|
| `ledger.test.ts` | `services/ledgerService.js` | D-03, D-04, D-05 |
| `gateTraceSummarizer.test.ts` | `services/gateTraceSummarizer.js` | D-06 |
| `historyPruner.test.ts` | `services/healthScoreCalculator.js` (prune) | D-01, D-02 |
| `learningsArchiver.test.ts` | `services/learningsArchiver.js` | D-08, D-09, D-10 |
| `routes/history.ledger.test.ts` | `GET /api/history/ledger` | D-05 |

- All tests use `.js` suffixed imports (NodeNext ES module resolution).
- `pnpm --filter @afw/backend test --run ledger` reports **2 test files failed** as required for Wave 0 RED — modules resolve to missing imports or assertions fire against unimplemented routes.
- Commit: `12873b7`

## Deviations from Plan

None — plan executed exactly as written.

The plan authored `ledger.test.ts` importing `../services/ledgerService.js` which does not exist; this is intentional RED-state behavior per TDD Wave 0, so no deviation required.

## Verification Results

- `pnpm --filter @afw/shared build` → exit 0
- `pnpm --filter @afw/backend test --run ledger` → exit 1 (**RED as intended**)
- All acceptance-criteria grep checks satisfied:
  - `export interface LedgerEntry` in ledger.ts ✓
  - `export interface LedgerFilter` in ledger.ts ✓
  - `outcome: 'pass' | 'fail' | 'unknown'` in ledger.ts ✓
  - `sourceTraceTimestamp: Timestamp` in ledger.ts ✓
  - `export * from './ledger.js';` in index.ts ✓
  - All 5 test files exist at exact paths with required `describe(...)` strings ✓

## Known Stubs

None. The contract file contains complete type definitions. Test files are intentional RED-state stubs required by TDD Wave 0 protocol — they are documented as such and will be turned green by Plans 02-05.

## Impact on Downstream Plans

- Plan 02 will implement `LedgerService` + `gateTraceSummarizer` (turns `ledger.test.ts` + `gateTraceSummarizer.test.ts` green).
- Plan 03 will implement `HealthScoreCalculator.prune` (turns `historyPruner.test.ts` green).
- Plan 04 will implement `LearningsArchiver` (turns `learningsArchiver.test.ts` green).
- Plan 05 will implement `GET /api/history/ledger` route (turns `routes/history.ledger.test.ts` green).

## Self-Check: PASSED

Files verified:
- FOUND: packages/shared/src/ledger.ts
- FOUND: packages/shared/src/index.ts (barrel export line present)
- FOUND: packages/backend/src/__tests__/ledger.test.ts
- FOUND: packages/backend/src/__tests__/gateTraceSummarizer.test.ts
- FOUND: packages/backend/src/__tests__/historyPruner.test.ts
- FOUND: packages/backend/src/__tests__/learningsArchiver.test.ts
- FOUND: packages/backend/src/__tests__/routes/history.ledger.test.ts

Commits verified:
- FOUND: 3196576 (Task 1: shared ledger types)
- FOUND: 12873b7 (Task 2: Wave 0 test scaffolds)
