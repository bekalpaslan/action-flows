---
phase: 999.1-history-and-memory-lifecycle-management
verified: 2026-04-10T00:00:00Z
status: human_needed
score: 11/12 must-haves verified
re_verification: false
human_verification:
  - test: "Trigger any action that creates a log folder (e.g., run /gsd:quick or any code/review agent step)"
    expected: "The newly created session dir under .claude/actionflows/logs/{action-type}/ contains an INDEX.md with all four required fields: session dir name, action type, ISO timestamp, one-line description"
    why_human: "Plan 06 Task 2 is a blocking manual checkpoint — programmatic verification cannot run an action to observe runtime behavior of create-log-folder instructions"
---

# Phase 999.1: History and Memory Lifecycle Management — Verification Report

**Phase Goal:** Implement 3-layer history model (raw/ledger/canonical) to prevent unbounded growth of gate-traces.jsonl, ActionFlows logs, and LEARNINGS.md. Add TTL for raw operational data, create searchable execution ledger, and establish promotion pipeline from raw to canonical. Fix missing logs/INDEX.md trust gap.
**Verified:** 2026-04-10
**Status:** human_needed — all automated checks pass; one blocking manual checkpoint from Plan 06 remains
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | LedgerEntry type is importable from @afw/shared | VERIFIED | `packages/shared/src/ledger.ts` exists with `export interface LedgerEntry` + `export interface LedgerFilter`; re-exported from `packages/shared/src/index.ts` line 685 |
| 2 | LedgerService appends LedgerEntry rows to packages/backend/data/ledger.jsonl | VERIFIED | `packages/backend/src/services/ledgerService.ts` — `append()` and `appendBatch()` write to configurable path, default `path.join(process.cwd(), 'data', 'ledger.jsonl')` |
| 3 | LedgerService.query() filters by gateId, outcome, time range, limit | VERIFIED | `query(filter: LedgerFilter)` at line 45 — filters gateId, outcome, fromTimestamp, toTimestamp, slice(-limit) |
| 4 | summarizeTrace() converts GateTrace to LedgerEntry or null for malformed input | VERIFIED | `packages/backend/src/services/gateTraceSummarizer.ts` — pure function, null guard on gateId/chainId/timestamp, reason capped at 200 chars, sourceTraceTimestamp set |
| 5 | HealthScoreCalculator.prune() atomically rewrites gate-traces.jsonl and updates in-memory traceBuffer | VERIFIED | `public prune(): GateTrace[]` at line 97; `this.traceBuffer = survivors` line 106; tmpPath + `renameSync` lines 112-117; log prefix `[HealthScore] Pruned` line 122 |
| 6 | CleanupService.runCleanup() invokes prune() and promotes traces to ledger via summarizeTrace before they are dropped | VERIFIED | `cleanup.ts` — `pruneGateTraces()` at line 112 calls `healthScoreCalculator.prune()`, maps through `summarizeTrace`, calls `appendBatch`; `[Cleanup] Promoted` log line 127 |
| 7 | When LEARNINGS.md has > 50 entries, oldest entries are moved to LEARNINGS-archive-YYYY.md, active file retains exactly 50 | VERIFIED | `packages/backend/src/services/learningsArchiver.ts` — `MAX_ACTIVE_ENTRIES = 50`, CRLF-safe (`replace(/\r\n/g, '\n')`), year-bucketed archive, `appendFileSync` for archives, `writeFileSync` for active |
| 8 | CleanupService.runCleanup() invokes archiveIfNeeded() on every daily run | VERIFIED | `cleanup.ts` line 86: `await this.archiveLearnings()` after `pruneGateTraces()`, which calls `learningsArchiver.archiveIfNeeded()` |
| 9 | GET /api/history/ledger returns 200 with { entries: LedgerEntry[] } and supports query filters | VERIFIED | `packages/backend/src/routes/history.ts` line 110: `router.get('/ledger', ...)` — imports `ledgerService` and `LedgerFilter`; calls `ledgerService.query(filter)`; returns `res.json({ entries })` |
| 10 | Backend index.ts wires LedgerService and LearningsArchiver into CleanupService before start() | VERIFIED | `index.ts` lines 809, 812, 815: `setPruneDeps` then `setLearningsArchiver` then `cleanupService.start()` in correct order |
| 11 | create-log-folder instructions mandate INDEX.md creation as Step 5 with required fields | VERIFIED | `.claude/actionflows/actions/_abstract/create-log-folder/instructions.md` contains "Step 5: Create INDEX.md (REQUIRED)", "Fifth: Immediately after", all four field labels, bash recipe, D-11/D-12 markers, LF line endings |
| 12 | Next action run actually produces INDEX.md (runtime behavior of updated instructions) | NEEDS HUMAN | Cannot be verified programmatically — requires triggering an agent that extends create-log-folder |

**Score:** 11/12 truths verified (automated)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/shared/src/ledger.ts` | LedgerEntry + LedgerFilter types | VERIFIED | 42 lines; both interfaces present with all required fields (timestamp, chainId, gateId, gateName, outcome, reason, sourceTraceTimestamp, healthScoreDelta?, sessionId?) |
| `packages/shared/src/index.ts` | Re-exports ledger.ts | VERIFIED | Line 685: `export * from './ledger.js';` |
| `packages/backend/src/services/ledgerService.ts` | LedgerService class: append, appendBatch, query | VERIFIED | 67 lines; all three methods present; singleton export `ledgerService`; log prefix `[LedgerService]` twice |
| `packages/backend/src/services/gateTraceSummarizer.ts` | Pure summarizeTrace(GateTrace): LedgerEntry \| null | VERIFIED | 29 lines; pure function (no fs/path/ledgerService imports); `.slice(0, 200)` present; `sourceTraceTimestamp: trace.timestamp` present |
| `packages/backend/src/services/healthScoreCalculator.ts` | public prune(): GateTrace[] | VERIFIED | `public prune(): GateTrace[]` at line 97; atomic rewrite via tmpPath + renameSync; buffer sync |
| `packages/backend/src/services/cleanup.ts` | pruneGateTraces + archiveLearnings steps | VERIFIED | Both private methods present; `setPruneDeps` and `setLearningsArchiver` setters; correct order in runCleanup |
| `packages/backend/src/services/learningsArchiver.ts` | LearningsArchiver class, 50-cap, CRLF-safe | VERIFIED | 95 lines; `MAX_ACTIVE_ENTRIES = 50`; `replace(/\r\n/g, '\n')`; year-scoped archive naming; singleton export |
| `packages/backend/src/routes/history.ts` | GET /api/history/ledger endpoint | VERIFIED | Route at line 110; imports both `ledgerService` and `LedgerFilter`; all five query param filters handled |
| `.claude/actionflows/actions/_abstract/create-log-folder/instructions.md` | Step 5 INDEX.md instruction | VERIFIED | All acceptance criteria met: Step 5 heading, Fifth numbered item, D-11/D-12, four field labels, bash recipe, Contract Contributions entry, LF-only line endings |
| `.gitattributes` | LEARNINGS.md LF enforcement | VERIFIED | Lines 3-4: `LEARNINGS.md text eol=lf` and `LEARNINGS-archive-*.md text eol=lf` |
| `packages/backend/src/__tests__/ledger.test.ts` | Wave 0 test stub | VERIFIED | File exists |
| `packages/backend/src/__tests__/gateTraceSummarizer.test.ts` | Wave 0 test stub | VERIFIED | File exists |
| `packages/backend/src/__tests__/historyPruner.test.ts` | Wave 0 test stub | VERIFIED | File exists |
| `packages/backend/src/__tests__/learningsArchiver.test.ts` | Wave 0 test stub | VERIFIED | File exists |
| `packages/backend/src/__tests__/routes/history.ledger.test.ts` | Wave 0 test stub | VERIFIED | File exists |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `packages/shared/src/index.ts` | `packages/shared/src/ledger.ts` | re-export | WIRED | `export * from './ledger.js'` line 685 |
| `packages/backend/src/services/ledgerService.ts` | `packages/shared/src/ledger.ts` | type import | WIRED | `import type { LedgerEntry, LedgerFilter } from '@afw/shared'` line 3 |
| `packages/backend/src/services/cleanup.ts` | `packages/backend/src/services/ledgerService.ts` | setPruneDeps injection | WIRED | `pruneDeps.ledgerService.appendBatch(...)` line 126; injected at startup |
| `packages/backend/src/index.ts` | `packages/backend/src/services/cleanup.ts` | setPruneDeps call before start | WIRED | Lines 809 (`setPruneDeps`), 812 (`setLearningsArchiver`), 815 (`start()`) — correct order |
| `packages/backend/src/services/cleanup.ts` | `packages/backend/src/services/learningsArchiver.ts` | setLearningsArchiver | WIRED | `setLearningsArchiver` setter at line 34; `archiveLearnings()` calls `archiveIfNeeded()` |
| `packages/backend/src/routes/history.ts` | `packages/backend/src/services/ledgerService.ts` | ledgerService.query() call | WIRED | `ledgerService.query(filter)` line 127; `res.json({ entries })` line 128 |
| `.claude/actionflows/actions/_abstract/create-log-folder/instructions.md` | `.claude/actionflows/logs/` | Step 5 INDEX.md instruction | DOCUMENTED | Instructions mandate INDEX.md creation; runtime enforcement requires human verification |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `routes/history.ts /ledger` | `entries` | `ledgerService.query(filter)` reads `data/ledger.jsonl` from disk | Yes — reads actual JSONL file written by prune pipeline | FLOWING |
| `services/cleanup.ts pruneGateTraces()` | `pruned` traces | `healthScoreCalculator.prune()` partitions live `traceBuffer` | Yes — operates on real in-memory buffer | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| LedgerService singleton exported | `node -e "const {ledgerService} = require('./packages/backend/dist/services/ledgerService.js')"` | Not run (requires compiled dist) | SKIP — no pre-built dist |
| Test stubs exist and reference correct describe labels | `grep "LedgerService (D-03, D-04)" packages/backend/src/__tests__/ledger.test.ts` | Match found | PASS |
| Wiring order: setPruneDeps before start() | Line numbers 809 < 815 in index.ts | Confirmed | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| D-01 | Plan 03 | 7-day TTL for raw gate traces | SATISFIED | `WINDOW_7D_MS = 7*24*60*60*1000` in healthScoreCalculator; prune() partitions by cutoff |
| D-02 | Plan 03 | CleanupService extension for prune orchestration | SATISFIED | `pruneGateTraces()` step in runCleanup() |
| D-03 | Plans 01, 02 | LedgerEntry type contract | SATISFIED | All required fields present in LedgerEntry interface |
| D-04 | Plans 01, 02 | LedgerService JSONL persistence at data/ledger.jsonl | SATISFIED | Default path `path.join(process.cwd(), 'data', 'ledger.jsonl')` |
| D-05 | Plan 05 | GET /api/history/ledger endpoint with filters | SATISFIED | Route exists with all 5 filter params |
| D-06 | Plans 02, 03 | Auto-promote raw traces to ledger at TTL | SATISFIED | `summarizeTrace` pipeline in pruneGateTraces() |
| D-08 | Plan 04 | 50-entry cap on active LEARNINGS.md | SATISFIED | `MAX_ACTIVE_ENTRIES = 50` enforced in archiveIfNeeded() |
| D-09 | Plan 04 | Year-scoped archive files | SATISFIED | `LEARNINGS-archive-${year}.md` naming |
| D-10 | Plan 04 | Append-only oldest-first archive | SATISFIED | `toArchive = entries.slice(0, overflow)` (oldest); `appendFileSync` for archive files |
| D-11 | Plan 06 | INDEX.md created in every new log session dir | SATISFIED (doc) / NEEDS HUMAN (runtime) | Instructions updated; runtime behavior requires human verification |
| D-12 | Plan 06 | INDEX.md minimum content fields | SATISFIED (doc) / NEEDS HUMAN (runtime) | Four required fields documented in instructions |

---

### Anti-Patterns Found

No blockers found. Key observations:

- `gateTraceSummarizer.ts` is pure — no fs/path/ledgerService imports confirmed.
- `LearningsArchiver` correctly uses CRLF normalization before parsing.
- Cleanup.ts gracefully skips pruneGateTraces and archiveLearnings when deps are not wired (`[Cleanup] ... skipped — ... not wired`).
- `ledgerService.ts` — `query()` skips malformed lines silently, preventing parse errors from corrupting results.

---

### Human Verification Required

#### 1. INDEX.md Runtime Production (Plan 06, Task 2 — blocking manual checkpoint)

**Test:** Trigger any action that creates a log folder. Easiest path: run `/gsd:quick` or any flow with a `code/` or `review/` step.
**Expected:**
- A new folder appears under `.claude/actionflows/logs/{action-type}/`
- That folder contains an `INDEX.md`
- The file has all four required fields: session dir name, action type, ISO timestamp, one-line description
- Older existing log dirs are left untouched (no backfill)

**Why human:** Plan 06 Task 2 is explicitly a blocking manual checkpoint. The instructions file has been verified to contain the required Step 5 mandate, but whether agents actually honor it at runtime cannot be confirmed without executing an action.

---

### Gaps Summary

No automated gaps. One blocking manual checkpoint (Plan 06 Task 2) is pending human confirmation. The instructions file change is verified correct. The runtime behavior of agents following the updated create-log-folder abstract is the only unverifiable item.

---

_Verified: 2026-04-10_
_Verifier: Claude (gsd-verifier)_
