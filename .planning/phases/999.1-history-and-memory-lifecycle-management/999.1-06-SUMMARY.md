---
phase: 999.1-history-and-memory-lifecycle-management
plan: 06
subsystem: framework-instructions
tags: [logging, framework, abstract-action, index-md, D-11, D-12]
requires: []
provides:
  - "Mandatory INDEX.md creation in every new log session directory"
  - "Required fields: session dir, action type, ISO timestamp, description"
affects:
  - ".claude/actionflows/actions/_abstract/create-log-folder/instructions.md"
tech-stack:
  added: []
  patterns:
    - "Abstract action instructions extended via numbered step + sub-section + contract additions"
key-files:
  created: []
  modified:
    - .claude/actionflows/actions/_abstract/create-log-folder/instructions.md
decisions:
  - "No backfill of legacy log dirs (per D-11 — legacy accepted as-is)"
  - "Contract gate on INDEX.md failure (abort action, not soft warning)"
metrics:
  tasks: 2
  files: 1
  completed: 2026-04-10
---

# Phase 999.1 Plan 06: Enforce INDEX.md on Log Session Creation Summary

One-liner: Updated the `_abstract/create-log-folder` framework instructions to mandate an `INDEX.md` (with session dir, action type, ISO timestamp, one-line description) inside every new log session directory, closing D-11 and D-12.

## What Changed

- Added Step 5 to the Critical Execution Order numbered list requiring INDEX.md creation immediately after `mkdir -p`.
- Added a new "Step 5: Create INDEX.md (REQUIRED)" sub-section with the four required fields and a bash recipe (`cat > "$folder/INDEX.md" <<EOF ... EOF`).
- Appended a new bullet to Contract Contributions → Output Contract additions listing the required `{folder}/INDEX.md` artifact.
- Preserved all existing prose: Critical Warnings, Shell Substitution Warning, CORRECT/WRONG bash examples, and Contract Contributions structure.

## Tasks Completed

| Task | Name                                      | Commit   | Files                                                                               |
| ---- | ----------------------------------------- | -------- | ----------------------------------------------------------------------------------- |
| 1    | Add Step 5 (Create INDEX.md) instructions | a766a72  | .claude/actionflows/actions/_abstract/create-log-folder/instructions.md             |
| 2    | Manual verification checkpoint            | deferred | (none — human-verify; deferred to post-wave verification)                           |

## Deviations from Plan

None — plan executed exactly as written. All acceptance criteria grep checks pass (Step 5 header, `Fifth:** Immediately after`, `D-11, D-12`, four field labels, heredoc literal, Contract `Required artifact:`/`INDEX.md`, zero CRLF, and all preserved sections intact).

## Known Stubs

None.

## Deferred Items

- **Task 2 manual verification (checkpoint:human-verify):** The human-verify checkpoint is deferred to post-wave manual verification. Verification steps: trigger any action that creates a log folder (e.g., `/gsd:quick` with a code/ or review/ step), locate the newly created folder under `.claude/actionflows/logs/{action-type}/`, confirm `INDEX.md` exists with all four required fields, and confirm legacy log dirs were left untouched. No backfill per D-11.

## Requirements Satisfied

- D-11: INDEX.md enforced at log dir creation; legacy accepted.
- D-12: Minimum content fields (session dir, action type, ISO timestamp, one-line description) documented and required.

## Self-Check

- FOUND: .claude/actionflows/actions/_abstract/create-log-folder/instructions.md (Step 5 verified via grep)
- FOUND: commit a766a72
- Verified: CRLF count = 0; all acceptance grep checks pass; existing Critical Warnings, Shell Substitution Warning, and Contract Contributions sections still present.

## Self-Check: PASSED
