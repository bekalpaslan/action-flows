---
phase: 11-test-stub-realignment-doc-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md
  - .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md
autonomous: true
requirements: [cleanup]
gap_closure: true
must_haves:
  truths:
    - "REQUIREMENTS.md traceability table shows SESSION-01..09, STATUS-01..03, CHAT-01..08, NEURAL-01..07, SAFETY-01..05 as Complete (32 rows flipped from Pending, cross-referenced against phase VERIFICATION files)"
    - "REQUIREMENTS.md bullet list shows the same 32 requirements with [x] checkboxes (flipped from [ ])"
    - "REQUIREMENTS.md has a Satisfied count line reflecting the new [x] count after the flip"
    - "Phase 04.1 VERIFICATION.md exists with status: passed in frontmatter, covering all 6 ROADMAP success criteria with grep-based evidence"
    - "Phase 999.1 VALIDATION.md frontmatter shows nyquist_compliant: true and wave_0_complete: true (both flipped from false)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table reflects real milestone state — Pending rows flipped to Complete for phase 6/7/8 REQ-IDs"
      contains: "| SESSION-01 | Phase 6 | Complete |"
    - path: ".planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md"
      provides: "Phase 04.1 verification report synthesized from 5 SUMMARY.md files, grep-based validation of 6 ROADMAP success criteria"
      contains: "status: passed"
      min_lines: 60
    - path: ".planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md"
      provides: "Frontmatter Nyquist flags reflect actual Wave 0 completion state"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/06-agent-sessions-status/06-VERIFICATION.md"
      via: "cross-reference phase verification status: passed before flipping SESSION/STATUS checkboxes"
      pattern: "status: passed"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/07-chat-panel/07-VERIFICATION.md"
      via: "cross-reference phase verification status: passed before flipping CHAT checkboxes"
      pattern: "status: passed"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/08-neural-validation-safety/08-VERIFICATION.md"
      via: "cross-reference phase verification status: passed before flipping NEURAL/SAFETY checkboxes"
      pattern: "status: passed"
---

<objective>
Clear documentation staleness across phases 6, 7, 8, 04.1, and 999.1 before v4.8 milestone archival. Phase executors completed the work but didn't update the upstream tracking documents, leaving a gap between verified reality (per phase VERIFICATION files) and tracker state (in REQUIREMENTS.md and frontmatter flags).

Purpose: All 74 milestone requirements are already verified satisfied in code per the corrected v4.8 audit. This plan updates the documentation surfaces to match — doc work only, no code changes. Three target files:
1. `.planning/REQUIREMENTS.md` — flip 32 checkboxes and 32 traceability-table rows for requirements verified in phase 6/7/8 VERIFICATION files (CONTEXT.md D-08 says 28 but the actual list resolves to 32 — SESSION 9, STATUS 3, CHAT 8, NEURAL 7, SAFETY 5)
2. `.planning/phases/04.1-framework-docs-realignment-.../04.1-VERIFICATION.md` — generate from scratch (phase complete but verification never run)
3. `.planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md` — flip two frontmatter flags that stayed false after Wave 0 completed

Output: 3 files modified (1 new, 2 updated). No code changes. Git diff must be entirely in `.planning/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-test-stub-realignment-doc-cleanup/11-CONTEXT.md
@.planning/v4.8-MILESTONE-AUDIT.md
@.planning/phases/06-agent-sessions-status/06-VERIFICATION.md
@.planning/phases/07-chat-panel/07-VERIFICATION.md
@.planning/phases/08-neural-validation-safety/08-VERIFICATION.md
@.planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md

<interfaces>
<!-- Key structural facts the executor needs. Extracted via grep and Read tools during planning. -->

**REQUIREMENTS.md baseline counts (before any changes):**
- Bullet checkboxes: 46 [x] plus 28 [ ] equals 74 total
- Traceability table: 34 Complete plus 40 Pending equals 74 total
- Pending rows split: 32 for phases 6/7/8 (this plan's scope) plus 8 for phase 9 (OUT OF SCOPE) plus 1 for phase 5 (OUT OF SCOPE — PIPE-06) equals 41. Wait — baseline count is 40 Pending, not 41. Recheck: the 8 out-of-scope rows are BENCH-01, BENCH-02, BENCH-03, BENCH-04, BENCH-08, FLOW-01, FLOW-02 (7 rows, not 8) plus PIPE-06 equals 8 out-of-scope Pending rows. Therefore 40 - 8 = 32 in-scope Pending rows. Confirms 32 is the flip count.
- Post-flip target: 8 Pending rows remaining, 32 new Complete rows (total 34 baseline + 32 = 66 Complete).

**REQUIREMENTS.md exact REQ-IDs to flip (32 total, grouped by phase):**
- Phase 6 VERIFICATION.md (12 IDs): SESSION-01, SESSION-02, SESSION-03, SESSION-04, SESSION-05, SESSION-06, SESSION-07, SESSION-08, SESSION-09, STATUS-01, STATUS-02, STATUS-03
- Phase 7 VERIFICATION.md (8 IDs): CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07, CHAT-08
- Phase 8 VERIFICATION.md (12 IDs): NEURAL-01, NEURAL-02, NEURAL-03, NEURAL-04, NEURAL-05, NEURAL-06, NEURAL-07, SAFETY-01, SAFETY-02, SAFETY-03, SAFETY-04, SAFETY-05

Per D-09 the executor MUST cross-reference EACH REQ-ID against its phase VERIFICATION.md body (not just the top-level status: passed). If any specific REQ-ID is NOT explicitly marked as verified in its phase's VERIFICATION.md, that specific checkbox MUST NOT be flipped — report discrepancies in the summary.

**REQUIREMENTS.md table row format (exact match):**
Current: `| SESSION-01 | Phase 6 | Pending |`
Target:  `| SESSION-01 | Phase 6 | Complete |`

NOTE on D-08 wording: CONTEXT.md says "Pending → Satisfied" but the actual file uses `Complete` as the positive status value (verified: `grep -c 'Complete' .planning/REQUIREMENTS.md` returns 34 baseline occurrences; `grep -c 'Satisfied' .planning/REQUIREMENTS.md` returns 0). Use `Complete` to match existing conventions.

**REQUIREMENTS.md bullet format (exact match):**
Current: `- [ ] **SESSION-01**: Persistent Claude session per workbench via Agent SDK (...)`
Target:  `- [x] **SESSION-01**: Persistent Claude session per workbench via Agent SDK (...)`

**REQUIREMENTS.md coverage count block (lines ~234-237):**
Current:
```
**Coverage:**
- v1 requirements: 74 total
- Mapped to phases: 74
- Unmapped: 0
```
Target — add Satisfied line:
```
**Coverage:**
- v1 requirements: 74 total
- Mapped to phases: 74
- Satisfied: {count} / 74
- Unmapped: 0
```
where `{count}` is the actual post-flip `- [x]` count (expected 46 + 32 = 78 IF all 32 cross-reference cleanly, OR fewer if some REQ-IDs are not found in phase VERIFICATION bodies).

Wait — the v1 total is 74 but 46 + 32 = 78 exceeds 74. Recheck: the 46 baseline [x] count INCLUDES v1 items plus any other sections. Let me clarify: only v1 items should be counted against "Satisfied: N / 74". Use grep scoped to v1 section only, OR accept that the bullet count may exceed 74 if there are non-v1 bullets with [x]. Post-flip target: `grep -c '^- \[x\]' .planning/REQUIREMENTS.md` should return the new total. The executor must compute the actual number and set it in the Coverage block rather than assuming a specific target number.

**Phase 04.1 VERIFICATION.md target path (exact, LONG):**
`.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md`

The phase directory already exists and contains `04.1-01-SUMMARY.md` through `04.1-05-SUMMARY.md`.

**Phase 04.1 ROADMAP success criteria (6 items to verify):**
From ROADMAP.md Phase 04.1 section:
1. Zero cosmic terminology exists in any .md file under `.claude/actionflows/` (excluding logs/)
2. CONTEXTS.md routes to exactly 7 workbenches (Work, Explore, Review, PM, Settings, Archive, Studio) plus system-health auto-target and editor manual context
3. `docs/` directory deleted, `.claude/actionflows/logs/` content deleted, `cosmic/MODEL.md` deleted
4. All broken cross-references to deleted `docs/` paths fixed
5. Functional gate vocabulary (Pre-Action Gate, Gate 1-14) preserved
6. Flow reassignment complete: maintenance flows to settings/work, intel flows to explore

**Phase 999.1 VALIDATION.md frontmatter (exact current state, lines 1-8):**
The frontmatter block (between YAML delimiters) contains these six key-value pairs in order:
- phase: 999.1
- slug: history-and-memory-lifecycle-management
- status: draft
- nyquist_compliant: false
- wave_0_complete: false
- created: 2026-04-10

Target (only 2 lines change — nyquist_compliant and wave_0_complete):
After the edit, the six frontmatter key-value pairs MUST be (in order):
- phase: 999.1
- slug: history-and-memory-lifecycle-management
- status: draft
- nyquist_compliant: true
- wave_0_complete: true
- created: 2026-04-10

Leave `status: draft` unchanged — that's a separate concern from Nyquist flags per D-14/D-15 scope.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Flip REQUIREMENTS.md checkboxes and traceability rows for verified phase 6/7/8 requirements per D-08/D-09/D-10</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (CURRENT state — 46 [x], 28 [ ], 34 Complete, 40 Pending)
    - .planning/phases/06-agent-sessions-status/06-VERIFICATION.md (frontmatter status: passed + body coverage of SESSION-01..09 and STATUS-01..03)
    - .planning/phases/07-chat-panel/07-VERIFICATION.md (frontmatter status: passed + body coverage of CHAT-01..08)
    - .planning/phases/08-neural-validation-safety/08-VERIFICATION.md (frontmatter status: passed + body coverage of NEURAL-01..07 and SAFETY-01..05)
    - .planning/phases/11-test-stub-realignment-doc-cleanup/11-CONTEXT.md sections D-08, D-09, D-10
  </read_first>
  <action>
    **Step 1 — Cross-reference each REQ-ID against its phase VERIFICATION.md body per D-09:**

    For each of the 32 candidate REQ-IDs below, read the corresponding phase VERIFICATION.md and confirm it is explicitly mentioned/verified in the body (not just the top-level status: passed). If any REQ-ID is NOT explicitly covered, omit it from the flip and note it in the summary.

    Candidate list (32 IDs):
    - Phase 6 (12): SESSION-01, SESSION-02, SESSION-03, SESSION-04, SESSION-05, SESSION-06, SESSION-07, SESSION-08, SESSION-09, STATUS-01, STATUS-02, STATUS-03
    - Phase 7 (8): CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07, CHAT-08
    - Phase 8 (12): NEURAL-01, NEURAL-02, NEURAL-03, NEURAL-04, NEURAL-05, NEURAL-06, NEURAL-07, SAFETY-01, SAFETY-02, SAFETY-03, SAFETY-04, SAFETY-05

    Verify coverage with grep:
    ```
    for id in SESSION-01 SESSION-02 SESSION-03 SESSION-04 SESSION-05 SESSION-06 SESSION-07 SESSION-08 SESSION-09 STATUS-01 STATUS-02 STATUS-03; do
      count=$(grep -c "$id" .planning/phases/06-agent-sessions-status/06-VERIFICATION.md)
      echo "$id: $count"
    done
    ```
    Repeat for Phase 7 (CHAT-*) and Phase 8 (NEURAL-*, SAFETY-*). Any `count: 0` result means DO NOT flip that specific ID.

    **Step 2 — Flip the bullet checkboxes for each verified ID:**

    Use the Edit tool (NOT sed — the file has markdown special chars). For each verified REQ-ID, apply:
    ```
    Edit file=".planning/REQUIREMENTS.md"
      old_string="- [ ] **SESSION-01**: Persistent Claude session per workbench via Agent SDK (`@anthropic-ai/claude-agent-sdk`)"
      new_string="- [x] **SESSION-01**: Persistent Claude session per workbench via Agent SDK (`@anthropic-ai/claude-agent-sdk`)"
    ```
    Repeat for every cross-referenced ID. Use the exact bullet text including backticks and em-dashes — the file uses UTF-8 special chars.

    **Step 3 — Flip the traceability table rows for each verified ID:**

    In the traceability table section (lines ~181-212), for each cross-referenced ID:
    ```
    Edit file=".planning/REQUIREMENTS.md"
      old_string="| SESSION-01 | Phase 6 | Pending |"
      new_string="| SESSION-01 | Phase 6 | Complete |"
    ```
    Use `Complete` NOT `Satisfied` — the existing table uses `Complete` as the positive value (verified: `grep -c 'Complete' .planning/REQUIREMENTS.md` returns 34 baseline; zero `Satisfied` occurrences).

    **Step 4 — Update the coverage count block per D-10:**

    After all flips, count the new checkboxes:
    ```
    grep -c "^- \[x\]" .planning/REQUIREMENTS.md
    ```
    Record the resulting count as `{new_count}`. Locate the Coverage block near line 234:
    ```
    **Coverage:**
    - v1 requirements: 74 total
    - Mapped to phases: 74
    - Unmapped: 0
    ```
    Edit to add a Satisfied line:
    ```
    **Coverage:**
    - v1 requirements: 74 total
    - Mapped to phases: 74
    - Satisfied: {new_count}
    - Unmapped: 0
    ```

    **Step 5 — Update the "Last updated" line at the bottom:**

    Locate `*Last updated: 2026-04-02 after roadmap creation*` and change to `*Last updated: 2026-04-11 (Phase 11 cleanup — flipped {N} checkboxes for verified phase 6/7/8 requirements)*` where `{N}` is the actual flip count.

    **DO NOT TOUCH:**
    - PIPE-06 (Phase 5) — stays Pending
    - BENCH-01, BENCH-02, BENCH-03, BENCH-04, BENCH-08 (Phase 9) — stay Pending
    - FLOW-01, FLOW-02 (Phase 9) — stay Pending
    - Any v2 requirements section
    - The "Out of Scope" table

    Per D-08, D-09, D-10.
  </action>
  <verify>
    <automated>grep -c '^- \[x\]' .planning/REQUIREMENTS.md ; grep -c '^- \[ \]' .planning/REQUIREMENTS.md ; grep -c 'Pending' .planning/REQUIREMENTS.md ; grep -c 'Complete' .planning/REQUIREMENTS.md ; grep -c 'Satisfied:' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \]' .planning/REQUIREMENTS.md` returns 0 (ideal) or at most the number of candidate IDs that FAILED cross-reference (must be documented in summary)
    - `grep -c '^- \[x\]' .planning/REQUIREMENTS.md` is at least 78 (46 baseline + 32 target), OR equal to 46 + N where N is number that cross-referenced cleanly
    - `grep -c 'Pending' .planning/REQUIREMENTS.md` returns exactly 8 (PIPE-06 + BENCH-01..04 + BENCH-08 + FLOW-01 + FLOW-02), OR 8 + any IDs excluded from flip
    - `grep -c 'Complete' .planning/REQUIREMENTS.md` returns at least 66 (34 baseline + 32 target)
    - `grep -c 'Satisfied:' .planning/REQUIREMENTS.md` returns 1 (new Coverage line added)
    - `grep -c '| SESSION-01 | Phase 6 | Complete |' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '| CHAT-01 | Phase 7 | Complete |' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '| NEURAL-01 | Phase 8 | Complete |' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '| PIPE-06 | Phase 5 | Pending |' .planning/REQUIREMENTS.md` returns 1 (PIPE-06 untouched)
    - `grep -c '| BENCH-01 | Phase 9 | Pending |' .planning/REQUIREMENTS.md` returns 1 (BENCH-01 untouched)
    - `grep -c '2026-04-11' .planning/REQUIREMENTS.md` returns at least 1 (Last updated line refreshed)
    - `git diff .planning/REQUIREMENTS.md | grep '^+' | wc -l` is in range 35-40 (32 table rows + 32 bullets - 32 unchanged + ~4 coverage/header lines = roughly 35-40 lines added; this is a sanity check, not a hard gate)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md traceability table and bullet list both reflect the verified state of phase 6/7/8 requirements per phase VERIFICATION.md files. Coverage block includes a Satisfied count. Last updated line refreshed to 2026-04-11. The 8 legitimately-pending rows (PIPE-06 + Phase 9 items) are untouched. Any IDs that could not be cross-referenced are listed in the plan summary for human review.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Synthesize Phase 04.1 VERIFICATION.md from 5 SUMMARY files per D-11/D-12/D-13</name>
  <files>.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-01-SUMMARY.md (Plan 01 — deletion of docs/, logs/, cosmic MODEL.md; 498 files deleted)
    - .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-02-SUMMARY.md (Plan 02 — deep rewrite ORCHESTRATOR.md, CONTEXTS.md, FLOWS.md, ACTIONS.md, project.config.md)
    - .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-03-SUMMARY.md (Plan 03 — deep rewrite CONTRACT.md, CLAUDE.md, actionflows/docs/living/, secondary files; 31 orphaned docs/ cross-refs resolved)
    - .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-04-SUMMARY.md (Plan 04 — batch update agent.md files, flow instructions, onboarding modules, LEARNINGS.md)
    - .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-05-SUMMARY.md (Plan 05 — final batch replace + comprehensive 8-check verification grep passed)
    - .planning/ROADMAP.md Phase 04.1 section (6 success criteria to verify)
    - .planning/phases/11-test-stub-realignment-doc-cleanup/11-CONTEXT.md sections D-11, D-12, D-13
  </read_first>
  <action>
    Generate a new verification report at the target path. The report follows the same structural format as other phase VERIFICATION.md files (frontmatter with status, coverage of success criteria, evidence). Since Phase 04.1 was purely a documentation/deletion phase, verification uses grep-based validation — no code tests needed (per D-12).

    **Use the Write tool** (the file does not exist yet).

    **Target path (EXACT — do not abbreviate):**
    `.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md`

    **File content to write:**

    ```markdown
    ---
    phase: 04.1-framework-docs-realignment
    verified: 2026-04-11T00:00:00Z
    status: passed
    score: 6/6 success criteria verified
    re_verification: false
    method: grep-based (pure docs/deletion phase, no code tests)
    synthesized_from:
      - 04.1-01-SUMMARY.md
      - 04.1-02-SUMMARY.md
      - 04.1-03-SUMMARY.md
      - 04.1-04-SUMMARY.md
      - 04.1-05-SUMMARY.md
    human_verification: []
    ---

    # Phase 04.1: Framework & Docs Realignment Verification Report

    **Phase Goal:** All `.claude/actionflows/` framework files use agentic OS vocabulary with zero cosmic remnants, CONTEXTS.md routes to 7 workbenches, stale `docs/` and `logs/` deleted.

    **Verified:** 2026-04-11T00:00:00Z
    **Status:** PASSED
    **Verification method:** Grep-based validation (no code tests — pure docs/deletion phase per D-12)
    **Re-verification:** No — initial verification, synthesized retroactively per Phase 11 D-11

    ---

    ## Goal Achievement

    All 6 ROADMAP success criteria verified against commits landed by plans 04.1-01 through 04.1-05. Evidence drawn from plan SUMMARY.md files and confirmed via grep against the current repository state.

    ---

    ## Success Criteria Verification

    ### Criterion 1: Zero cosmic terminology in `.claude/actionflows/` (excluding logs/)

    **Status:** SATISFIED

    **Evidence from 04.1-05-SUMMARY.md:** Plan 05 final comprehensive 8-check verification grep run confirms 0 cosmic remnants.

    **Verification commands (re-run for confirmation):**
    ```bash
    grep -r -l "cosmic\|Big Bang\|Void\|Nebula\|Dark Matter" .claude/actionflows/ --exclude-dir=logs 2>/dev/null | wc -l
    # Expected: 0
    ```

    **Exceptions per decisions:**
    - "StarId" type and "Stars/" folder references in SYSTEM.md preserved as TypeScript code identifiers (not cosmic terminology)
    - "sparks of insight" in narrate/agent.md preserved as standard English metaphor (not the cosmic entity "Sparks")
    - "harmony" in code paths (harmonyDetector.ts, harmony:check) preserved as functional vocabulary per Plan 02 decision

    ---

    ### Criterion 2: CONTEXTS.md routes to exactly 7 workbenches + system-health + editor

    **Status:** SATISFIED

    **Evidence from 04.1-02-SUMMARY.md:** CONTEXTS.md rewritten to route to 7 workbenches (Work, Explore, Review, PM, Settings, Archive, Studio) plus system-health auto-target and editor manual context. Plan 05 verification confirmed 9 context sections in CONTEXTS.md.

    **Verification commands:**
    ```bash
    grep -c "^## " .claude/actionflows/CONTEXTS.md
    # Expected: at least 9 context sections (7 workbenches + system-health + editor)
    grep -c "Work\|Explore\|Review\|PM\|Settings\|Archive\|Studio" .claude/actionflows/CONTEXTS.md
    # Expected: at least 7 (one per workbench)
    ```

    ---

    ### Criterion 3: `docs/` deleted, `.claude/actionflows/logs/` content deleted, `cosmic/MODEL.md` deleted

    **Status:** SATISFIED

    **Evidence from 04.1-01-SUMMARY.md:** 498 files deleted across docs/ (108 files), .claude/actionflows/logs/ (389 files), and cosmic/MODEL.md.

    **Verification commands:**
    ```bash
    test ! -d docs/ && echo "docs/ deleted OK"
    test ! -f .claude/actionflows/docs/cosmic/MODEL.md && echo "MODEL.md deleted OK"
    find .claude/actionflows/logs -type f -not -name INDEX.md | wc -l
    # Expected: 0 (only INDEX.md files may remain per 999.1)
    ```

    ---

    ### Criterion 4: All broken cross-references to deleted `docs/` paths fixed

    **Status:** SATISFIED

    **Evidence from 04.1-01-SUMMARY.md and 04.1-03-SUMMARY.md:** Plan 01 identified 31 orphaned references to `docs/` paths in actionflows framework files. Plan 03 resolved all 31 cross-reference cleanups as part of deep rewrite of CONTRACT.md, CLAUDE.md, and secondary critical files. Plan 05 confirmed 0 broken cross-references remaining.

    **Verification commands:**
    ```bash
    grep -r "docs/architecture\|docs/intel\|docs/living" .claude/actionflows/ --exclude-dir=logs 2>/dev/null | wc -l
    # Expected: 0
    ```

    ---

    ### Criterion 5: Functional gate vocabulary (Pre-Action Gate, Gate 1-14) preserved

    **Status:** SATISFIED

    **Evidence from 04.1-05-SUMMARY.md:** Plan 05 verification check 8 confirmed functional gate vocabulary preserved (10+ occurrences).

    **Verification commands:**
    ```bash
    grep -r "Pre-Action Gate\|Gate 1\|Gate 2\|Gate 3\|Gate 4\|Gate 5" .claude/actionflows/ --exclude-dir=logs 2>/dev/null | wc -l
    # Expected: > 0
    ```

    ---

    ### Criterion 6: Flow reassignment complete (maintenance → settings/work, intel → explore)

    **Status:** SATISFIED

    **Evidence from 04.1-02-SUMMARY.md and 04.1-04-SUMMARY.md:** Plan 02 rewrote FLOWS.md with new context mappings. Plan 04 batch-updated flow instructions. Plan 05 verified maintenance context enum values replaced with settings/studio/system-health in templates.

    **Verification commands:**
    ```bash
    grep -c "intel-analysis" .claude/actionflows/CONTEXTS.md .claude/actionflows/FLOWS.md 2>/dev/null
    # Expected: at least 1 referencing explore context
    grep -c "maintenance" .claude/actionflows/CONTEXTS.md
    # Expected: 0 (removed from active context routing per Plan 02)
    ```

    **Note per 04.1 decisions:** `maintenance/` flow directory physical move was declared optional follow-up; context routing was updated but some directory names may persist as historical artifacts.

    ---

    ## Summary

    - **Plans executed:** 5/5 (04.1-01 through 04.1-05)
    - **Success criteria verified:** 6/6
    - **Files touched across all plans:** 498 deleted + 46 rewritten = 544 total file operations
    - **Duration across all plans:** 1 + 13 + 15 + 9 + 9 = 47 minutes
    - **Risk level:** Low — pure deletion + terminology replacement, no behavioral code changes
    - **Net result:** Zero cosmic remnants, 7-workbench routing aligned, stale docs cleared

    ---

    ## Cross-Phase Dependencies Verified

    - Phase 04.1 preserved functional gate vocabulary needed by ORCHESTRATOR.md gate protocol
    - Phase 04.1 preserved code-path references (StarId, harmonyDetector.ts) so Phase 05+ code work was not affected
    - Phase 04.1 left intel-analysis output path updated to `.claude/actionflows/logs/intel/` since docs/ was deleted

    ---

    ## Re-verification

    None needed — this is the initial (retroactive) verification report synthesized during Phase 11 cleanup. All plans completed 2026-04-02; report generated 2026-04-11 per D-11/D-12/D-13.

    ---

    *Phase: 04.1-framework-docs-realignment*
    *Verified: 2026-04-11*
    *Method: grep-based synthesis from 5 plan SUMMARY files*
    ```

    Write the above exact content to the target path. Use the Write tool (file does not exist).

    After writing, run the grep-based verification commands above interactively to confirm the 6 criteria actually hold against the current repo state. Record the actual grep output numbers in the task summary. If any grep command returns an unexpected result (e.g., non-zero cosmic remnants), update the VERIFICATION.md status to `partial` and note the specific failure — DO NOT lie about the result.

    Per D-11, D-12, D-13.
  </action>
  <verify>
    <automated>test -f ".planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md" && grep -c "status: passed" ".planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md" && wc -l ".planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md`
    - `grep -c "status: passed" .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md` returns at least 1
    - `grep -c "Criterion 1\|Criterion 2\|Criterion 3\|Criterion 4\|Criterion 5\|Criterion 6" .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md` returns 6
    - `wc -l .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md` returns at least 100 lines (min_lines: 60 is floor; real synthesis should be longer)
    - `grep -c "synthesized_from" .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md` returns 1
    - All 5 SUMMARY file references present: `grep -c "04.1-01-SUMMARY\|04.1-02-SUMMARY\|04.1-03-SUMMARY\|04.1-04-SUMMARY\|04.1-05-SUMMARY" .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md` returns at least 5
    - The interactive grep validation commands run during the task confirm the 6 success criteria hold against current repo state (no cosmic remnants, no broken docs/ cross-refs, docs/ deleted, etc.) — any mismatches reported in summary with corrected status
  </acceptance_criteria>
  <done>
    Phase 04.1 VERIFICATION.md exists with status: passed, synthesized from all 5 plan SUMMARY files, covers all 6 ROADMAP success criteria with grep-based evidence. Runtime grep validation confirms the criteria still hold against the current repo. Phase 11 Success Criterion 3 from ROADMAP is satisfied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Flip Phase 999.1 VALIDATION.md Nyquist frontmatter flags per D-14/D-15/D-16</name>
  <files>.planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md (CURRENT frontmatter: nyquist_compliant: false, wave_0_complete: false at lines 5-6)
    - .planning/phases/11-test-stub-realignment-doc-cleanup/11-CONTEXT.md sections D-14, D-15, D-16
    - .planning/v4.8-MILESTONE-AUDIT.md residual_cleanup.doc_staleness item 3 (Wave 0 test stubs all exist and ran GREEN during Phase 999.1 execution)
  </read_first>
  <action>
    Apply exactly TWO one-line Edit operations to the frontmatter. Do NOT modify anything outside the frontmatter block. Do NOT touch `status: draft` (that's a separate concern per D-14 scope).

    **Edit 1:**
    ```
    Edit file=".planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md"
      old_string="nyquist_compliant: false"
      new_string="nyquist_compliant: true"
    ```

    **Edit 2:**
    ```
    Edit file=".planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md"
      old_string="wave_0_complete: false"
      new_string="wave_0_complete: true"
    ```

    **Rationale for the flip (per D-16):** All Wave 0 test stubs referenced in the VALIDATION.md "Wave 0 Requirements" section were created during Plan 999.1-01 (LedgerEntry test stubs) and moved through RED/GREEN by Plan 999.1-02 (LedgerService implementation). The milestone audit confirms "Wave 0 test stubs all exist and ran GREEN during Phase 999.1 execution" and the Phase 999.1 state in STATE.md shows 6/6 plans complete. The flags were initial draft defaults that were never updated after Wave 0 completed.

    **DO NOT:**
    - Change `status: draft` — out of scope per D-14 (separate concern)
    - Add any new fields to frontmatter
    - Modify the body of the VALIDATION.md file (Test Infrastructure, Sampling Rate, Per-Task Verification Map tables, etc. — those are wave-related content, not the Nyquist flags)
    - Touch any other phase's VALIDATION.md file

    Per D-14, D-15, D-16.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md ; grep -c "wave_0_complete: true" .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md ; grep -c "nyquist_compliant: false\|wave_0_complete: false" .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nyquist_compliant: true' .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md` returns exactly 1
    - `grep -c 'wave_0_complete: true' .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md` returns exactly 1
    - `grep -c 'nyquist_compliant: false' .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md` returns 0
    - `grep -c 'wave_0_complete: false' .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md` returns 0
    - `grep -c 'status: draft' .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md` returns 1 (unchanged — out of scope)
    - `git diff .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md` shows exactly 2 changed lines (2 `-` / 2 `+`), both in the frontmatter (lines 5-6)
    - `wc -l .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md` is unchanged from baseline (no lines added or removed, just 2 value changes)
  </acceptance_criteria>
  <done>
    Phase 999.1 VALIDATION.md frontmatter shows nyquist_compliant: true and wave_0_complete: true. No other part of the file was modified. Phase 11 Success Criterion 4 from ROADMAP is satisfied.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run the phase-level success criteria checks:

```bash
# Success Criterion 2: REQUIREMENTS.md checkbox refresh
grep -c '^- \[x\]' .planning/REQUIREMENTS.md
# Expected: 78 (46 baseline + 32 flipped) — or lower if cross-reference excluded some

grep -c 'Pending' .planning/REQUIREMENTS.md
# Expected: 8 (PIPE-06 + 7 Phase 9 rows) — or higher if cross-reference excluded some

grep -c '2026-04-11' .planning/REQUIREMENTS.md
# Expected: at least 1 (Last updated refreshed)

# Success Criterion 3: Phase 04.1 VERIFICATION.md exists with passed status
test -f .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md
grep -c 'status: passed' .planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-VERIFICATION.md

# Success Criterion 4: Phase 999.1 Nyquist flags flipped
grep 'nyquist_compliant\|wave_0_complete' .planning/phases/999.1-history-and-memory-lifecycle-management/999.1-VALIDATION.md
# Expected: both show : true
```

**No code changes allowed.** Verify:
```bash
git diff --name-only | grep -v '^.planning/' | wc -l
# Expected: 0 (only .planning/ files modified)
```
</verification>

<success_criteria>
1. REQUIREMENTS.md reflects verified state of phase 6/7/8 requirements (bullet checkboxes flipped, traceability rows flipped, Satisfied count added, Last updated refreshed)
2. Phase 04.1 VERIFICATION.md exists with status: passed covering all 6 ROADMAP success criteria
3. Phase 999.1 VALIDATION.md frontmatter shows nyquist_compliant: true and wave_0_complete: true
4. Zero files modified outside `.planning/` directory
5. Roadmap Phase 11 Success Criteria 2, 3, 4 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-stub-realignment-doc-cleanup/11-02-SUMMARY.md` with:
- Baseline state per file (counts for REQUIREMENTS.md; file-doesn't-exist for 04.1 VERIFICATION; flag state for 999.1 VALIDATION)
- End state per file
- Exact number of REQ-IDs flipped (32 if all cross-reference cleanly, fewer otherwise)
- List of any REQ-IDs that could NOT be cross-referenced in phase VERIFICATION files (for human review)
- Confirmation that no files outside `.planning/` were modified
- Decision references: D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16
</output>
