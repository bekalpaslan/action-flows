---
phase: 04-layout-navigation
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/app/src/workbenches/shell/AppShell.tsx
  - packages/app/src/workbenches/shell/AppShell.css
  - packages/app/src/workbenches/sidebar/Sidebar.tsx
  - packages/app/src/workbenches/sidebar/SidebarItem.tsx
  - packages/app/src/workbenches/sidebar/SidebarPlaceholder.tsx
  - packages/app/src/workbenches/sidebar/SidebarPlaceholder.css
  - packages/app/src/status/WebSocketStatus.tsx
  - packages/app/src/status/WebSocketStatus.css
  - packages/app/src/App.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-04]

must_haves:
  truths:
    - "App renders a 3-panel horizontal layout with sidebar, workspace, and chat"
    - "Sidebar shows 7 workbenches with lucide icons and clicking switches workspace"
    - "Sidebar collapses to icon-only mode and expands back"
    - "Panels can be resized by dragging handles between them"
    - "Panel sizes persist to localStorage via autoSaveId"
  artifacts:
    - path: "packages/app/src/workbenches/shell/AppShell.tsx"
      provides: "3-panel PanelGroup layout with horizontal direction"
      contains: "PanelGroup"
      min_lines: 40
    - path: "packages/app/src/workbenches/sidebar/Sidebar.tsx"
      provides: "Sidebar component with collapse/expand, workbench list, WebSocketStatus"
      contains: "sidebarCollapsed"
      min_lines: 30
    - path: "packages/app/src/workbenches/sidebar/SidebarItem.tsx"
      provides: "Individual nav item with icon, label, active state, tooltip when collapsed"
      contains: "Tooltip"
      min_lines: 20
    - path: "packages/app/src/status/WebSocketStatus.tsx"
      provides: "WebSocket status indicator with Tailwind classes instead of CSS file"
      contains: "cn("
  key_links:
    - from: "packages/app/src/workbenches/shell/AppShell.tsx"
      to: "react-resizable-panels"
      via: "import { Panel, PanelGroup, PanelResizeHandle }"
      pattern: "PanelGroup.*direction.*horizontal"
    - from: "packages/app/src/workbenches/shell/AppShell.tsx"
      to: "packages/app/src/workbenches/sidebar/Sidebar.tsx"
      via: "import { Sidebar }"
      pattern: "<Sidebar"
    - from: "packages/app/src/workbenches/sidebar/Sidebar.tsx"
      to: "packages/app/src/stores/uiStore.ts"
      via: "useUIStore"
      pattern: "sidebarCollapsed"
    - from: "packages/app/src/workbenches/sidebar/SidebarItem.tsx"
      to: "packages/app/src/components/ui/tooltip.tsx"
      via: "import { Tooltip, TooltipTrigger, TooltipContent }"
      pattern: "Tooltip"
---

<objective>
Rewrite AppShell with react-resizable-panels 3-panel layout, replace SidebarPlaceholder with a full Sidebar component featuring lucide icons and collapse/expand behavior, and migrate WebSocketStatus to Tailwind.

Purpose: Deliver the core 3-panel resizable layout (LAYOUT-01) with sidebar navigation (LAYOUT-02) and resize/collapse support (LAYOUT-04).
Output: Rebuilt AppShell.tsx, new Sidebar.tsx + SidebarItem.tsx, migrated WebSocketStatus.tsx. Old placeholder files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-layout-navigation/04-RESEARCH.md
@.planning/phases/04-layout-navigation/04-UI-SPEC.md
@.planning/phases/04-layout-navigation/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->
From packages/app/src/lib/types.ts (after Plan 01):
```typescript
import type { LucideIcon } from 'lucide-react';
export type WorkbenchId = 'work' | 'explore' | 'review' | 'pm' | 'settings' | 'archive' | 'studio';
export interface WorkbenchMeta {
  id: WorkbenchId;
  label: string;
  icon: LucideIcon;
}
export const WORKBENCHES: readonly WorkbenchMeta[] = [
  { id: 'work', label: 'Work', icon: Briefcase },
  { id: 'explore', label: 'Explore', icon: Compass },
  // ... all 7 with icons
] as const;
```

From packages/app/src/stores/uiStore.ts (after Plan 01):
```typescript
export const useUIStore = create<UIState>((set) => ({
  activeWorkbench: 'work',
  setActiveWorkbench: (id) => set({ activeWorkbench: id }),
  sidebarCollapsed: false,
  setSidebarCollapsed: (collapsed) => set({ sidebarCollapsed: collapsed }),
  toggleSidebar: () => set((s) => ({ sidebarCollapsed: !s.sidebarCollapsed })),
  chatCollapsed: false,
  setChatCollapsed: (collapsed) => set({ chatCollapsed: collapsed }),
  toggleChat: () => set((s) => ({ chatCollapsed: !s.chatCollapsed })),
  pipelineCollapsed: false,
  setPipelineCollapsed: (collapsed) => set({ pipelineCollapsed: collapsed }),
  togglePipeline: () => set((s) => ({ pipelineCollapsed: !s.pipelineCollapsed })),
  commandPaletteOpen: false,
  setCommandPaletteOpen: (open) => set({ commandPaletteOpen: open }),
  toggleCommandPalette: () => set((s) => ({ commandPaletteOpen: !s.commandPaletteOpen })),
}));
```

From packages/app/src/components/ui/index.ts:
```typescript
export { Button, buttonVariants } from './button';
export { Badge, badgeVariants } from './badge';
export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider } from './tooltip';
```

From packages/app/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) { ... }
```

From packages/app/src/status/WebSocketStatus.tsx (current):
```typescript
import { useWSStore } from '../stores/wsStore';
import './WebSocketStatus.css';
export function WebSocketStatus() {
  const status = useWSStore((s) => s.status);
  return (
    <div className="ws-status">
      <span className={`ws-status__dot ws-status__dot--${status}`} />
      <span>{STATUS_LABELS[status] ?? status}</span>
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AppShell with 3-panel PanelGroup layout and create Sidebar</name>
  <files>
    packages/app/src/workbenches/shell/AppShell.tsx,
    packages/app/src/workbenches/sidebar/Sidebar.tsx,
    packages/app/src/workbenches/sidebar/SidebarItem.tsx,
    packages/app/src/App.tsx
  </files>
  <read_first>
    - packages/app/src/workbenches/shell/AppShell.tsx
    - packages/app/src/workbenches/sidebar/SidebarPlaceholder.tsx
    - packages/app/src/App.tsx
    - packages/app/src/stores/uiStore.ts
    - packages/app/src/lib/types.ts
    - packages/app/src/components/ui/tooltip.tsx
    - .planning/phases/04-layout-navigation/04-UI-SPEC.md (ResizableLayout section, Sidebar section, Interaction Contracts)
    - .planning/phases/04-layout-navigation/04-RESEARCH.md (Pattern 1, Pattern 4, Pattern 5, Anti-Patterns)
  </read_first>
  <action>
**CRITICAL CONSTRAINTS from UI-SPEC:**
- Panel and PanelResizeHandle MUST be direct children of PanelGroup (no wrapper divs between them)
- Do NOT apply display, flex-direction, flex-wrap, or overflow styles on PanelGroup
- autoSaveId="app-layout" on horizontal PanelGroup for localStorage persistence
- Preserve `key={activeWorkbench}` on WorkspaceArea for clean remount on workbench switch

**1. Rewrite AppShell.tsx:**
Replace the CSS Grid layout with react-resizable-panels PanelGroup. The component must:

```typescript
import { useRef } from 'react';
import { Panel, PanelGroup, PanelResizeHandle, type ImperativePanelHandle } from 'react-resizable-panels';
import { useUIStore } from '@/stores/uiStore';
import { useWebSocket } from '@/hooks/useWebSocket';
import { Sidebar } from '../sidebar/Sidebar';
import { WorkspaceArea } from '../workspace/WorkspaceArea';
import { ChatPlaceholder } from '../chat/ChatPlaceholder';
import { cn } from '@/lib/utils';
```

- Remove `import './AppShell.css';` (CSS file already deleted)
- Create `sidebarRef = useRef<ImperativePanelHandle>(null)` and `chatRef = useRef<ImperativePanelHandle>(null)` for imperative panel control
- Store the refs in a module-level `panelHandles` export so the command palette can access them:
  ```typescript
  export const panelHandles = {
    sidebar: null as ImperativePanelHandle | null,
    chat: null as ImperativePanelHandle | null,
  };
  ```
- In the component body, sync refs to panelHandles:
  ```typescript
  // Sync refs to module-level handles for command palette access
  useEffect(() => {
    panelHandles.sidebar = sidebarRef.current;
    panelHandles.chat = chatRef.current;
  }, []);
  ```

- PanelGroup with `direction="horizontal"` and `autoSaveId="app-layout"` and `className="h-screen"`
- Sidebar Panel: `ref={sidebarRef}`, `defaultSize={15}`, `minSize={4}`, `maxSize={25}`, `collapsible`, `collapsedSize={4}`, `onCollapse={() => setSidebarCollapsed(true)}`, `onExpand={() => setSidebarCollapsed(false)}`
- DragHandle (vertical): `<PanelResizeHandle>` with these exact classes:
  ```
  className={cn(
    'relative flex items-center justify-center w-[10px] cursor-col-resize',
    'before:absolute before:rounded-full before:bg-transparent',
    'before:h-8 before:w-1',
    'before:transition-[background-color] before:duration-150 before:ease-in-out',
    'hover:before:bg-border-strong/50',
    'active:before:bg-accent/30'
  )}
  ```
- Workspace Panel: `defaultSize={60}`, `minSize={35}` -- NOT collapsible. Render `<WorkspaceArea key={activeWorkbench} workbenchId={activeWorkbench} />`
- Second DragHandle (same styling)
- Chat Panel: `ref={chatRef}`, `defaultSize={25}`, `minSize={0}`, `maxSize={40}`, `collapsible`, `collapsedSize={0}`, `onCollapse={() => setChatCollapsed(true)}`, `onExpand={() => setChatCollapsed(false)}`

- The outer div is GONE. PanelGroup is the root element of AppShell. It gets `className="h-screen"`.

**2. Create Sidebar.tsx** at packages/app/src/workbenches/sidebar/Sidebar.tsx:

```typescript
import { WORKBENCHES } from '@/lib/types';
import { useUIStore } from '@/stores/uiStore';
import { Button } from '@/components/ui';
import { TooltipProvider } from '@/components/ui';
import { WebSocketStatus } from '@/status/WebSocketStatus';
import { SidebarItem } from './SidebarItem';
import { ChevronLeft, ChevronRight } from 'lucide-react';
import { cn } from '@/lib/utils';
```

Props: `collapsed: boolean` (passed from AppShell). Also accept optional `onExpand?: () => void` for clicking items in collapsed state to expand.

Structure:
```
<TooltipProvider>
  <nav aria-label="Workbench navigation" className={cn('flex flex-col h-full bg-surface-2', collapsed ? 'p-3' : 'p-4')}>
    {!collapsed && <h2 className="text-heading font-semibold text-text mb-6">Workbenches</h2>}
    <ul role="list" className="flex flex-col gap-2">
      {WORKBENCHES.map((wb) => (
        <SidebarItem
          key={wb.id}
          workbench={wb}
          isActive={wb.id === activeWorkbench}
          collapsed={collapsed}
          onSelect={() => {
            setActiveWorkbench(wb.id);
            if (collapsed && onExpand) onExpand();
          }}
        />
      ))}
    </ul>
    <div className="mt-auto flex flex-col gap-2">
      <Button
        variant="ghost"
        size="icon"
        onClick={toggleSidebar}
        aria-expanded={!collapsed}
        aria-label={collapsed ? 'Expand sidebar' : 'Collapse sidebar'}
        className="self-center"
      >
        {collapsed ? <ChevronRight className="h-5 w-5" /> : <ChevronLeft className="h-5 w-5" />}
      </Button>
      <WebSocketStatus />
    </div>
  </nav>
</TooltipProvider>
```

**3. Create SidebarItem.tsx** at packages/app/src/workbenches/sidebar/SidebarItem.tsx:

Props interface:
```typescript
import type { WorkbenchMeta } from '@/lib/types';

interface SidebarItemProps {
  workbench: WorkbenchMeta;
  isActive: boolean;
  collapsed: boolean;
  onSelect: () => void;
}
```

Implementation follows UI-SPEC exactly:
- Item height: `h-11` (44px touch target)
- Expanded: `flex items-center gap-2 px-4 py-2 rounded-md cursor-pointer transition-colors`
- Collapsed: `flex items-center justify-center px-3 rounded-md cursor-pointer transition-colors`
- Active state: `bg-surface-3 text-text border-l-[3px] border-l-accent`
- Inactive state: `text-text-dim hover:bg-surface-3 hover:text-text border-l-[3px] border-l-transparent`
- Icon: `<Icon className="h-5 w-5 shrink-0" />`
- Label (expanded only): `<span className="text-caption font-semibold">{workbench.label}</span>`
- When collapsed, wrap in Tooltip:
  ```
  <Tooltip>
    <TooltipTrigger asChild>{content}</TooltipTrigger>
    <TooltipContent side="right">{workbench.label}</TooltipContent>
  </Tooltip>
  ```
- Set `aria-current="page"` when isActive

**4. Update App.tsx:**
Wrap AppShell with TooltipProvider if not already done in Sidebar (the Sidebar handles its own TooltipProvider internally, so App.tsx stays minimal). No changes needed to App.tsx unless the current version has issues.

**5. Delete old files:**
- Delete packages/app/src/workbenches/sidebar/SidebarPlaceholder.tsx
- Delete packages/app/src/workbenches/sidebar/SidebarPlaceholder.css (if it exists)
- Delete packages/app/src/workbenches/shell/AppShell.css (if it exists)
  </action>
  <verify>
    <automated>cd packages/app && grep -c "PanelGroup" src/workbenches/shell/AppShell.tsx && grep -c "direction.*horizontal" src/workbenches/shell/AppShell.tsx && grep -c "autoSaveId" src/workbenches/shell/AppShell.tsx && test -f src/workbenches/sidebar/Sidebar.tsx && echo "Sidebar exists" && test -f src/workbenches/sidebar/SidebarItem.tsx && echo "SidebarItem exists" && ! test -f src/workbenches/sidebar/SidebarPlaceholder.tsx && echo "Placeholder deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PanelGroup" packages/app/src/workbenches/shell/AppShell.tsx` matches
    - `grep 'direction="horizontal"' packages/app/src/workbenches/shell/AppShell.tsx` matches
    - `grep 'autoSaveId="app-layout"' packages/app/src/workbenches/shell/AppShell.tsx` matches
    - `grep "defaultSize={15}" packages/app/src/workbenches/shell/AppShell.tsx` matches (sidebar panel)
    - `grep "defaultSize={60}" packages/app/src/workbenches/shell/AppShell.tsx` matches (workspace panel)
    - `grep "defaultSize={25}" packages/app/src/workbenches/shell/AppShell.tsx` matches (chat panel)
    - `grep "collapsible" packages/app/src/workbenches/shell/AppShell.tsx` returns at least 2 matches (sidebar + chat)
    - `grep "key={activeWorkbench}" packages/app/src/workbenches/shell/AppShell.tsx` matches (preserved)
    - `grep 'aria-label="Workbench navigation"' packages/app/src/workbenches/sidebar/Sidebar.tsx` matches
    - `grep "TooltipContent" packages/app/src/workbenches/sidebar/SidebarItem.tsx` matches
    - `grep 'aria-current="page"' packages/app/src/workbenches/sidebar/SidebarItem.tsx` matches
    - `grep "panelHandles" packages/app/src/workbenches/shell/AppShell.tsx` matches (module-level export)
    - SidebarPlaceholder.tsx does NOT exist
    - AppShell.css does NOT exist (or has no import in AppShell.tsx)
  </acceptance_criteria>
  <done>AppShell renders 3-panel PanelGroup with sidebar (15%, collapsible to 4%), workspace (60%), chat (25%, collapsible to 0%). Sidebar shows 7 workbenches with lucide icons, collapse/expand toggle, active state with accent border, tooltips when collapsed. Old placeholder files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate WebSocketStatus to Tailwind and clean up CSS imports</name>
  <files>
    packages/app/src/status/WebSocketStatus.tsx,
    packages/app/src/status/WebSocketStatus.css
  </files>
  <read_first>
    - packages/app/src/status/WebSocketStatus.tsx
    - packages/app/src/status/WebSocketStatus.css
    - packages/app/src/stores/wsStore.ts
    - .planning/phases/04-layout-navigation/04-RESEARCH.md (WebSocketStatus migration section)
  </read_first>
  <action>
**Migrate WebSocketStatus.tsx from BEM CSS to Tailwind utility classes.**

The current CSS uses:
- `.ws-status`: flex, align-items center, gap 8px, font-size 13px, font-weight 600, color text-dim, padding 8px 16px
- `.ws-status__dot`: 8px square, border-radius 50%, flex-shrink 0
- `.ws-status__dot--connected`: bg success
- `.ws-status__dot--reconnecting`: bg warning + pulse animation
- `.ws-status__dot--disconnected`: bg destructive
- `.ws-status__dot--connecting`: bg warning + pulse animation

Rewrite to:
```typescript
import { useWSStore } from '../stores/wsStore';
import { cn } from '@/lib/utils';

const STATUS_LABELS: Record<string, string> = {
  connected: 'Connected',
  reconnecting: 'Reconnecting...',
  disconnected: 'Disconnected',
  connecting: 'Connecting...',
};

const DOT_COLORS: Record<string, string> = {
  connected: 'bg-success',
  reconnecting: 'bg-warning animate-pulse',
  disconnected: 'bg-destructive',
  connecting: 'bg-warning animate-pulse',
};

export function WebSocketStatus() {
  const status = useWSStore((s) => s.status);
  return (
    <div className="flex items-center gap-2 text-caption font-semibold text-text-dim px-4 py-2">
      <span className={cn('h-2 w-2 rounded-full shrink-0', DOT_COLORS[status] ?? 'bg-text-muted')} />
      <span>{STATUS_LABELS[status] ?? status}</span>
    </div>
  );
}
```

Key mappings:
- `gap: 8px` -> `gap-2`
- `font-size: 13px` -> `text-caption` (maps to --font-size-caption from theme.css)
- `font-weight: 600` -> `font-semibold`
- `color: var(--color-text-dim)` -> `text-text-dim`
- `padding: 8px 16px` -> `px-4 py-2`
- `width: 8px; height: 8px` -> `h-2 w-2`
- `border-radius: 50%` -> `rounded-full`
- `flex-shrink: 0` -> `shrink-0`
- Pulse animation -> `animate-pulse` (Tailwind built-in)
- `var(--color-success)` -> `bg-success`
- `var(--color-warning)` -> `bg-warning`
- `var(--color-destructive)` -> `bg-destructive`

Remove the CSS import: delete `import './WebSocketStatus.css';`
Delete the CSS file: packages/app/src/status/WebSocketStatus.css

Also check for any remaining CSS imports in the workbenches directory and remove them if the CSS files no longer exist. The TSX files still have stale CSS imports (ChatPlaceholder, WorkspaceArea, AppShell, SidebarPlaceholder) -- AppShell and SidebarPlaceholder are handled by Task 1 (rewrite/delete). ChatPlaceholder and WorkspaceArea will be handled in Plan 03.
  </action>
  <verify>
    <automated>cd packages/app && grep -c "cn(" src/status/WebSocketStatus.tsx && ! grep -q "import.*\.css" src/status/WebSocketStatus.tsx && echo "No CSS import" && ! test -f src/status/WebSocketStatus.css && echo "CSS deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cn(" packages/app/src/status/WebSocketStatus.tsx` matches at least 1 line
    - `grep "import.*WebSocketStatus.css" packages/app/src/status/WebSocketStatus.tsx` returns NO match
    - packages/app/src/status/WebSocketStatus.css does NOT exist
    - `grep "text-text-dim" packages/app/src/status/WebSocketStatus.tsx` matches
    - `grep "bg-success" packages/app/src/status/WebSocketStatus.tsx` matches
    - `grep "bg-warning" packages/app/src/status/WebSocketStatus.tsx` matches
    - `grep "bg-destructive" packages/app/src/status/WebSocketStatus.tsx` matches
    - `grep "rounded-full" packages/app/src/status/WebSocketStatus.tsx` matches
  </acceptance_criteria>
  <done>WebSocketStatus uses Tailwind utility classes via cn(). CSS file deleted. No stale CSS imports remain in the file.</done>
</task>

</tasks>

<verification>
- `cd packages/app && pnpm type-check` passes (all new components compile)
- `pnpm type-check` passes across all packages
- `grep -r "import.*\.css" packages/app/src/workbenches/shell/` returns no matches
- `grep -r "import.*\.css" packages/app/src/workbenches/sidebar/` returns no matches
- `grep -r "import.*\.css" packages/app/src/status/` returns no matches
- AppShell renders PanelGroup with 3 panels
- Sidebar renders 7 workbenches with icons
</verification>

<success_criteria>
- AppShell uses PanelGroup with horizontal direction and 3 panels (sidebar 15%, workspace 60%, chat 25%)
- Sidebar and Chat panels are collapsible with onCollapse/onExpand syncing to uiStore
- Sidebar renders 7 workbenches with lucide icons, active state with accent border, collapse toggle button
- SidebarItem shows tooltips when sidebar is collapsed
- WebSocketStatus uses Tailwind classes, no CSS file
- SidebarPlaceholder and all workbench CSS files deleted
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-layout-navigation/04-02-SUMMARY.md`
</output>
