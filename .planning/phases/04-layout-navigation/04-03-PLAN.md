---
phase: 04-layout-navigation
plan: "03"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/app/src/workbenches/workspace/WorkspaceArea.tsx
  - packages/app/src/workbenches/workspace/WorkspaceArea.css
  - packages/app/src/workbenches/workspace/PipelinePlaceholder.tsx
  - packages/app/src/workbenches/chat/ChatPlaceholder.tsx
  - packages/app/src/workbenches/chat/ChatPlaceholder.css
autonomous: true
requirements: [LAYOUT-03, LAYOUT-04]

must_haves:
  truths:
    - "Workspace area splits vertically into pipeline region (top 25%) and content area (bottom 75%)"
    - "Pipeline region is collapsible to 0% and expandable back"
    - "Content area renders the correct workbench page component and is scrollable"
    - "Pipeline placeholder shows 'Pipeline -- Phase 5' text"
    - "Chat placeholder renders with Tailwind classes, no CSS file"
  artifacts:
    - path: "packages/app/src/workbenches/workspace/WorkspaceArea.tsx"
      provides: "Vertical PanelGroup splitting pipeline and content"
      contains: "PanelGroup"
      min_lines: 30
    - path: "packages/app/src/workbenches/workspace/PipelinePlaceholder.tsx"
      provides: "Placeholder component for Phase 5 pipeline region"
      contains: "Pipeline"
    - path: "packages/app/src/workbenches/chat/ChatPlaceholder.tsx"
      provides: "Chat placeholder with Tailwind styling"
      contains: "cn("
  key_links:
    - from: "packages/app/src/workbenches/workspace/WorkspaceArea.tsx"
      to: "react-resizable-panels"
      via: "import { Panel, PanelGroup, PanelResizeHandle }"
      pattern: "PanelGroup.*direction.*vertical"
    - from: "packages/app/src/workbenches/workspace/WorkspaceArea.tsx"
      to: "packages/app/src/stores/uiStore.ts"
      via: "useUIStore"
      pattern: "pipelineCollapsed"
    - from: "packages/app/src/workbenches/workspace/WorkspaceArea.tsx"
      to: "packages/app/src/workbenches/workspace/PipelinePlaceholder.tsx"
      via: "import { PipelinePlaceholder }"
      pattern: "<PipelinePlaceholder"
---

<objective>
Rebuild WorkspaceArea with a nested vertical PanelGroup splitting pipeline (top) and content (bottom), create PipelinePlaceholder for Phase 5, and migrate ChatPlaceholder to Tailwind.

Purpose: Deliver the workspace pipeline/content split (LAYOUT-03) and ensure all shell panels use Tailwind (LAYOUT-04 collapse support).
Output: Rebuilt WorkspaceArea.tsx with vertical split, new PipelinePlaceholder.tsx, migrated ChatPlaceholder.tsx. Old CSS files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-layout-navigation/04-RESEARCH.md
@.planning/phases/04-layout-navigation/04-UI-SPEC.md
@.planning/phases/04-layout-navigation/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->
From packages/app/src/stores/uiStore.ts (after Plan 01):
```typescript
export const useUIStore = create<UIState>((set) => ({
  // ... existing fields ...
  pipelineCollapsed: false,
  setPipelineCollapsed: (collapsed) => set({ pipelineCollapsed: collapsed }),
  togglePipeline: () => set((s) => ({ pipelineCollapsed: !s.pipelineCollapsed })),
}));
```

From packages/app/src/lib/types.ts:
```typescript
export type WorkbenchId = 'work' | 'explore' | 'review' | 'pm' | 'settings' | 'archive' | 'studio';
```

From packages/app/src/workbenches/workspace/WorkspaceArea.tsx (current, before rewrite):
```typescript
const PAGE_MAP: Record<WorkbenchId, React.FC> = {
  work: WorkPage,
  explore: ExplorePage,
  review: ReviewPage,
  pm: PMPage,
  settings: SettingsPage,
  archive: ArchivePage,
  studio: StudioPage,
};
export function WorkspaceArea({ workbenchId }: WorkspaceAreaProps) {
  const Page = PAGE_MAP[workbenchId];
  return <main className="workspace"><Page /></main>;
}
```

From packages/app/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild WorkspaceArea with vertical PanelGroup and create PipelinePlaceholder</name>
  <files>
    packages/app/src/workbenches/workspace/WorkspaceArea.tsx,
    packages/app/src/workbenches/workspace/PipelinePlaceholder.tsx
  </files>
  <read_first>
    - packages/app/src/workbenches/workspace/WorkspaceArea.tsx
    - packages/app/src/stores/uiStore.ts
    - packages/app/src/lib/types.ts
    - packages/app/src/workbenches/pages/WorkPage.tsx
    - .planning/phases/04-layout-navigation/04-UI-SPEC.md (WorkspaceArea section, Pipeline region, Content region, Drag handle horizontal)
    - .planning/phases/04-layout-navigation/04-RESEARCH.md (Pattern 2: Nested Vertical PanelGroup)
  </read_first>
  <action>
**1. Create PipelinePlaceholder.tsx** at packages/app/src/workbenches/workspace/PipelinePlaceholder.tsx:

Per UI-SPEC Pipeline region spec:
- Background: `bg-surface` (var(--color-surface))
- Border: bottom border only -- `border-b border-border`
- Content: Centered placeholder text
- Overflow: `overflow-hidden`

```typescript
import { cn } from '@/lib/utils';

export function PipelinePlaceholder() {
  return (
    <div className={cn(
      'flex items-center justify-center h-full',
      'bg-surface border-b border-border overflow-hidden'
    )}>
      <span className="text-caption text-text-dim">Pipeline -- Phase 5</span>
    </div>
  );
}
```

**2. Rewrite WorkspaceArea.tsx** with a nested vertical PanelGroup:

Remove `import './WorkspaceArea.css';` (stale CSS import)

Keep the existing PAGE_MAP and all page imports. Replace the JSX:

```typescript
import { Panel, PanelGroup, PanelResizeHandle } from 'react-resizable-panels';
import type { WorkbenchId } from '@/lib/types';
import { useUIStore } from '@/stores/uiStore';
import { cn } from '@/lib/utils';
import { PipelinePlaceholder } from './PipelinePlaceholder';
import { WorkPage } from '../pages/WorkPage';
import { ExplorePage } from '../pages/ExplorePage';
import { ReviewPage } from '../pages/ReviewPage';
import { PMPage } from '../pages/PMPage';
import { SettingsPage } from '../pages/SettingsPage';
import { ArchivePage } from '../pages/ArchivePage';
import { StudioPage } from '../pages/StudioPage';

const PAGE_MAP: Record<WorkbenchId, React.FC> = {
  work: WorkPage,
  explore: ExplorePage,
  review: ReviewPage,
  pm: PMPage,
  settings: SettingsPage,
  archive: ArchivePage,
  studio: StudioPage,
};

interface WorkspaceAreaProps {
  workbenchId: WorkbenchId;
}

export function WorkspaceArea({ workbenchId }: WorkspaceAreaProps) {
  const setPipelineCollapsed = useUIStore((s) => s.setPipelineCollapsed);
  const Page = PAGE_MAP[workbenchId];

  return (
    <PanelGroup direction="vertical" autoSaveId="workspace-split">
      <Panel
        defaultSize={25}
        minSize={10}
        maxSize={50}
        collapsible
        collapsedSize={0}
        onCollapse={() => setPipelineCollapsed(true)}
        onExpand={() => setPipelineCollapsed(false)}
      >
        <PipelinePlaceholder />
      </Panel>
      <PanelResizeHandle
        className={cn(
          'relative flex items-center justify-center h-[10px] cursor-row-resize',
          'before:absolute before:rounded-full before:bg-transparent',
          'before:h-1 before:w-8',
          'before:transition-[background-color] before:duration-150 before:ease-in-out',
          'hover:before:bg-border-strong/50',
          'active:before:bg-accent/30'
        )}
      />
      <Panel defaultSize={75} minSize={50}>
        <main className="overflow-y-auto h-full p-6 bg-surface" role="main">
          <Page />
        </main>
      </Panel>
    </PanelGroup>
  );
}
```

Key details from UI-SPEC:
- Pipeline Panel: defaultSize=25, minSize=10, maxSize=50, collapsible to 0%
- Content Panel: defaultSize=75, minSize=50, NOT collapsible
- Drag handle (horizontal): height 10px hit zone, 4px visible bar (h-1 w-8), cursor row-resize
- Content area: overflow-y auto (scrollable), p-6 (24px padding), bg-surface, role="main"
- autoSaveId="workspace-split" for separate localStorage persistence from the horizontal layout

**3. Delete WorkspaceArea.css** if it exists (already deleted from Phase 3 migration, but ensure the import is removed from the TSX).
  </action>
  <verify>
    <automated>cd packages/app && grep -c "PanelGroup" src/workbenches/workspace/WorkspaceArea.tsx && grep 'direction="vertical"' src/workbenches/workspace/WorkspaceArea.tsx && grep 'autoSaveId="workspace-split"' src/workbenches/workspace/WorkspaceArea.tsx && test -f src/workbenches/workspace/PipelinePlaceholder.tsx && echo "PipelinePlaceholder exists"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PanelGroup" packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches
    - `grep 'direction="vertical"' packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches
    - `grep 'autoSaveId="workspace-split"' packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches
    - `grep "defaultSize={25}" packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches (pipeline)
    - `grep "defaultSize={75}" packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches (content)
    - `grep "collapsible" packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches
    - `grep "collapsedSize={0}" packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches
    - `grep "pipelineCollapsed" packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches
    - `grep 'role="main"' packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches
    - `grep "overflow-y-auto" packages/app/src/workbenches/workspace/WorkspaceArea.tsx` matches
    - PipelinePlaceholder.tsx exists with "Pipeline -- Phase 5" text
    - No `import.*\.css` in WorkspaceArea.tsx
  </acceptance_criteria>
  <done>WorkspaceArea renders nested vertical PanelGroup with pipeline (25%, collapsible to 0%) and content (75%, scrollable). PipelinePlaceholder shows centered "Pipeline -- Phase 5" text with bottom border. Pipeline collapse/expand syncs to uiStore.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ChatPlaceholder to Tailwind and clean up all stale CSS imports</name>
  <files>
    packages/app/src/workbenches/chat/ChatPlaceholder.tsx
  </files>
  <read_first>
    - packages/app/src/workbenches/chat/ChatPlaceholder.tsx
    - .planning/phases/04-layout-navigation/04-UI-SPEC.md (Copywriting Contract -- chat placeholder text)
  </read_first>
  <action>
**Rewrite ChatPlaceholder.tsx** to use Tailwind classes instead of CSS file:

Per UI-SPEC:
- Background: `bg-surface-2` (same as sidebar, secondary surface)
- The chat panel collapses to 0% so when visible it should fill its space
- Placeholder text: "Chat -- Phase 7" (per UI-SPEC Copywriting Contract)

```typescript
import { cn } from '@/lib/utils';

export function ChatPlaceholder() {
  return (
    <aside className={cn(
      'flex items-center justify-center h-full',
      'bg-surface-2 text-caption text-text-dim'
    )}>
      Chat -- Phase 7
    </aside>
  );
}
```

Remove `import './ChatPlaceholder.css';` (stale import, CSS file already deleted).

Delete ChatPlaceholder.css if it still exists.

**Final CSS import sweep:** After this task, verify no `.css` imports remain in any file under packages/app/src/workbenches/. Run:
```bash
grep -r "import.*\.css" packages/app/src/workbenches/
```
If any remain, remove them.
  </action>
  <verify>
    <automated>cd packages/app && ! grep -q "import.*\.css" src/workbenches/chat/ChatPlaceholder.tsx && echo "No CSS import" && grep -c "bg-surface-2" src/workbenches/chat/ChatPlaceholder.tsx && grep -rL "import.*\.css" src/workbenches/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*\.css" packages/app/src/workbenches/chat/ChatPlaceholder.tsx` returns NO match
    - `grep "bg-surface-2" packages/app/src/workbenches/chat/ChatPlaceholder.tsx` matches
    - `grep "text-text-dim" packages/app/src/workbenches/chat/ChatPlaceholder.tsx` matches
    - `grep "Chat -- Phase 7" packages/app/src/workbenches/chat/ChatPlaceholder.tsx` matches
    - `grep -r "import.*\.css" packages/app/src/workbenches/` returns NO matches (zero stale CSS imports anywhere in workbenches)
  </acceptance_criteria>
  <done>ChatPlaceholder uses Tailwind classes. No stale CSS imports remain in any file under packages/app/src/workbenches/.</done>
</task>

</tasks>

<verification>
- `cd packages/app && pnpm type-check` passes
- `pnpm type-check` passes across all packages
- `grep -r "import.*\.css" packages/app/src/workbenches/` returns no matches
- WorkspaceArea renders vertical PanelGroup with 2 panels
- PipelinePlaceholder shows Phase 5 placeholder text
- ChatPlaceholder uses Tailwind, no CSS file
</verification>

<success_criteria>
- WorkspaceArea uses vertical PanelGroup with autoSaveId="workspace-split"
- Pipeline panel defaults to 25%, collapsible to 0%, syncs to uiStore.pipelineCollapsed
- Content panel defaults to 75%, scrollable, renders workbench page via PAGE_MAP
- PipelinePlaceholder renders centered text with bottom border
- ChatPlaceholder uses Tailwind utility classes, no CSS imports
- Horizontal drag handle uses row-resize cursor, same styling pattern as vertical handles
- Zero stale CSS imports remain in workbenches/ directory
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-layout-navigation/04-03-SUMMARY.md`
</output>
