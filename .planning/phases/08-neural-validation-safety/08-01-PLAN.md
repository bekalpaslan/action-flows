---
phase: 08-neural-validation-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/hooks/src/utils/design-rules.ts
  - packages/hooks/src/afw-design-validate-pre.ts
  - packages/hooks/src/afw-design-validate-post.ts
  - packages/hooks/src/utils/violation-reporter.ts
  - packages/app/src/components/ui/manifest.json
  - .claude/settings.json
autonomous: true
requirements:
  - NEURAL-01
  - NEURAL-02
  - NEURAL-03
  - NEURAL-05
  - NEURAL-06
  - NEURAL-07

must_haves:
  truths:
    - "PreToolUse hook blocks .tsx/.css file writes containing raw hex colors in CSS property contexts"
    - "PreToolUse hook blocks .tsx file writes containing inline style={{ }} attributes"
    - "PostToolUse hook warns on non-token hex color strings in .tsx/.css files"
    - "Hooks skip .ts logic files, test files, config files, and design-tokens.css itself"
    - "Hooks handle Write, Edit, and MultiEdit tool input shapes correctly"
    - "A static manifest.json exists alongside manifest.ts for agent context injection"
    - "Hooks are registered in .claude/settings.json with correct matchers and paths"
  artifacts:
    - path: "packages/hooks/src/utils/design-rules.ts"
      provides: "Shared regex patterns for critical (block) and warning (advise) violations"
      exports: ["CRITICAL_PATTERNS", "WARNING_PATTERNS", "VALIDATED_EXTENSIONS", "SKIP_PATTERNS"]
    - path: "packages/hooks/src/afw-design-validate-pre.ts"
      provides: "PreToolUse hook script that blocks design system violations"
      contains: "process.exit(2)"
    - path: "packages/hooks/src/afw-design-validate-post.ts"
      provides: "PostToolUse hook script that audits compliance and warns"
      contains: "hookSpecificOutput"
    - path: "packages/hooks/src/utils/violation-reporter.ts"
      provides: "Fire-and-forget POST of violation events to backend API"
      exports: ["reportViolation"]
    - path: "packages/app/src/components/ui/manifest.json"
      provides: "Static JSON export of component manifest for agent context injection"
      contains: "Button"
    - path: ".claude/settings.json"
      provides: "Hook registration for PreToolUse and PostToolUse design validation"
      contains: "afw-design-validate-pre"
  key_links:
    - from: "packages/hooks/src/afw-design-validate-pre.ts"
      to: "packages/hooks/src/utils/design-rules.ts"
      via: "import CRITICAL_PATTERNS"
      pattern: "import.*design-rules"
    - from: "packages/hooks/src/afw-design-validate-post.ts"
      to: "packages/hooks/src/utils/design-rules.ts"
      via: "import WARNING_PATTERNS"
      pattern: "import.*design-rules"
    - from: "packages/hooks/src/afw-design-validate-pre.ts"
      to: "packages/hooks/src/utils/violation-reporter.ts"
      via: "import reportViolation"
      pattern: "import.*violation-reporter"
---

<objective>
Create the hook-based design system enforcement layer -- PreToolUse and PostToolUse hook scripts that validate agent file edits against component library rules using regex pattern matching.

Purpose: This is the core intelligence of Phase 8. Agents can no longer write raw CSS or bypass the design system. PreToolUse blocks violations before they land (NEURAL-02), PostToolUse audits compliance and surfaces warnings (NEURAL-03). Both hooks share design rules from a common module (NEURAL-01). A static manifest.json is exported for agent context injection (NEURAL-07).

Output: 6 files -- design-rules.ts, PreToolUse hook, PostToolUse hook, violation-reporter.ts, manifest.json, updated settings.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-neural-validation-safety/08-CONTEXT.md
@.planning/phases/08-neural-validation-safety/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/hooks/src/utils/http.ts:
```typescript
export async function postEvent(backendUrl: string, event: WorkspaceEvent): Promise<boolean>;
```

From packages/hooks/src/afw-control-check.ts (PreToolUse hook pattern):
```typescript
// Reads JSON from stdin via fs.readFileSync(0, 'utf-8')
// Uses process.exit(2) to block, process.exit(0) to allow
// BACKEND_URL from process.env.AFW_BACKEND_URL || 'http://localhost:3001'
interface PreToolUseHookInput {
  tool_name: string;
  tool_input: Record<string, unknown>;
  session_id: string;
}
```

From packages/hooks/package.json:
```json
{
  "type": "module",
  "dependencies": { "@afw/shared": "workspace:*" },
  "scripts": { "build": "tsc" }
}
```

From packages/hooks/tsconfig.json:
```json
{ "extends": "../../tsconfig.base.json", "compilerOptions": { "outDir": "./dist" } }
```

From packages/app/src/components/ui/manifest.ts:
```typescript
export interface ComponentManifestEntry {
  name: string;
  importPath: string;
  description: string;
  variants: Record<string, string[]>;
  defaultVariants: Record<string, string>;
  sizes: string[];
  subComponents: string[];
  radixPrimitive: string | null;
  props: Record<string, { type: string; required: boolean; description: string; }>;
}
export const componentManifest: ComponentManifestEntry[];
```

From .claude/settings.json (existing hook registration format):
```json
{
  "hooks": {
    "PreToolUse": [
      { "matcher": "Task", "hooks": [{ "type": "command", "command": "node D:/ActionFlowsDashboard/packages/hooks/dist/hooks/src/afw-step-spawned.js", "timeout": 10 }] }
    ],
    "PostToolUse": [
      { "matcher": "Bash", "hooks": [{ "type": "command", "command": "node D:/ActionFlowsDashboard/packages/hooks/dist/hooks/src/afw-output-capture.js", "timeout": 10 }] }
    ]
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design rules module, violation reporter, and manifest.json</name>
  <files>
    packages/hooks/src/utils/design-rules.ts,
    packages/hooks/src/utils/violation-reporter.ts,
    packages/app/src/components/ui/manifest.json
  </files>
  <read_first>
    packages/hooks/src/utils/http.ts,
    packages/hooks/src/utils/parser.ts,
    packages/hooks/package.json,
    packages/hooks/tsconfig.json,
    packages/app/src/components/ui/manifest.ts,
    packages/app/src/styles/design-tokens.css
  </read_first>
  <action>
**1. Create `packages/hooks/src/utils/design-rules.ts`** with shared regex patterns per D-01 (regex, not AST) and D-03 (moderate strictness):

Export `CRITICAL_PATTERNS` array (blocked in PreToolUse, exit 2):
- `no-raw-hex`: regex `/(?:color|background(?:-color)?|border(?:-color)?|fill|stroke|outline-color)\s*:\s*#[0-9a-fA-F]{3,8}\b/g` with message "Raw hex color detected. Use var(--color-*) design tokens."
- `no-raw-color-fn`: regex `/(?:color|background(?:-color)?|border(?:-color)?|fill|stroke)\s*:\s*(?:rgb|rgba|hsl|hsla)\s*\(/g` with message "Raw color function detected. Use var(--color-*) design tokens."
- `no-inline-style`: regex `/style\s*=\s*\{\s*\{/g` with message "Inline style attribute detected. Use Tailwind classes with design tokens."

Export `WARNING_PATTERNS` array (warned in PostToolUse):
- `hex-outside-tokens`: regex `/['"]#[0-9a-fA-F]{3,8}['"]/g` with message "Hex color string found. Consider using a design token reference."

Export `VALIDATED_EXTENSIONS`: `['.tsx', '.css']` per D-04.

Export `SKIP_PATTERNS`: regexes for `.test.`, `.spec.`, `.config.`, `design-tokens`, `theme\.css`, `__tests__`, `__mocks__`, `node_modules`.

Each pattern entry has shape: `{ rule: string; pattern: RegExp; message: string; }`.
Export the type `DesignRule` for this shape.

**2. Create `packages/hooks/src/utils/violation-reporter.ts`**:

Export `reportViolation(filePath: string, violations: string[], sessionId: string): void` -- fire-and-forget POST to `${BACKEND_URL}/api/validation/violations` with body `{ filePath, violations, sessionId, timestamp: new Date().toISOString() }`. Use `fetch` with `AbortSignal.timeout(3000)`. Catch all errors silently (log to stderr at debug level). `BACKEND_URL` from `process.env.AFW_BACKEND_URL || 'http://localhost:3001'`.

**3. Create `packages/app/src/components/ui/manifest.json`**:

Read `manifest.ts` to get the full `componentManifest` array. Create a static JSON file with the same data structure. The JSON should be a direct serialization of the `componentManifest` array from `manifest.ts`. This file is consumed by agents via the Read tool (per NEURAL-07). Do NOT import or programmatically generate it -- manually transcribe the current manifest entries into JSON format.
  </action>
  <verify>
    <automated>cd D:/ActionFlowsDashboard && npx tsc --noEmit -p packages/hooks/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "CRITICAL_PATTERNS" packages/hooks/src/utils/design-rules.ts returns at least 1
    - grep -c "WARNING_PATTERNS" packages/hooks/src/utils/design-rules.ts returns at least 1
    - grep -c "VALIDATED_EXTENSIONS" packages/hooks/src/utils/design-rules.ts returns at least 1
    - grep -c "SKIP_PATTERNS" packages/hooks/src/utils/design-rules.ts returns at least 1
    - grep -c "no-raw-hex" packages/hooks/src/utils/design-rules.ts returns at least 1
    - grep -c "no-inline-style" packages/hooks/src/utils/design-rules.ts returns at least 1
    - grep -c "reportViolation" packages/hooks/src/utils/violation-reporter.ts returns at least 1
    - grep -c "api/validation/violations" packages/hooks/src/utils/violation-reporter.ts returns at least 1
    - File packages/app/src/components/ui/manifest.json exists and contains "Button"
    - File packages/app/src/components/ui/manifest.json is valid JSON (node -e "JSON.parse(require('fs').readFileSync('...'))")
  </acceptance_criteria>
  <done>Design rules module exports CRITICAL_PATTERNS (3 rules), WARNING_PATTERNS (1 rule), VALIDATED_EXTENSIONS, SKIP_PATTERNS. Violation reporter POSTs to backend. manifest.json contains all 12 component entries.</done>
</task>

<task type="auto">
  <name>Task 2: Create PreToolUse and PostToolUse hook scripts and register in settings.json</name>
  <files>
    packages/hooks/src/afw-design-validate-pre.ts,
    packages/hooks/src/afw-design-validate-post.ts,
    .claude/settings.json
  </files>
  <read_first>
    packages/hooks/src/afw-control-check.ts,
    packages/hooks/src/afw-output-capture.ts,
    packages/hooks/src/utils/design-rules.ts,
    packages/hooks/src/utils/violation-reporter.ts,
    .claude/settings.json
  </read_first>
  <action>
**1. Create `packages/hooks/src/afw-design-validate-pre.ts`** (PreToolUse hook per D-02):

Follow the established hook pattern from `afw-control-check.ts`:
- Read JSON from stdin: `const inputData = fs.readFileSync(0, 'utf-8');`
- Parse as `PreToolUseInput` with fields: `session_id: string`, `tool_name: string`, `tool_input: { file_path?: string; content?: string; new_string?: string; edits?: Array<{ new_string: string }> }`
- Normalize `file_path` with `path.normalize(filePath).replace(/\\/g, '/')` (Windows path fix per RESEARCH Pitfall 4)
- Check extension with `path.extname()` against `VALIDATED_EXTENSIONS` (D-04: only .tsx and .css)
- Check against `SKIP_PATTERNS` (test files, config, design-tokens.css, theme.css)
- Extract content to check: if `tool_name === 'Write'` use `tool_input.content`; if `'Edit'` use `tool_input.new_string`; if `'MultiEdit'` iterate `tool_input.edits` array and join `.new_string` values (RESEARCH Pitfall 3)
- Check `CRITICAL_PATTERNS` against content. Reset regex lastIndex before each match (global flag).
- If violations found:
  - Write to stderr: `"Design System Violation (BLOCKED):\n"` + joined violation messages + `"\n\nFix: Use var(--color-*) tokens from design-tokens.css instead of raw values.\nReference: packages/app/src/styles/theme.css"`
  - Call `reportViolation(normalizedPath, violationMessages, sessionId)` fire-and-forget (no await)
  - `process.exit(2)` to block the tool call
- If no violations: `process.exit(0)` to allow
- Wrap in `main().catch(() => process.exit(0))` -- silent failure = allow (per RESEARCH)

**2. Create `packages/hooks/src/afw-design-validate-post.ts`** (PostToolUse hook per NEURAL-03/06):

Follow PostToolUse output format from RESEARCH Pattern 1:
- Read JSON from stdin (same as PreToolUse)
- Same file_path normalization, extension check, skip patterns
- Extract content the same way as PreToolUse (Write/Edit/MultiEdit handling)
- Check `WARNING_PATTERNS` against content
- Also check `CRITICAL_PATTERNS` as defense-in-depth (RESEARCH Pitfall 6) -- report at warning level, not block
- If issues found:
  - Output JSON to stdout with `hookSpecificOutput.additionalContext` containing the warning messages (NEURAL-06: semantic compliance via additionalContext)
  - Call `reportViolation(normalizedPath, warningMessages, sessionId)` fire-and-forget with severity 'warning'
  - `process.exit(0)` (PostToolUse does NOT block, only advises)
- If no issues: `process.exit(0)` silently

**3. Update `.claude/settings.json`**:

Add a new entry to the `hooks.PreToolUse` array:
```json
{
  "matcher": "Write|Edit|MultiEdit",
  "hooks": [
    {
      "type": "command",
      "command": "node D:/ActionFlowsDashboard/packages/hooks/dist/hooks/src/afw-design-validate-pre.js",
      "timeout": 15
    }
  ]
}
```

Add a new entry to the `hooks.PostToolUse` array:
```json
{
  "matcher": "Write|Edit|MultiEdit",
  "hooks": [
    {
      "type": "command",
      "command": "node D:/ActionFlowsDashboard/packages/hooks/dist/hooks/src/afw-design-validate-post.js",
      "timeout": 15
    }
  ]
}
```

Preserve all existing hook entries. The new entries are ADDITIONAL, not replacements.

After creating both hook files, run `cd D:/ActionFlowsDashboard && pnpm --filter @afw/hooks build` to compile TypeScript to dist/.
  </action>
  <verify>
    <automated>cd D:/ActionFlowsDashboard && pnpm --filter @afw/hooks build && node -e "const fs=require('fs'); const s=JSON.parse(fs.readFileSync('.claude/settings.json','utf-8')); const pre=s.hooks.PreToolUse.some(e=>e.matcher==='Write|Edit|MultiEdit'); const post=s.hooks.PostToolUse.some(e=>e.matcher==='Write|Edit|MultiEdit'); console.log('PreToolUse registered:', pre); console.log('PostToolUse registered:', post); if(!pre||!post) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "process.exit(2)" packages/hooks/src/afw-design-validate-pre.ts returns at least 1
    - grep -c "process.exit(0)" packages/hooks/src/afw-design-validate-pre.ts returns at least 1
    - grep -c "CRITICAL_PATTERNS" packages/hooks/src/afw-design-validate-pre.ts returns at least 1
    - grep -c "path.normalize" packages/hooks/src/afw-design-validate-pre.ts returns at least 1 (Windows path handling)
    - grep -c "MultiEdit" packages/hooks/src/afw-design-validate-pre.ts returns at least 1
    - grep -c "hookSpecificOutput" packages/hooks/src/afw-design-validate-post.ts returns at least 1
    - grep -c "additionalContext" packages/hooks/src/afw-design-validate-post.ts returns at least 1
    - grep -c "WARNING_PATTERNS" packages/hooks/src/afw-design-validate-post.ts returns at least 1
    - grep "afw-design-validate-pre" .claude/settings.json returns a match
    - grep "afw-design-validate-post" .claude/settings.json returns a match
    - grep "Write|Edit|MultiEdit" .claude/settings.json returns matches
    - pnpm --filter @afw/hooks build succeeds with exit code 0
    - File packages/hooks/dist/hooks/src/afw-design-validate-pre.js exists after build
    - File packages/hooks/dist/hooks/src/afw-design-validate-post.js exists after build
  </acceptance_criteria>
  <done>PreToolUse hook blocks raw hex/rgb/inline-style in .tsx/.css files (exit 2). PostToolUse hook warns on non-token colors via additionalContext. Both handle Write/Edit/MultiEdit input shapes. Both registered in settings.json with "Write|Edit|MultiEdit" matcher and 15s timeout. Hooks build successfully.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @afw/hooks build` -- hooks package compiles cleanly
2. `pnpm type-check` -- no new type errors across monorepo
3. `grep -c "afw-design-validate" .claude/settings.json` -- returns 2 (pre + post registered)
4. `node -e "JSON.parse(require('fs').readFileSync('packages/app/src/components/ui/manifest.json', 'utf-8'))"` -- valid JSON
5. `ls packages/hooks/dist/hooks/src/afw-design-validate-*.js` -- both compiled files exist
</verification>

<success_criteria>
- PreToolUse hook exits 2 when content contains raw hex color in CSS property context (e.g., `color: #fff`)
- PreToolUse hook exits 0 when content uses design tokens (e.g., `var(--color-text)`)
- PostToolUse hook outputs additionalContext warning for hex strings in non-CSS contexts
- Both hooks skip .ts logic files, test files, and design-tokens.css
- Both hooks handle Write (content), Edit (new_string), and MultiEdit (edits[].new_string) input shapes
- manifest.json contains all 12 component entries matching manifest.ts
- Hooks registered in settings.json and build to dist/
</success_criteria>

<output>
After completion, create `.planning/phases/08-neural-validation-safety/08-01-SUMMARY.md`
</output>
