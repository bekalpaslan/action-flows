---
phase: 04.1-framework-docs-realignment
plan: 04
type: execute
wave: 3
depends_on: ["04.1-02", "04.1-03"]
files_modified:
  - .claude/actionflows/actions/*/agent.md (21 files)
  - .claude/actionflows/flows/maintenance/*/instructions.md (6 files)
  - .claude/actionflows/flows/intel/*/instructions.md (1 file)
  - .claude/actionflows/onboarding/*.md (12 files)
  - .claude/actionflows/LEARNINGS.md
autonomous: true
requirements:
  - D-01
  - D-06
must_haves:
  truths:
    - "All 21 agent.md files use new context names (no maintenance or intel affinities)"
    - "All flow instruction files reference correct new contexts"
    - "Onboarding modules teach the agentic OS model, not the cosmic model"
    - "LEARNINGS.md insights preserved with cosmic terminology stripped"
  artifacts:
    - path: ".claude/actionflows/actions/diagnose/agent.md"
      provides: "Agent definition with updated context affinity and zero harmony cosmic refs"
    - path: ".claude/actionflows/actions/verify-healing/agent.md"
      provides: "Agent definition with system health terminology"
    - path: ".claude/actionflows/actions/narrate/agent.md"
      provides: "Agent definition with zero cosmic descriptions"
  key_links:
    - from: "agent.md context affinities"
      to: "CONTEXTS.md context names (from Plan 02)"
      via: "Context names in agent affinities must match CONTEXTS.md"
      pattern: "settings|work|explore|review|pm|archive|studio"
    - from: "flow instructions.md context references"
      to: "FLOWS.md flow registry (from Plan 02)"
      via: "Flow context assignments must match FLOWS.md"
      pattern: "settings|work|explore"
---

<objective>
Batch update all Tier 2 files: 21 agent.md files, 7 maintenance/intel flow instructions, 12 onboarding modules, and LEARNINGS.md. These files need moderate terminology replacement -- more than a simple search-replace but less than a full rewrite.

Purpose: After Plans 02 and 03 established the new vocabulary in core instruction files, these Tier 2 files must match. Agent definitions contain context affinities pointing to "maintenance" and "intel". Flow instructions describe their context association. Onboarding modules teach the cosmic model to new users. LEARNINGS.md contains operational insights wrapped in cosmic terminology.

Output: All ~40 Tier 2 files updated with agentic OS vocabulary and correct context references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-CONTEXT.md
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-RESEARCH.md

# Reference Plan 02 summary for new vocabulary established:
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 21 agent.md files -- context affinities and cosmic descriptions</name>
  <files>.claude/actionflows/actions/analyze/agent.md, .claude/actionflows/actions/analyze/figma-extraction/agent.md, .claude/actionflows/actions/audit/agent.md, .claude/actionflows/actions/brainstorm/agent.md, .claude/actionflows/actions/code/agent.md, .claude/actionflows/actions/code/backend/agent.md, .claude/actionflows/actions/code/frontend/agent.md, .claude/actionflows/actions/commit/agent.md, .claude/actionflows/actions/diagnose/agent.md, .claude/actionflows/actions/docs-lookup/agent.md, .claude/actionflows/actions/isolate/agent.md, .claude/actionflows/actions/narrate/agent.md, .claude/actionflows/actions/notify/agent.md, .claude/actionflows/actions/onboarding/agent.md, .claude/actionflows/actions/plan/agent.md, .claude/actionflows/actions/review/agent.md, .claude/actionflows/actions/second-opinion/agent.md, .claude/actionflows/actions/test/agent.md, .claude/actionflows/actions/test/e2e-playwright/agent.md, .claude/actionflows/actions/test/playwright/agent.md, .claude/actionflows/actions/verify-healing/agent.md</files>
  <read_first>
    - Each of the 21 agent.md files (read each before modifying)
    - .claude/actionflows/CONTEXTS.md (to verify valid context names after Plan 02)
  </read_first>
  <action>
Update all 21 agent.md files. For each file:

1. **Read the file first.** Look for:
   - Context affinity fields referencing "maintenance" or "intel"
   - Cosmic terminology in agent descriptions (harmony as cosmic concept, cosmic metaphors)
   - References to removed contexts

2. **Update context affinities:**
   - "maintenance" -> "settings" (for system management agents like diagnose, verify-healing, isolate)
   - "maintenance" -> "work" (for bug-fixing agents)
   - "intel" -> "explore"
   - Verify the new affinity makes semantic sense for that agent's purpose

3. **Replace cosmic terminology per D-06 mapping:**
   - "harmony violations" -> "system health violations" (in diagnose/agent.md which has 6 harmony refs)
   - "harmony" as cosmic concept -> "system health" (in verify-healing/agent.md which has 4 harmony refs)
   - Cosmic descriptions in narrate/agent.md (6 cosmic refs) -> agentic OS framing
   - "harmony" in code/agent.md (2 harmony refs) -> evaluate if cosmic or code reference
   - Other agents: check for and replace any cosmic terminology

4. **PRESERVE functional vocabulary:**
   - "gate" as a checkpoint concept in agent instructions -> keep
   - Code references to actual file paths -> keep
   - Operational behavior rules -> keep

**Priority order (highest cosmic density first per research):**
- diagnose/agent.md (6 harmony)
- narrate/agent.md (6 cosmic)
- verify-healing/agent.md (4 harmony)
- code/agent.md (2 harmony)
- Then all remaining agents (check each for any cosmic terms)

**Efficiency:** Read each file, make changes if needed, move to next. Some agents may have zero cosmic content -- skip them quickly.
  </action>
  <verify>
    <automated>cd "D:/ActionFlowsDashboard/.claude/worktrees/agent-a0fe8c98" && echo "=== Context affinity check ===" && grep -rli "maintenance\|intel" .claude/actionflows/actions/*/agent.md .claude/actionflows/actions/*/*/agent.md 2>/dev/null | wc -l | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS: no old context affinities"; else echo "FAIL: {} files with old affinities"; fi' && echo "=== Cosmic check ===" && grep -rli "cosmic\|living universe\|big bang\|The Hearth\|The Observatory\|The Cartographer\|The Tribunal\|The Loom\|The War Table\|The Vault\|The Guardian\|The Spy Glass" .claude/actionflows/actions/ --include="agent.md" 2>/dev/null | wc -l | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS: zero cosmic in agents"; else echo "FAIL: {} files with cosmic"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Zero agent.md files reference "maintenance" or "intel" as context affinities
    - Zero agent.md files contain cosmic terminology (cosmic, living universe, big bang, cosmic entity names)
    - All context affinities point to valid contexts: work, explore, review, pm, settings, archive, studio
    - Functional gate vocabulary preserved in agent instructions
    - Code references to actual filenames preserved
  </acceptance_criteria>
  <done>All 21 agent.md files updated with correct context affinities and zero cosmic terminology.</done>
</task>

<task type="auto">
  <name>Task 2: Update flow instructions, onboarding modules, and LEARNINGS.md</name>
  <files>.claude/actionflows/flows/maintenance/*/instructions.md (all), .claude/actionflows/flows/intel/*/instructions.md (all), .claude/actionflows/onboarding/*.md (all), .claude/actionflows/LEARNINGS.md</files>
  <read_first>
    - List all files in .claude/actionflows/flows/maintenance/
    - List all files in .claude/actionflows/flows/intel/
    - List all files in .claude/actionflows/onboarding/ (if exists) or .claude/actionflows/actions/onboarding/modules/
    - .claude/actionflows/LEARNINGS.md
  </read_first>
  <action>
Update three categories of Tier 2 files:

**1. Flow instruction files under maintenance/ and intel/ directories:**

Per research recommendation: keep flow files in their current directory locations. Directory names are less important than FLOWS.md routing (which Plan 02 already updated). Update the CONTENT of each instruction file:

For each flow in `.claude/actionflows/flows/maintenance/`:
- `bug-triage/instructions.md`: Update context reference from "maintenance" to "work"
- `cleanup/instructions.md`: Update context reference from "maintenance" to "settings"
- `design-token-migration/instructions.md`: Update context reference from "maintenance" to "settings"
- `harmony-audit-and-fix/instructions.md` (14 harmony refs per research): Substantial rewrite -- replace "harmony" cosmic concept with "system health", keep operational audit steps intact
- `health-protocol/instructions.md` (7 harmony refs): Replace "harmony" with "system health" in descriptions, keep protocol steps intact
- `logging-check/instructions.md`: Update context reference from "maintenance" to "settings"
- `parser-update/instructions.md`: Update context reference from "maintenance" to "settings"

For `.claude/actionflows/flows/intel/`:
- `intel-analysis/instructions.md` (lines 90-191 reference docs/intel/ paths per research): Update context from "intel" to "explore". Fix any broken cross-references to `docs/intel/` paths -- either remove or update to valid paths.

**2. Onboarding modules:**

Find all onboarding .md files (likely in `.claude/actionflows/actions/onboarding/modules/` or `.claude/actionflows/onboarding/`). Per research:
- `01-welcome.md` (8 cosmic lines): Rewrite welcome to teach agentic OS model, not cosmic model. Replace "Stars", "universe" etc. with workbenches, agents, dashboard.
- `09-harmony.md` (17 harmony lines): Rename conceptually to system health. Rewrite to teach system health monitoring instead of cosmic harmony.
- `10-completion.md` (6 harmony lines): Update completion summary to reference agentic OS concepts.
- All other modules: Check for and replace any cosmic terminology.

**3. LEARNINGS.md:**

Per discretion recommendation from research: Keep all operational insights, strip only cosmic terminology:
- "harmony violations" -> "system health violations"
- "gate" as cosmic entity -> "validation checkpoint" (but "gate" as checkpoint number stays)
- Any cosmic entity names (Stars, Sparks, etc.) -> their agentic OS equivalents
- Preserve the actual lessons learned -- they contain valuable operational knowledge
  </action>
  <verify>
    <automated>cd "D:/ActionFlowsDashboard/.claude/worktrees/agent-a0fe8c98" && echo "=== Flow instructions ===" && grep -rli "maintenance\|intel" .claude/actionflows/flows/maintenance/ .claude/actionflows/flows/intel/ --include="*.md" 2>/dev/null | xargs grep -li "context.*maintenance\|context.*intel" 2>/dev/null | wc -l | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS: no old context refs in flows"; else echo "FAIL: {} flow files with old context"; fi' && echo "=== Onboarding cosmic ===" && grep -rli "cosmic\|living universe\|big bang\|Stars\|Sparks\|Moons" .claude/actionflows/actions/onboarding/ --include="*.md" 2>/dev/null | wc -l | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS: zero cosmic in onboarding"; else echo "FAIL: {} onboarding files with cosmic"; fi' && echo "=== LEARNINGS.md ===" && grep -ci "cosmic\|living universe" .claude/actionflows/LEARNINGS.md 2>/dev/null | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS: no cosmic in learnings"; else echo "FAIL: {} cosmic refs in learnings"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - All maintenance/ flow instructions reference "settings" or "work" instead of "maintenance"
    - intel-analysis/ instructions reference "explore" instead of "intel"
    - harmony-audit-and-fix/instructions.md rewritten with "system health" replacing cosmic "harmony"
    - health-protocol/instructions.md updated with "system health" terminology
    - Onboarding modules teach agentic OS model -- zero cosmic terminology
    - LEARNINGS.md operational insights preserved, cosmic terminology stripped
    - No broken cross-references to docs/intel/ or other deleted paths
  </acceptance_criteria>
  <done>All flow instructions, onboarding modules, and LEARNINGS.md updated. Zero cosmic remnants in Tier 2 files.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rli "cosmic\|living universe\|big bang\|The Hearth\|The Observatory\|The Cartographer\|The Tribunal\|The Loom\|The War Table\|The Vault\|The Guardian\|The Spy Glass" .claude/actionflows/actions/ .claude/actionflows/flows/ --include="*.md" | grep -v /logs/` returns zero results
2. `grep -rli "context.*maintenance\|context.*intel" .claude/actionflows/flows/ --include="*.md"` returns zero results
3. Zero broken cross-references to `docs/intel/` paths
</verification>

<success_criteria>
- All 21 agent.md files: correct context affinities, zero cosmic terminology
- All maintenance/intel flow instructions: updated context references
- All onboarding modules: agentic OS vocabulary
- LEARNINGS.md: insights preserved, cosmic terminology stripped
- Everything consistent with vocabulary established in Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-04-SUMMARY.md`
</output>
