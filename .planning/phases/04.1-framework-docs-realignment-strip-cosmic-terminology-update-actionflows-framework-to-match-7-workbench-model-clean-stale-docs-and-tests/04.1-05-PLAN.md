---
phase: 04.1-framework-docs-realignment
plan: 05
type: execute
wave: 3
depends_on: ["04.1-02", "04.1-03"]
files_modified:
  - .claude/actionflows/actions/*/instructions.md (22+ files)
  - .claude/actionflows/flows/*/instructions.md (remaining non-maintenance/intel)
  - .claude/actionflows/templates/*.md (33 files)
  - .claude/actionflows/checklists/*.md (2 files)
  - .claude/actionflows/actions/agent-standards/instructions.md
autonomous: true
requirements:
  - D-01
  - D-06
must_haves:
  truths:
    - "Zero cosmic terminology exists anywhere in .claude/actionflows/ (excluding logs/)"
    - "No broken cross-references to docs/ paths exist anywhere in actionflows/"
    - "Comprehensive verification grep confirms complete cosmic term removal"
  artifacts:
    - path: ".claude/actionflows/"
      provides: "Entire framework directory with zero cosmic remnants"
  key_links:
    - from: "Verification grep"
      to: "All .md files in .claude/actionflows/"
      via: "Comprehensive term search covers ALL cosmic variants"
      pattern: "cosmic|universe|big bang|hearth|observatory|cartographer|tribunal|loom|war table|vault|guardian|spy glass|Stars|Sparks|Moons|The Void"
---

<objective>
Batch replace cosmic terminology in all remaining Tier 3 files (action instructions, flow instructions, templates, checklists) and run the comprehensive verification grep to confirm zero cosmic remnants across the entire actionflows/ directory.

Purpose: This is the final sweep. Plans 02-04 handled deep rewrites and moderate updates. This plan handles the ~60+ remaining files that need light mechanical replacement plus templates. Then the comprehensive verification grep (Pitfall 5 from research) confirms no cosmic term survived anywhere.

Output: Zero cosmic remnants in the entire .claude/actionflows/ directory. Phase 4.1 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-CONTEXT.md
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-RESEARCH.md

# Reference prior summaries for vocabulary established:
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-02-SUMMARY.md
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch replace cosmic terms in action instructions, flow instructions, templates, and checklists</name>
  <files>.claude/actionflows/actions/*/instructions.md, .claude/actionflows/flows/*/instructions.md (non-maintenance/intel -- those handled in Plan 04), .claude/actionflows/templates/*.md, .claude/actionflows/checklists/*.md, .claude/actionflows/actions/agent-standards/instructions.md</files>
  <read_first>
    - Run `grep -rli "cosmic\|harmony\|maintenance\|intel\|universe\|big bang\|hearth\|observatory\|cartographer\|tribunal\|loom\|war table\|vault\|guardian\|spy glass\|Stars\|Sparks\|Moons\|The Void" .claude/actionflows/ --include="*.md" | grep -v /logs/ | grep -v ORCHESTRATOR | grep -v CONTEXTS | grep -v FLOWS | grep -v ACTIONS | grep -v CONTRACT | grep -v MANUAL | grep -v ROUTING | grep -v GATE_STRUCTURE | grep -v ORCHESTRATOR_OBSERVABILITY | grep -v project.config | grep -v /docs/living/ | grep -v LEARNINGS | grep -v /maintenance/ | grep -v /intel/ | grep -v agent.md | grep -v /onboarding/` to find all remaining files with cosmic terms not yet handled by Plans 02-04
    - This gives the exact list of files this task must update
  </read_first>
  <action>
For every file identified by the grep in read_first:

1. **Read the file.** Assess whether it needs:
   - Simple search-replace (most Tier 3 files)
   - Moderate rewrite (some action instructions with descriptive cosmic sections)

2. **Apply the D-06 terminology mapping:**
   - "Stars" (as cosmic entity, not common word "stars") -> "Workbenches"
   - "Moons" (as cosmic entity) -> "Data Sources"
   - "Gates" (as cosmic entity) -> "Validation Layer" -- BUT "Gate 1", "Gate 2", "Pre-Action Gate" STAY
   - "Sparks" (as cosmic entity) -> "Agents"
   - "The Void" / "Harmony" (as cosmic space) -> "System Health"
   - "Big Bang" -> "Initial Setup"
   - "Cosmic Map" -> "Dashboard"
   - "living universe" -> "agentic OS" or "workbench system"
   - "cosmic" -> replace with appropriate non-cosmic term based on context
   - "universe" (in cosmic context, not "universe of options" etc.) -> "system" or "platform"
   - All cosmic entity proper names: The Hearth, The Observatory, The Cartographer's Table, The Tribunal, The Loom, The War Table, The Vault, The Guardian, The Spy Glass -> their workbench equivalents or remove if referencing deleted entities

3. **Update context references:**
   - "maintenance" context -> "settings" or "work"
   - "intel" context -> "explore"
   - "harmony" context -> "system-health"

4. **Fix cross-references:**
   - Any remaining `docs/architecture/` or `docs/living/` or `docs/intel/` refs -> remove or update
   - Specifically `agent-standards/instructions.md` line 72 references `docs/architecture/CONTRACT_EVOLUTION.md` (per research) -> remove

5. **Handle `## Gate` section headers in action instructions.md files:**
   Per research Tier 3 note: "Each has a `## Gate` section header (functional, not cosmic -- keep as-is)."
   These are functional section headers describing which gate checkpoint the action relates to. KEEP THEM.

6. **Templates:** Most templates reference "harmony" or "maintenance" only in example context fields or cross-reference paths. These are mechanical replacements.

**Efficiency approach:** After the initial grep, sort results by file path. Process action instructions alphabetically, then flows, then templates, then checklists. Many files may need only 1-2 line changes.
  </action>
  <verify>
    <automated>cd "D:/ActionFlowsDashboard/.claude/worktrees/agent-a0fe8c98" && grep -rli "cosmic\|living universe\|big bang\|The Hearth\|The Observatory\|The Cartographer\|The Tribunal\|The Loom\|The War Table\|The Vault\|The Guardian\|The Spy Glass\|Sparks\|Moons" .claude/actionflows/ --include="*.md" | grep -v /logs/ | wc -l | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS: zero cosmic remnants"; else echo "FAIL: {} files with cosmic terms"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Zero files in .claude/actionflows/ (excluding logs/) contain cosmic entity names
    - Zero files reference "maintenance" or "intel" as context names
    - Zero broken cross-references to docs/ paths
    - Functional gate section headers (`## Gate`) preserved in action instructions
    - Functional gate checkpoint names (Gate 1-14) preserved everywhere
  </acceptance_criteria>
  <done>All remaining Tier 3 files updated with zero cosmic remnants.</done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive verification grep -- confirm zero cosmic remnants across entire actionflows/</name>
  <files>No files modified -- verification only</files>
  <read_first>
    - No files to read -- this task runs verification commands
  </read_first>
  <action>
Run the comprehensive verification grep from the research's Validation Architecture section. This is the phase gate -- ALL checks must pass.

**Verification check 1 (D-01 -- zero cosmic remnants):**
```bash
grep -rli "cosmic\|living universe\|big bang\|hearth\|observatory\|cartographer\|tribunal\|loom\|war table\|vault\|guardian\|spy glass" .claude/actionflows/ --include="*.md" | grep -v /logs/ | wc -l
```
Expected: 0

**Verification check 2 (D-03 -- CONTEXTS.md structure):**
```bash
grep -c "^### " .claude/actionflows/CONTEXTS.md
```
Expected: 9 (7 routable: work, explore, review, pm, settings, archive, studio + system-health + editor)

**Verification check 3 (D-04 -- docs/ deleted):**
```bash
test -d docs/ && echo FAIL || echo PASS
```
Expected: PASS

**Verification check 4 (D-05 -- logs/ empty):**
```bash
find .claude/actionflows/logs -type f -not -name "INDEX.md" | wc -l
```
Expected: 0

**Verification check 5 (D-06 -- no old context names in routing):**
```bash
grep -c "^### maintenance\|^### intel" .claude/actionflows/CONTEXTS.md
```
Expected: 0

**Verification check 6 (cross-references clean):**
```bash
grep -rn "docs/architecture\|docs/intel" .claude/actionflows/ --include="*.md" | grep -v /logs/ | wc -l
```
Expected: 0

**Verification check 7 (expanded cosmic term search):**
```bash
grep -rli "universe\|Sparks\|Moons\|The Void\|cosmic map\|Big Bang\|Stars" .claude/actionflows/ --include="*.md" | grep -v /logs/ | wc -l
```
Expected: 0 (Note: "Stars" may false-positive on "starts" or "restart" -- use word boundary grep if needed: `grep -rlwi "Stars"`)

**Verification check 8 (functional vocabulary preserved):**
```bash
grep -c "Pre-Action Gate\|Gate 1\|Gate 2" .claude/actionflows/ORCHESTRATOR.md
```
Expected: > 0 (confirms functional gate vocabulary was NOT accidentally removed)

If ANY check fails, identify the offending file(s) and fix them. This task does not complete until ALL 8 checks pass.
  </action>
  <verify>
    <automated>cd "D:/ActionFlowsDashboard/.claude/worktrees/agent-a0fe8c98" && COSMIC=$(grep -rli "cosmic\|living universe\|big bang\|hearth\|observatory\|cartographer\|tribunal\|loom\|war table\|vault\|guardian\|spy glass" .claude/actionflows/ --include="*.md" | grep -v /logs/ | wc -l) && DOCS=$(test -d docs/ && echo 1 || echo 0) && LOGS=$(find .claude/actionflows/logs -type f -not -name "INDEX.md" 2>/dev/null | wc -l) && OLDCTX=$(grep -c "^### maintenance\|^### intel" .claude/actionflows/CONTEXTS.md) && XREF=$(grep -rn "docs/architecture\|docs/intel" .claude/actionflows/ --include="*.md" 2>/dev/null | grep -v /logs/ | wc -l) && GATES=$(grep -c "Pre-Action Gate" .claude/actionflows/ORCHESTRATOR.md) && echo "Cosmic remnants: $COSMIC (expect 0)" && echo "docs/ exists: $DOCS (expect 0)" && echo "Log files: $LOGS (expect 0)" && echo "Old contexts: $OLDCTX (expect 0)" && echo "Broken xrefs: $XREF (expect 0)" && echo "Gate preserved: $GATES (expect >0)" && if [ "$COSMIC" -eq 0 ] && [ "$DOCS" -eq 0 ] && [ "$LOGS" -eq 0 ] && [ "$OLDCTX" -eq 0 ] && [ "$XREF" -eq 0 ] && [ "$GATES" -gt 0 ]; then echo "ALL CHECKS PASS"; else echo "SOME CHECKS FAILED"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Check 1: Zero cosmic remnants in actionflows/ (excluding logs/)
    - Check 2: CONTEXTS.md has 9 context sections
    - Check 3: docs/ directory does not exist
    - Check 4: logs/ has zero non-INDEX files
    - Check 5: No maintenance or intel context sections
    - Check 6: Zero broken cross-references to docs/ paths
    - Check 7: Expanded cosmic term search returns zero
    - Check 8: Functional gate vocabulary confirmed present
    - ALL 8 checks pass
  </acceptance_criteria>
  <done>Comprehensive verification complete. All 8 checks pass. Phase 4.1 is fully complete -- zero cosmic remnants, all framework files aligned with 7-workbench agentic OS model.</done>
</task>

</tasks>

<verification>
This plan IS the verification plan. The 8-check comprehensive grep in Task 2 is the phase gate.
All checks must return expected values before Phase 4.1 can be marked complete.
</verification>

<success_criteria>
- Every .md file in .claude/actionflows/ (excluding logs/) has zero cosmic terminology
- CONTEXTS.md correctly routes to 7 workbenches + system-health + editor
- docs/ directory deleted
- logs/ content deleted
- Zero broken cross-references
- Functional gate vocabulary preserved
- Phase 4.1 ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-05-SUMMARY.md`
</output>
