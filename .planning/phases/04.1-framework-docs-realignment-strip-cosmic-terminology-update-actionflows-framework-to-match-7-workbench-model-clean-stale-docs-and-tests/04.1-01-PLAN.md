---
phase: 04.1-framework-docs-realignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ (entire directory deleted)
  - .claude/actionflows/logs/ (all files deleted)
  - .claude/actionflows/docs/cosmic/MODEL.md (deleted)
autonomous: true
requirements:
  - D-04
  - D-05
must_haves:
  truths:
    - "The docs/ directory no longer exists in the repository"
    - "The .claude/actionflows/logs/ directory contains no files except optionally INDEX.md"
    - "The cosmic/MODEL.md file no longer exists"
  artifacts:
    - path: "docs/"
      provides: "MUST NOT EXIST after this plan"
    - path: ".claude/actionflows/logs/"
      provides: "Empty directory (no .md files except optional INDEX.md)"
  key_links:
    - from: "Deletion of docs/"
      to: "Plan 03 cross-reference cleanup"
      via: "Plan 03 will fix all broken refs to docs/ paths"
      pattern: "docs/architecture|docs/living|docs/intel"
---

<objective>
Delete all stale content: top-level docs/ folder, all historical log files in actionflows/logs/, and the obsolete cosmic/MODEL.md spec.

Purpose: Remove 497+ files of stale cosmic-era content so that agents never read obsolete references. This is the safest first step -- pure deletion with no rewrite risk. Reduces scope for all subsequent plans.

Output: Clean repository with no docs/, empty logs/, no cosmic model spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-CONTEXT.md
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete docs/ directory and actionflows logs</name>
  <files>docs/, .claude/actionflows/logs/, .claude/actionflows/docs/cosmic/MODEL.md</files>
  <read_first>
    - List docs/ directory to confirm it exists and see its contents
    - List .claude/actionflows/logs/ to confirm file count
    - Confirm .claude/actionflows/docs/cosmic/MODEL.md exists
  </read_first>
  <action>
Delete three targets per D-04, D-05, and research recommendations:

1. **Delete entire docs/ directory** (per D-04): Run `rm -rf docs/`. This removes ~108 files of stale project documentation. Git history preserves all content.

2. **Delete all files in .claude/actionflows/logs/** (per D-05): Run `find .claude/actionflows/logs/ -type f -delete` to remove all ~389 log files. Keep the logs/ directory itself (it may be needed for future log creation). If a logs/INDEX.md exists, it can be deleted too -- fresh start.

3. **Delete .claude/actionflows/docs/cosmic/MODEL.md** (per research recommendation): This file is entirely the cosmic model spec (530+ lines, 53 cosmic references). It is obsolete -- the sidebar navigation model replaces it. Run `rm .claude/actionflows/docs/cosmic/MODEL.md`. If the cosmic/ directory is now empty, remove it too with `rmdir .claude/actionflows/docs/cosmic/`.

Do NOT delete any other files in .claude/actionflows/docs/living/ -- those are handled in Plan 03.
  </action>
  <verify>
    <automated>cd "D:/ActionFlowsDashboard/.claude/worktrees/agent-a0fe8c98" && test -d docs/ && echo "FAIL: docs/ still exists" || echo "PASS: docs/ deleted" && find .claude/actionflows/logs/ -type f 2>/dev/null | wc -l | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS: logs empty"; else echo "FAIL: {} log files remain"; fi' && test -f .claude/actionflows/docs/cosmic/MODEL.md && echo "FAIL: MODEL.md still exists" || echo "PASS: MODEL.md deleted"</automated>
  </verify>
  <acceptance_criteria>
    - docs/ directory does not exist
    - .claude/actionflows/logs/ contains zero files
    - .claude/actionflows/docs/cosmic/MODEL.md does not exist
    - .claude/actionflows/docs/cosmic/ directory does not exist (removed if empty)
    - No other files were deleted
  </acceptance_criteria>
  <done>All three deletion targets confirmed removed. Zero stale files remain in docs/ or logs/.</done>
</task>

<task type="auto">
  <name>Task 2: Verify deletions and check for orphaned references</name>
  <files>No files modified -- verification only</files>
  <read_first>
    - Run git status to confirm expected deletions
  </read_first>
  <action>
Run verification checks to confirm clean deletion:

1. **Confirm docs/ gone:** `test ! -d docs/ && echo PASS`
2. **Confirm logs empty:** `find .claude/actionflows/logs/ -type f | wc -l` should be 0
3. **Confirm MODEL.md gone:** `test ! -f .claude/actionflows/docs/cosmic/MODEL.md && echo PASS`
4. **Count orphaned references to docs/ paths:** Run `grep -rn "docs/" .claude/actionflows/ --include="*.md" | grep -v "/logs/" | grep -v "actionflows/docs/"` to find references to the deleted top-level docs/ folder. These will be fixed in Plan 03 but record the count here for tracking.
5. **Run git status** to see the full deletion diff and confirm only expected paths are removed.

This task produces a count of orphaned cross-references that Plan 03 must fix.
  </action>
  <verify>
    <automated>cd "D:/ActionFlowsDashboard/.claude/worktrees/agent-a0fe8c98" && test ! -d docs/ && echo "PASS: docs/ gone" && test ! -f .claude/actionflows/docs/cosmic/MODEL.md && echo "PASS: MODEL.md gone" && LOGCOUNT=$(find .claude/actionflows/logs/ -type f 2>/dev/null | wc -l) && echo "Log files remaining: $LOGCOUNT"</automated>
  </verify>
  <acceptance_criteria>
    - All three deletion targets confirmed absent
    - Orphaned reference count recorded (expected: 10-15 refs to docs/ paths in actionflows files)
    - Git status shows only expected deletions
  </acceptance_criteria>
  <done>Deletions verified. Orphaned reference count documented for Plan 03 to resolve.</done>
</task>

</tasks>

<verification>
- `test ! -d docs/` returns true
- `find .claude/actionflows/logs/ -type f | wc -l` returns 0
- `test ! -f .claude/actionflows/docs/cosmic/MODEL.md` returns true
- No files outside docs/, logs/, and cosmic/MODEL.md were deleted
</verification>

<success_criteria>
- docs/ directory completely removed (per D-04)
- All log files in actionflows/logs/ removed (per D-05)
- cosmic/MODEL.md removed (obsolete cosmic spec)
- Repository still builds: `pnpm type-check` passes (no code changed)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-01-SUMMARY.md`
</output>
