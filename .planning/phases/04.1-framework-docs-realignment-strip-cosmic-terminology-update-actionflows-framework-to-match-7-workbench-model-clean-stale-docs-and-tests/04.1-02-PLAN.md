---
phase: 04.1-framework-docs-realignment
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - .claude/actionflows/ORCHESTRATOR.md
  - .claude/actionflows/CONTEXTS.md
  - .claude/actionflows/FLOWS.md
  - .claude/actionflows/ACTIONS.md
  - .claude/actionflows/project.config.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-06
must_haves:
  truths:
    - "An agent reading ORCHESTRATOR.md sees workbench-based agentic OS vocabulary with zero cosmic metaphors"
    - "CONTEXTS.md routes to exactly 7 workbenches (Work, Explore, Review, PM, Settings, Archive, Studio) plus system-health auto-target and editor manual context"
    - "FLOWS.md has no maintenance or intel sections -- their flows are reassigned to settings, work, and explore"
    - "ACTIONS.md context affinities reference the new 7-workbench model with no maintenance or intel"
    - "project.config.md project description matches .planning/PROJECT.md framing"
  artifacts:
    - path: ".claude/actionflows/ORCHESTRATOR.md"
      provides: "Orchestrator brain rules with agentic OS vocabulary"
      contains: "workbench"
    - path: ".claude/actionflows/CONTEXTS.md"
      provides: "Context routing with 7 routable + system-health + editor"
      contains: "### studio"
    - path: ".claude/actionflows/FLOWS.md"
      provides: "Flow registry with reassigned maintenance/intel flows"
      contains: "## settings"
    - path: ".claude/actionflows/ACTIONS.md"
      provides: "Action catalog with updated context affinities"
    - path: ".claude/actionflows/project.config.md"
      provides: "Project config with agentic OS description"
  key_links:
    - from: "CONTEXTS.md routing table"
      to: "FLOWS.md flow registry"
      via: "Context names must match between both files"
      pattern: "work|explore|review|pm|settings|archive|studio"
    - from: "ACTIONS.md context affinities"
      to: "CONTEXTS.md context names"
      via: "Action affinities reference valid context names"
      pattern: "settings|work|explore"
---

<objective>
Deep rewrite the 5 core instruction files that the orchestrator and agents read at session start: ORCHESTRATOR.md, CONTEXTS.md, FLOWS.md, ACTIONS.md, and project.config.md.

Purpose: These files establish the vocabulary that all other framework files must match. They must be rewritten first (not just search-replaced) because cosmic terminology is structural -- it appears in headings, entity definitions, routing tables, and philosophy sections. After this plan, the "source of truth" files speak the agentic OS language.

Output: 5 rewritten core instruction files with zero cosmic remnants and correct 7-workbench routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-CONTEXT.md
@.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-RESEARCH.md

# Read these files IN FULL before editing -- they are the rewrite targets:
@.claude/actionflows/ORCHESTRATOR.md
@.claude/actionflows/CONTEXTS.md
@.claude/actionflows/FLOWS.md
@.claude/actionflows/ACTIONS.md
@.claude/actionflows/project.config.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deep rewrite CONTEXTS.md -- restructure routing for 7 workbenches</name>
  <files>.claude/actionflows/CONTEXTS.md</files>
  <read_first>
    - .claude/actionflows/CONTEXTS.md (full file -- this is the rewrite target)
    - .planning/PROJECT.md (lines 33-41 for workbench definitions)
    - .planning/REQUIREMENTS.md (BENCH-01 through BENCH-07 for workbench purposes)
  </read_first>
  <action>
Perform a structural rewrite of CONTEXTS.md per D-03 and the research Flow Reassignment Plan. This is NOT search-replace -- the routing table structure must change.

**Structural changes (per D-03):**

1. **Remove `### maintenance` section entirely.** Its flows are reassigned:
   - `bug-triage/` moves to `work` (bug fixing is active work)
   - `cleanup/` moves to `settings` (housekeeping is system management)
   - `design-token-migration/` moves to `settings`
   - `harmony-audit-and-fix/` moves to `settings` (rename to `health-audit-and-fix/` in description)
   - `health-protocol/` moves to `settings`
   - `logging-check/` moves to `settings`
   - `parser-update/` moves to `settings`
   - `code-and-review/` already exists in `work` -- no duplicate needed

2. **Remove `### intel` section entirely.** Its flow is reassigned:
   - `intel-analysis/` moves to `explore`

3. **Add `### studio` section** as a new routable context:
   - Purpose: Component preview, layout testing, live renders, agent UI materialization
   - Icon: (use a paint/brush icon or similar)
   - Triggers: preview, component preview, test layout, live render, studio, design, prototype, visual test
   - Flows: (empty for now -- Phase 9 will add flows)
   - Examples: "preview the new card component", "test this layout", "open studio", "prototype a dashboard view"

4. **Change `### archive` from Auto-Target to Routable.** Users navigate to it via sidebar.
   - Purpose: Historical sessions, searchable memory
   - Icon: (keep existing)
   - Triggers: history, archived, past sessions, search history, old sessions, look back
   - Flows: (empty for now)
   - Examples: "show me archived sessions", "search past conversations", "find old work on auth"

5. **Rename `### harmony` auto-target to `### system-health`:**
   - Purpose: System health monitoring, violations, and remediations
   - Routing: Populated by system health detection, not user requests

6. **Update "Routable Contexts" count** from 7 to 7 (was 7 with maintenance+intel, now 7 with archive+studio replacing them).

7. **Update the Context-to-Flow Directory Mapping table** at the bottom:
   - Remove maintenance and intel rows
   - Add studio row
   - Update archive row (now routable)
   - Move maintenance flows to settings row
   - Move intel flows to explore row

8. **Update the Routing Guide table:**
   - Change all "maintenance" context entries to their new context (work or settings)
   - Change "intel" entries to "explore"
   - Add studio routing entries
   - Add archive routing entries
   - Rename any "harmony" references in routing guide to "system-health"

**Terminology in descriptions:** Replace any cosmic language in descriptions. "Violations, sins, and remediations" in harmony becomes "System health violations and remediations" in system-health. Keep all functional routing vocabulary intact.

**CRITICAL -- Dual-usage "gate" rule:** The Routing Guide references "HUMAN GATE" in flow chains. This is FUNCTIONAL vocabulary (a human approval checkpoint). Do NOT rename these. Only rename cosmic descriptions of gates as an entity type.
  </action>
  <verify>
    <automated>cd "D:/ActionFlowsDashboard/.claude/worktrees/agent-a0fe8c98" && grep -c "^### " .claude/actionflows/CONTEXTS.md | xargs -I{} bash -c 'echo "Section count: {}"' && grep -c "maintenance\|intel" .claude/actionflows/CONTEXTS.md | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS: no maintenance/intel"; else echo "FAIL: {} refs remain"; fi' && grep -c "studio" .claude/actionflows/CONTEXTS.md | xargs -I{} bash -c 'if [ {} -gt 0 ]; then echo "PASS: studio present"; else echo "FAIL: studio missing"; fi' && grep -c "system-health" .claude/actionflows/CONTEXTS.md | xargs -I{} bash -c 'if [ {} -gt 0 ]; then echo "PASS: system-health present"; else echo "FAIL: system-health missing"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - CONTEXTS.md has exactly 7 routable contexts: work, explore, review, pm, settings, archive, studio
    - CONTEXTS.md has 1 auto-target: system-health
    - CONTEXTS.md has 1 manual: editor
    - No "maintenance" or "intel" context sections remain
    - studio section exists with purpose, triggers, examples
    - archive is promoted to routable with triggers and examples
    - harmony auto-target renamed to system-health
    - Context-to-Flow Directory Mapping table updated
    - Routing Guide table updated
    - Zero cosmic terminology in the file
    - Functional "HUMAN GATE" references preserved in flow chain descriptions
  </acceptance_criteria>
  <done>CONTEXTS.md fully restructured for 7-workbench model with zero cosmic remnants and correct flow reassignments.</done>
</task>

<task type="auto">
  <name>Task 2: Deep rewrite ORCHESTRATOR.md, FLOWS.md, ACTIONS.md, project.config.md</name>
  <files>.claude/actionflows/ORCHESTRATOR.md, .claude/actionflows/FLOWS.md, .claude/actionflows/ACTIONS.md, .claude/actionflows/project.config.md</files>
  <read_first>
    - .claude/actionflows/ORCHESTRATOR.md (full file)
    - .claude/actionflows/FLOWS.md (full file)
    - .claude/actionflows/ACTIONS.md (full file)
    - .claude/actionflows/project.config.md (full file)
    - .planning/PROJECT.md (for new project description framing)
  </read_first>
  <action>
Deep rewrite 4 core instruction files. For each file, read it in full first, then rewrite with cosmic terminology replaced by agentic OS vocabulary per D-06.

**ORCHESTRATOR.md:**
- Rewrite the opening section (lines 10-17, 34 per research) that uses "living universe" framing. Replace with agentic OS framing from .planning/PROJECT.md: workbench-based dashboard, per-workbench agent sessions, sidebar navigation.
- Replace all "harmony" references that describe the cosmic concept with "system health". Example: "harmony violations" becomes "system health violations", "harmony detection" becomes "system health monitoring".
- PRESERVE all functional gate vocabulary: "Pre-Action Gate", "Gate 1", "Gate 2" through "Gate 14" are operational orchestrator checkpoints. Do NOT rename these. Only rename cosmic descriptions of what gates ARE as an entity type.
- PRESERVE all operational behavior rules, chain compilation format, execution protocol.
- Replace "Stars" with "Workbenches", "Sparks" with "Agents", "Moons" with "Data Sources", "Big Bang" with "Initial Setup", "Cosmic Map" with "Dashboard".
- Replace any "living universe" with "agentic personal OS" or "workbench system".
- Update any references to "maintenance" context to "settings" or "work" as appropriate.
- Update any references to "intel" context to "explore".

**FLOWS.md:**
- Remove the `## maintenance` section entirely. Reassign its flows:
  - Move `bug-triage/` to `## work` section
  - Move `cleanup/`, `design-token-migration/`, `harmony-audit-and-fix/`, `health-protocol/`, `parser-update/` to `## settings` section
  - The `code-and-review/` already exists in work -- do not duplicate
- Remove the `## intel` section entirely. Move `intel-analysis/` to `## explore` section.
- Add `## studio` section (empty for now, placeholder for Phase 9)
- Add `## archive` section (empty for now, placeholder)
- In the "Healing Flows Philosophy" section: replace "harmony" with "system health" in descriptions. Keep the operational healing flow mechanism intact.
- In Detailed Flow Specifications: update `harmony-audit-and-fix/` description to use "system health" instead of "harmony". Update its context from "maintenance" to "settings".
- Replace any remaining cosmic terminology per D-06 mapping.

**ACTIONS.md:**
- Update ALL context affinity columns: replace "maintenance" with "settings" or "work" as appropriate per the flow reassignment plan.
- Remove any "intel" context affinities, replace with "explore".
- Replace cosmic terminology in action descriptions per D-06.
- Keep all action definitions structurally intact -- only update terminology and context names.

**project.config.md:**
- Rewrite the project description block (lines 10-15 per research) to match .planning/PROJECT.md framing.
- Replace "living universe" tagline with "Agentic Personal OS" framing.
- Replace "For Coders/Regular Users/Everyone" descriptions with the versions from .planning/PROJECT.md (which already use agentic OS language, but verify and sync).
- Update any workbench references to use the new 7 names.
- Keep all technical configuration values (ports, paths, commands) unchanged.
  </action>
  <verify>
    <automated>cd "D:/ActionFlowsDashboard/.claude/worktrees/agent-a0fe8c98" && echo "=== ORCHESTRATOR.md ===" && grep -ci "living universe\|cosmic map\|big bang\|Stars.*Workbenches\|Sparks.*Agents" .claude/actionflows/ORCHESTRATOR.md | xargs -I{} echo "Cosmic remnants: {}" && echo "=== FLOWS.md ===" && grep -c "^## maintenance\|^## intel" .claude/actionflows/FLOWS.md | xargs -I{} bash -c 'if [ {} -eq 0 ]; then echo "PASS: no maintenance/intel sections"; else echo "FAIL: {} old sections"; fi' && grep -c "^## studio" .claude/actionflows/FLOWS.md | xargs -I{} bash -c 'if [ {} -gt 0 ]; then echo "PASS: studio section present"; else echo "FAIL: studio missing"; fi' && echo "=== ACTIONS.md ===" && grep -ci "maintenance\|intel" .claude/actionflows/ACTIONS.md | xargs -I{} echo "Old context refs: {}" && echo "=== project.config.md ===" && grep -ci "living universe\|cosmic" .claude/actionflows/project.config.md | xargs -I{} echo "Cosmic refs: {}"</automated>
  </verify>
  <acceptance_criteria>
    - ORCHESTRATOR.md: zero "living universe", "cosmic map", "big bang" references; "Pre-Action Gate" and "Gate 1-14" preserved; harmony as cosmic concept replaced with system health
    - FLOWS.md: no ## maintenance or ## intel sections; bug-triage under work; cleanup/health flows under settings; intel-analysis under explore; studio and archive sections present; healing philosophy uses "system health"
    - ACTIONS.md: no "maintenance" or "intel" in context affinities; all affinities reference valid new context names
    - project.config.md: project description matches agentic OS framing; zero cosmic terminology
    - All operational behavior rules, chain formats, gate checkpoint system preserved intact
  </acceptance_criteria>
  <done>All 4 core instruction files rewritten with agentic OS vocabulary. Operational rules preserved. Flow reassignment complete. Context affinities updated.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rli "living universe\|cosmic map\|big bang\|The Hearth\|The Observatory\|The Cartographer\|The Tribunal\|The Loom\|The War Table\|The Vault\|The Guardian\|The Spy Glass" .claude/actionflows/ORCHESTRATOR.md .claude/actionflows/CONTEXTS.md .claude/actionflows/FLOWS.md .claude/actionflows/ACTIONS.md .claude/actionflows/project.config.md` returns zero results
2. `grep -c "^## maintenance\|^## intel" .claude/actionflows/FLOWS.md` returns 0
3. `grep -c "^### maintenance\|^### intel" .claude/actionflows/CONTEXTS.md` returns 0
4. `grep -c "studio" .claude/actionflows/CONTEXTS.md` returns > 0
5. ORCHESTRATOR.md still contains "Pre-Action Gate" and "Gate 1" (functional vocabulary preserved)
</verification>

<success_criteria>
- All 5 core instruction files use agentic OS vocabulary exclusively
- CONTEXTS.md routes to exactly 7 workbenches + system-health + editor
- FLOWS.md has all maintenance flows reassigned to work/settings
- FLOWS.md has intel flows reassigned to explore
- ACTIONS.md context affinities match new context names
- project.config.md description matches .planning/PROJECT.md
- Functional gate vocabulary (Gate 1-14, Pre-Action Gate) preserved
- Functional code references (harmonyDetector.ts, API paths) not renamed
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-framework-docs-realignment-strip-cosmic-terminology-update-actionflows-framework-to-match-7-workbench-model-clean-stale-docs-and-tests/04.1-02-SUMMARY.md`
</output>
