---
phase: 03-design-system
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/app/src/components/ui/dialog.tsx
  - packages/app/src/components/ui/tabs.tsx
  - packages/app/src/components/ui/tooltip.tsx
  - packages/app/src/components/ui/dropdown-menu.tsx
  - packages/app/src/components/ui/select.tsx
  - packages/app/src/components/ui/radio-group.tsx
autonomous: true
requirements:
  - DESIGN-02
  - DESIGN-03
must_haves:
  truths:
    - "Dialog renders with overlay, content panel, close button, and 4 size variants (sm, md, lg, full)"
    - "Tabs renders with list, triggers, and content panels with active accent indicator"
    - "Tooltip renders with delay, positioning, and fade-in animation"
    - "Dropdown menu renders with items, separators, labels, checkbox items, and radio items"
    - "Select renders with trigger matching Input visual contract and dropdown content"
    - "Radio group renders with radio items showing accent circle when checked"
  artifacts:
    - path: "packages/app/src/components/ui/dialog.tsx"
      provides: "Dialog with Radix primitives and overlay"
      exports: ["Dialog", "DialogTrigger", "DialogContent", "DialogHeader", "DialogFooter", "DialogTitle", "DialogDescription", "DialogClose"]
    - path: "packages/app/src/components/ui/tabs.tsx"
      provides: "Tabs with Radix primitives"
      exports: ["Tabs", "TabsList", "TabsTrigger", "TabsContent"]
    - path: "packages/app/src/components/ui/tooltip.tsx"
      provides: "Tooltip with Radix primitives"
      exports: ["Tooltip", "TooltipTrigger", "TooltipContent", "TooltipProvider"]
    - path: "packages/app/src/components/ui/dropdown-menu.tsx"
      provides: "DropdownMenu with Radix primitives"
      exports: ["DropdownMenu", "DropdownMenuTrigger", "DropdownMenuContent", "DropdownMenuItem", "DropdownMenuSeparator", "DropdownMenuLabel", "DropdownMenuCheckboxItem", "DropdownMenuRadioItem", "DropdownMenuGroup"]
    - path: "packages/app/src/components/ui/select.tsx"
      provides: "Select with Radix primitives"
      exports: ["Select", "SelectTrigger", "SelectContent", "SelectItem", "SelectValue", "SelectGroup", "SelectLabel"]
    - path: "packages/app/src/components/ui/radio-group.tsx"
      provides: "RadioGroup with Radix primitives"
      exports: ["RadioGroup", "RadioGroupItem"]
  key_links:
    - from: "packages/app/src/components/ui/dialog.tsx"
      to: "@radix-ui/react-dialog"
      via: "import"
      pattern: "import.*DialogPrimitive.*from.*@radix-ui/react-dialog"
    - from: "packages/app/src/components/ui/dropdown-menu.tsx"
      to: "@radix-ui/react-dropdown-menu"
      via: "import"
      pattern: "import.*DropdownMenuPrimitive.*from.*@radix-ui/react-dropdown-menu"
---

<objective>
Build 6 Radix-heavy components: Dialog, Tabs, Tooltip, DropdownMenu, Select, RadioGroup. These components wrap Radix UI primitives with Tailwind styling and cn() class composition.

Purpose: Second batch of the 12-component library (per D-03). These are the overlay and interactive components that require Radix for accessibility.
Output: 6 fully typed component files using Radix primitives + CVA + Tailwind + cn() with exact UI-SPEC values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system/03-UI-SPEC.md
@.planning/phases/03-design-system/03-RESEARCH.md
@.planning/phases/03-design-system/03-01-SUMMARY.md

<interfaces>
From packages/app/src/lib/utils.ts (created in Plan 01):
```typescript
export function cn(...inputs: ClassValue[]): string;
```

Tailwind utilities available from theme.css:
- Semantic colors: bg-surface, bg-surface-2, bg-surface-3, bg-accent, text-text, text-text-dim, text-text-muted, border-border, border-border-strong, text-destructive, text-warning, text-success, text-info
- Radius: rounded-xs (4px), rounded-sm (6px), rounded-md (8px), rounded-lg (10px), rounded-xl (14px), rounded-2xl (18px), rounded-full
- Shadows: shadow-xs, shadow-sm, shadow-md, shadow-lg, shadow-xl, shadow-2xl
- Glows: shadow-glow-default, shadow-glow-hover, shadow-glow-focus, shadow-glow-accent
- Typography: text-caption (13px), text-body (15px), text-heading (20px), text-display (28px)
- Animations: animate-fade-in (150ms), animate-fade-in-scale (400ms), animate-slide-in-bottom (150ms)
- Z-index: z-[1000] (dropdown), z-[1300] (modal-backdrop), z-[1400] (modal), z-[1500] (popover), z-[1600] (tooltip)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dialog, Tabs, and Tooltip components</name>
  <files>
    packages/app/src/components/ui/dialog.tsx,
    packages/app/src/components/ui/tabs.tsx,
    packages/app/src/components/ui/tooltip.tsx
  </files>
  <read_first>
    packages/app/src/lib/utils.ts,
    packages/app/src/styles/theme.css,
    .planning/phases/03-design-system/03-UI-SPEC.md,
    .planning/phases/03-design-system/03-RESEARCH.md
  </read_first>
  <action>
**Dialog (`packages/app/src/components/ui/dialog.tsx`):**
Per UI-SPEC Dialog spec. Uses @radix-ui/react-dialog. The RESEARCH.md provides a complete reference implementation. Key specs:
- Overlay: rgba(0,0,0,0.5) with backdrop-blur-sm, z-[1300]
- Content: centered (fixed left-1/2 top-1/2 -translate), border border-border, bg-surface-2, p-6, shadow-xl, z-[1400], rounded-xl (14px)
- Animation: data-[state=open]:animate-fade-in-scale (400ms ease-out)
- Close button: absolute right-4 top-4, X icon (lucide-react), sr-only "Close" text
- Sub-components: Dialog, DialogTrigger, DialogPortal, DialogOverlay, DialogClose, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription
- DialogTitle: text-heading font-semibold text-text
- DialogDescription: text-body text-text-dim
- DialogFooter: flex flex-col-reverse gap-2 sm:flex-row sm:justify-end

Use the EXACT code from RESEARCH.md Pattern 3 (Dialog Component), which already has correct values. Import X icon from lucide-react.

**Tabs (`packages/app/src/components/ui/tabs.tsx`):**
Per UI-SPEC Tabs spec. Uses @radix-ui/react-tabs. Key specs:
- TabsList: flex items-center gap-1 border-b border-border bg-transparent
- TabsTrigger: h-11 (44px touch target), px-4, text-caption font-semibold, text-text-dim default, text-text + accent bottom border on active
- Active indicator: data-[state=active]:border-b-2 data-[state=active]:border-accent data-[state=active]:text-text
- Hover: hover:text-text
- TabsContent: mt-2

```typescript
import * as React from 'react';
import * as TabsPrimitive from '@radix-ui/react-tabs';
import { cn } from '@/lib/utils';

const Tabs = TabsPrimitive.Root;

const TabsList = React.forwardRef<
  React.ComponentRef<typeof TabsPrimitive.List>,
  React.ComponentPropsWithoutRef<typeof TabsPrimitive.List>
>(({ className, ...props }, ref) => (
  <TabsPrimitive.List
    ref={ref}
    className={cn('flex items-center gap-1 border-b border-border', className)}
    {...props}
  />
));
TabsList.displayName = TabsPrimitive.List.displayName;

const TabsTrigger = React.forwardRef<
  React.ComponentRef<typeof TabsPrimitive.Trigger>,
  React.ComponentPropsWithoutRef<typeof TabsPrimitive.Trigger>
>(({ className, ...props }, ref) => (
  <TabsPrimitive.Trigger
    ref={ref}
    className={cn(
      'inline-flex h-11 items-center justify-center px-4 text-caption font-semibold text-text-dim',
      'border-b-2 border-transparent transition-colors',
      'hover:text-text',
      'focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent',
      'disabled:pointer-events-none disabled:opacity-50',
      'data-[state=active]:border-accent data-[state=active]:text-text',
      className
    )}
    {...props}
  />
));
TabsTrigger.displayName = TabsPrimitive.Trigger.displayName;

const TabsContent = React.forwardRef<
  React.ComponentRef<typeof TabsPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof TabsPrimitive.Content>
>(({ className, ...props }, ref) => (
  <TabsPrimitive.Content
    ref={ref}
    className={cn('mt-2 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent', className)}
    {...props}
  />
));
TabsContent.displayName = TabsPrimitive.Content.displayName;

export { Tabs, TabsList, TabsTrigger, TabsContent };
```

**Tooltip (`packages/app/src/components/ui/tooltip.tsx`):**
Per UI-SPEC Tooltip spec. Uses @radix-ui/react-tooltip. Key specs:
- Background: bg-surface-3 (dark: #14213d, light: #e5e5e5)
- Text: text-caption text-text (13px)
- Padding: px-3 py-1.5 (12px horizontal, 6px vertical)
- Border radius: rounded-sm (6px)
- Z-index: z-[1600]
- Animation: animate-fade-in + animate-slide-in-bottom, duration-fast (150ms)
- Delay: 400ms open delay, 0ms close delay (set on TooltipProvider)

```typescript
import * as React from 'react';
import * as TooltipPrimitive from '@radix-ui/react-tooltip';
import { cn } from '@/lib/utils';

const TooltipProvider = TooltipPrimitive.Provider;
const Tooltip = TooltipPrimitive.Root;
const TooltipTrigger = TooltipPrimitive.Trigger;

const TooltipContent = React.forwardRef<
  React.ComponentRef<typeof TooltipPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof TooltipPrimitive.Content>
>(({ className, sideOffset = 4, ...props }, ref) => (
  <TooltipPrimitive.Portal>
    <TooltipPrimitive.Content
      ref={ref}
      sideOffset={sideOffset}
      className={cn(
        'z-[1600] overflow-hidden rounded-sm bg-surface-3 px-3 py-1.5 text-caption text-text shadow-md',
        'animate-fade-in',
        className
      )}
      {...props}
    />
  </TooltipPrimitive.Portal>
));
TooltipContent.displayName = TooltipPrimitive.Content.displayName;

export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider };
```

Note: The 400ms open delay and 0ms close delay are configured on the TooltipProvider via its `delayDuration` prop (default 400ms in Radix) and `skipDelayDuration` prop. The TooltipProvider should wrap the app at the root level (will be added in a later phase when tooltips are used).
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit --pretty 2>&1 | grep -E "error TS" | grep -E "components/ui/(dialog|tabs|tooltip)" | head -5 || echo "No type errors in dialog/tabs/tooltip"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DialogPrimitive" packages/app/src/components/ui/dialog.tsx` matches (Radix import)
    - `grep "DialogOverlay\|DialogContent\|DialogHeader\|DialogFooter\|DialogTitle\|DialogDescription\|DialogClose" packages/app/src/components/ui/dialog.tsx` returns 7+ matches
    - `grep "z-\[1400\]" packages/app/src/components/ui/dialog.tsx` matches (modal z-index)
    - `grep "z-\[1300\]" packages/app/src/components/ui/dialog.tsx` matches (backdrop z-index)
    - `grep "sr-only" packages/app/src/components/ui/dialog.tsx` matches (accessible close button)
    - `grep "TabsPrimitive" packages/app/src/components/ui/tabs.tsx` matches (Radix import)
    - `grep "h-11" packages/app/src/components/ui/tabs.tsx` matches (44px touch target)
    - `grep "data-\[state=active\]:border-accent" packages/app/src/components/ui/tabs.tsx` matches
    - `grep "TooltipPrimitive" packages/app/src/components/ui/tooltip.tsx` matches (Radix import)
    - `grep "z-\[1600\]" packages/app/src/components/ui/tooltip.tsx` matches (tooltip z-index)
    - `grep "bg-surface-3" packages/app/src/components/ui/tooltip.tsx` matches
    - No .css files in packages/app/src/components/ui/
  </acceptance_criteria>
  <done>
    Dialog, Tabs, and Tooltip components exist with correct Radix primitives, all UI-SPEC visual specs, z-index values, and animation classes. Dialog has accessible close button with sr-only text. Tabs triggers are 44px touch targets. Tooltip uses surface-3 background.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DropdownMenu, Select, and RadioGroup components</name>
  <files>
    packages/app/src/components/ui/dropdown-menu.tsx,
    packages/app/src/components/ui/select.tsx,
    packages/app/src/components/ui/radio-group.tsx
  </files>
  <read_first>
    packages/app/src/lib/utils.ts,
    packages/app/src/styles/theme.css,
    .planning/phases/03-design-system/03-UI-SPEC.md
  </read_first>
  <action>
**DropdownMenu (`packages/app/src/components/ui/dropdown-menu.tsx`):**
Per UI-SPEC Dropdown spec. Uses @radix-ui/react-dropdown-menu. Key specs:
- Content: bg-surface-3, border 1px solid border-strong, rounded-md (8px), shadow-lg, z-[1000]
- Animation: data-[state=open]:animate-fade-in (scale from 95%), duration-fast (150ms)
- Item: min-h-9 (36px), px-2, text-body, cursor-pointer, rounded-sm for inner radius
- Item hover: bg-accent/10 (10% accent opacity) -- use bg-[rgba(62,103,191,0.10)]
- Separator: h-px bg-border, my-1
- Label: px-2 py-1.5 text-caption font-semibold text-text-dim
- Sub-components: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuLabel, DropdownMenuCheckboxItem, DropdownMenuRadioItem, DropdownMenuGroup

```typescript
import * as React from 'react';
import * as DropdownMenuPrimitive from '@radix-ui/react-dropdown-menu';
import { Check, Circle } from 'lucide-react';
import { cn } from '@/lib/utils';

const DropdownMenu = DropdownMenuPrimitive.Root;
const DropdownMenuTrigger = DropdownMenuPrimitive.Trigger;
const DropdownMenuGroup = DropdownMenuPrimitive.Group;
const DropdownMenuPortal = DropdownMenuPrimitive.Portal;
const DropdownMenuSub = DropdownMenuPrimitive.Sub;

const DropdownMenuContent = React.forwardRef<
  React.ComponentRef<typeof DropdownMenuPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Content>
>(({ className, sideOffset = 4, ...props }, ref) => (
  <DropdownMenuPrimitive.Portal>
    <DropdownMenuPrimitive.Content
      ref={ref}
      sideOffset={sideOffset}
      className={cn(
        'z-[1000] min-w-[8rem] overflow-hidden rounded-md border border-border-strong bg-surface-3 p-1 shadow-lg',
        'data-[state=open]:animate-fade-in',
        className
      )}
      {...props}
    />
  </DropdownMenuPrimitive.Portal>
));
DropdownMenuContent.displayName = DropdownMenuPrimitive.Content.displayName;

const DropdownMenuItem = React.forwardRef<
  React.ComponentRef<typeof DropdownMenuPrimitive.Item>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Item> & { inset?: boolean }
>(({ className, inset, ...props }, ref) => (
  <DropdownMenuPrimitive.Item
    ref={ref}
    className={cn(
      'relative flex min-h-9 cursor-pointer select-none items-center gap-2 rounded-sm px-2 py-1.5 text-body text-text outline-none transition-colors',
      'focus:bg-[rgba(62,103,191,0.10)] focus:text-text',
      'data-[disabled]:pointer-events-none data-[disabled]:opacity-50',
      inset && 'pl-8',
      className
    )}
    {...props}
  />
));
DropdownMenuItem.displayName = DropdownMenuPrimitive.Item.displayName;

const DropdownMenuCheckboxItem = React.forwardRef<
  React.ComponentRef<typeof DropdownMenuPrimitive.CheckboxItem>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.CheckboxItem>
>(({ className, children, checked, ...props }, ref) => (
  <DropdownMenuPrimitive.CheckboxItem
    ref={ref}
    className={cn(
      'relative flex min-h-9 cursor-pointer select-none items-center rounded-sm py-1.5 pl-8 pr-2 text-body text-text outline-none transition-colors',
      'focus:bg-[rgba(62,103,191,0.10)]',
      'data-[disabled]:pointer-events-none data-[disabled]:opacity-50',
      className
    )}
    checked={checked}
    {...props}
  >
    <span className="absolute left-2 flex h-3.5 w-3.5 items-center justify-center">
      <DropdownMenuPrimitive.ItemIndicator>
        <Check className="h-4 w-4" />
      </DropdownMenuPrimitive.ItemIndicator>
    </span>
    {children}
  </DropdownMenuPrimitive.CheckboxItem>
));
DropdownMenuCheckboxItem.displayName = DropdownMenuPrimitive.CheckboxItem.displayName;

const DropdownMenuRadioItem = React.forwardRef<
  React.ComponentRef<typeof DropdownMenuPrimitive.RadioItem>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.RadioItem>
>(({ className, children, ...props }, ref) => (
  <DropdownMenuPrimitive.RadioItem
    ref={ref}
    className={cn(
      'relative flex min-h-9 cursor-pointer select-none items-center rounded-sm py-1.5 pl-8 pr-2 text-body text-text outline-none transition-colors',
      'focus:bg-[rgba(62,103,191,0.10)]',
      'data-[disabled]:pointer-events-none data-[disabled]:opacity-50',
      className
    )}
    {...props}
  >
    <span className="absolute left-2 flex h-3.5 w-3.5 items-center justify-center">
      <DropdownMenuPrimitive.ItemIndicator>
        <Circle className="h-2 w-2 fill-current" />
      </DropdownMenuPrimitive.ItemIndicator>
    </span>
    {children}
  </DropdownMenuPrimitive.RadioItem>
));
DropdownMenuRadioItem.displayName = DropdownMenuPrimitive.RadioItem.displayName;

const DropdownMenuLabel = React.forwardRef<
  React.ComponentRef<typeof DropdownMenuPrimitive.Label>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Label> & { inset?: boolean }
>(({ className, inset, ...props }, ref) => (
  <DropdownMenuPrimitive.Label
    ref={ref}
    className={cn('px-2 py-1.5 text-caption font-semibold text-text-dim', inset && 'pl-8', className)}
    {...props}
  />
));
DropdownMenuLabel.displayName = DropdownMenuPrimitive.Label.displayName;

const DropdownMenuSeparator = React.forwardRef<
  React.ComponentRef<typeof DropdownMenuPrimitive.Separator>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Separator>
>(({ className, ...props }, ref) => (
  <DropdownMenuPrimitive.Separator
    ref={ref}
    className={cn('-mx-1 my-1 h-px bg-border', className)}
    {...props}
  />
));
DropdownMenuSeparator.displayName = DropdownMenuPrimitive.Separator.displayName;

export {
  DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem,
  DropdownMenuCheckboxItem, DropdownMenuRadioItem, DropdownMenuLabel,
  DropdownMenuSeparator, DropdownMenuGroup, DropdownMenuPortal, DropdownMenuSub,
};
```

**Select (`packages/app/src/components/ui/select.tsx`):**
Per UI-SPEC Select spec and RESEARCH.md recommendation: use @radix-ui/react-select (not popover) for full accessibility (typeahead, scroll-into-view, grouped items, correct ARIA combobox semantics). Style trigger to match Input visual contract (h-10, border, rounded-md). Content matches Dropdown visual (surface-3, shadow-lg). Item height 36px. Selected item shows accent text + checkmark.

```typescript
import * as React from 'react';
import * as SelectPrimitive from '@radix-ui/react-select';
import { Check, ChevronDown, ChevronUp } from 'lucide-react';
import { cn } from '@/lib/utils';

const Select = SelectPrimitive.Root;
const SelectGroup = SelectPrimitive.Group;
const SelectValue = SelectPrimitive.Value;

const SelectTrigger = React.forwardRef<
  React.ComponentRef<typeof SelectPrimitive.Trigger>,
  React.ComponentPropsWithoutRef<typeof SelectPrimitive.Trigger>
>(({ className, children, ...props }, ref) => (
  <SelectPrimitive.Trigger
    ref={ref}
    className={cn(
      'flex h-10 w-full items-center justify-between rounded-md border border-border bg-[rgba(138,138,138,0.24)] px-3 text-body text-text',
      'placeholder:text-text-muted',
      'focus-visible:outline-none focus-visible:border-accent focus-visible:shadow-glow-focus',
      'disabled:cursor-not-allowed disabled:opacity-50',
      '[&>span]:line-clamp-1',
      className
    )}
    {...props}
  >
    {children}
    <SelectPrimitive.Icon asChild>
      <ChevronDown className="h-4 w-4 text-text-muted" />
    </SelectPrimitive.Icon>
  </SelectPrimitive.Trigger>
));
SelectTrigger.displayName = SelectPrimitive.Trigger.displayName;

const SelectScrollUpButton = React.forwardRef<
  React.ComponentRef<typeof SelectPrimitive.ScrollUpButton>,
  React.ComponentPropsWithoutRef<typeof SelectPrimitive.ScrollUpButton>
>(({ className, ...props }, ref) => (
  <SelectPrimitive.ScrollUpButton
    ref={ref}
    className={cn('flex cursor-default items-center justify-center py-1', className)}
    {...props}
  >
    <ChevronUp className="h-4 w-4 text-text-muted" />
  </SelectPrimitive.ScrollUpButton>
));
SelectScrollUpButton.displayName = SelectPrimitive.ScrollUpButton.displayName;

const SelectScrollDownButton = React.forwardRef<
  React.ComponentRef<typeof SelectPrimitive.ScrollDownButton>,
  React.ComponentPropsWithoutRef<typeof SelectPrimitive.ScrollDownButton>
>(({ className, ...props }, ref) => (
  <SelectPrimitive.ScrollDownButton
    ref={ref}
    className={cn('flex cursor-default items-center justify-center py-1', className)}
    {...props}
  >
    <ChevronDown className="h-4 w-4 text-text-muted" />
  </SelectPrimitive.ScrollDownButton>
));
SelectScrollDownButton.displayName = SelectPrimitive.ScrollDownButton.displayName;

const SelectContent = React.forwardRef<
  React.ComponentRef<typeof SelectPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof SelectPrimitive.Content>
>(({ className, children, position = 'popper', ...props }, ref) => (
  <SelectPrimitive.Portal>
    <SelectPrimitive.Content
      ref={ref}
      className={cn(
        'relative z-[1500] max-h-96 min-w-[8rem] overflow-hidden rounded-md border border-border-strong bg-surface-3 shadow-lg',
        'data-[state=open]:animate-fade-in',
        position === 'popper' && 'data-[side=bottom]:translate-y-1 data-[side=top]:-translate-y-1',
        className
      )}
      position={position}
      {...props}
    >
      <SelectScrollUpButton />
      <SelectPrimitive.Viewport
        className={cn(
          'p-1',
          position === 'popper' && 'h-[var(--radix-select-trigger-height)] w-full min-w-[var(--radix-select-trigger-width)]'
        )}
      >
        {children}
      </SelectPrimitive.Viewport>
      <SelectScrollDownButton />
    </SelectPrimitive.Content>
  </SelectPrimitive.Portal>
));
SelectContent.displayName = SelectPrimitive.Content.displayName;

const SelectLabel = React.forwardRef<
  React.ComponentRef<typeof SelectPrimitive.Label>,
  React.ComponentPropsWithoutRef<typeof SelectPrimitive.Label>
>(({ className, ...props }, ref) => (
  <SelectPrimitive.Label
    ref={ref}
    className={cn('px-2 py-1.5 text-caption font-semibold text-text-dim', className)}
    {...props}
  />
));
SelectLabel.displayName = SelectPrimitive.Label.displayName;

const SelectItem = React.forwardRef<
  React.ComponentRef<typeof SelectPrimitive.Item>,
  React.ComponentPropsWithoutRef<typeof SelectPrimitive.Item>
>(({ className, children, ...props }, ref) => (
  <SelectPrimitive.Item
    ref={ref}
    className={cn(
      'relative flex min-h-9 w-full cursor-pointer select-none items-center rounded-sm py-1.5 pl-8 pr-2 text-body text-text outline-none',
      'focus:bg-[rgba(62,103,191,0.10)] focus:text-text',
      'data-[disabled]:pointer-events-none data-[disabled]:opacity-50',
      className
    )}
    {...props}
  >
    <span className="absolute left-2 flex h-3.5 w-3.5 items-center justify-center">
      <SelectPrimitive.ItemIndicator>
        <Check className="h-4 w-4 text-accent" />
      </SelectPrimitive.ItemIndicator>
    </span>
    <SelectPrimitive.ItemText>{children}</SelectPrimitive.ItemText>
  </SelectPrimitive.Item>
));
SelectItem.displayName = SelectPrimitive.Item.displayName;

const SelectSeparator = React.forwardRef<
  React.ComponentRef<typeof SelectPrimitive.Separator>,
  React.ComponentPropsWithoutRef<typeof SelectPrimitive.Separator>
>(({ className, ...props }, ref) => (
  <SelectPrimitive.Separator
    ref={ref}
    className={cn('-mx-1 my-1 h-px bg-border', className)}
    {...props}
  />
));
SelectSeparator.displayName = SelectPrimitive.Separator.displayName;

export {
  Select, SelectGroup, SelectValue, SelectTrigger, SelectContent,
  SelectLabel, SelectItem, SelectSeparator, SelectScrollUpButton, SelectScrollDownButton,
};
```

**RadioGroup (`packages/app/src/components/ui/radio-group.tsx`):**
Per UI-SPEC Radio spec. Uses @radix-ui/react-radio-group. Key specs:
- Size: 16px circle (w-4 h-4), rounded-full
- Unchecked: transparent bg, border-default
- Checked: accent border, inner 8px accent circle (h-2 w-2)
- Disabled: opacity-50, cursor-not-allowed
- Focus: 2px accent outline, 2px offset

```typescript
import * as React from 'react';
import * as RadioGroupPrimitive from '@radix-ui/react-radio-group';
import { Circle } from 'lucide-react';
import { cn } from '@/lib/utils';

const RadioGroup = React.forwardRef<
  React.ComponentRef<typeof RadioGroupPrimitive.Root>,
  React.ComponentPropsWithoutRef<typeof RadioGroupPrimitive.Root>
>(({ className, ...props }, ref) => (
  <RadioGroupPrimitive.Root
    className={cn('grid gap-2', className)}
    {...props}
    ref={ref}
  />
));
RadioGroup.displayName = RadioGroupPrimitive.Root.displayName;

const RadioGroupItem = React.forwardRef<
  React.ComponentRef<typeof RadioGroupPrimitive.Item>,
  React.ComponentPropsWithoutRef<typeof RadioGroupPrimitive.Item>
>(({ className, ...props }, ref) => (
  <RadioGroupPrimitive.Item
    ref={ref}
    className={cn(
      'aspect-square h-4 w-4 rounded-full border border-border bg-transparent text-accent transition-colors',
      'focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent',
      'disabled:cursor-not-allowed disabled:opacity-50',
      'data-[state=checked]:border-accent',
      className
    )}
    {...props}
  >
    <RadioGroupPrimitive.Indicator className="flex items-center justify-center">
      <Circle className="h-2 w-2 fill-accent text-accent" />
    </RadioGroupPrimitive.Indicator>
  </RadioGroupPrimitive.Item>
));
RadioGroupItem.displayName = RadioGroupPrimitive.Item.displayName;

export { RadioGroup, RadioGroupItem };
```
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit --pretty 2>&1 | grep -E "error TS" | grep -E "components/ui/(dropdown|select|radio)" | head -5 || echo "No type errors in dropdown/select/radio"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DropdownMenuPrimitive" packages/app/src/components/ui/dropdown-menu.tsx` matches
    - `grep "DropdownMenuItem\|DropdownMenuCheckboxItem\|DropdownMenuRadioItem\|DropdownMenuSeparator\|DropdownMenuLabel" packages/app/src/components/ui/dropdown-menu.tsx` returns 5+ matches
    - `grep "z-\[1000\]" packages/app/src/components/ui/dropdown-menu.tsx` matches (dropdown z-index)
    - `grep "bg-\[rgba(62,103,191,0.10)\]" packages/app/src/components/ui/dropdown-menu.tsx` matches (hover highlight)
    - `grep "SelectPrimitive" packages/app/src/components/ui/select.tsx` matches (Radix Select, not Popover)
    - `grep "SelectTrigger\|SelectContent\|SelectItem\|SelectValue" packages/app/src/components/ui/select.tsx` returns 4+ matches
    - `grep "h-10" packages/app/src/components/ui/select.tsx` matches (Input visual contract height)
    - `grep "ChevronDown" packages/app/src/components/ui/select.tsx` matches (chevron icon)
    - `grep "text-accent" packages/app/src/components/ui/select.tsx` matches (selected item indicator)
    - `grep "RadioGroupPrimitive" packages/app/src/components/ui/radio-group.tsx` matches
    - `grep "h-4 w-4" packages/app/src/components/ui/radio-group.tsx` matches (16px size)
    - `grep "rounded-full" packages/app/src/components/ui/radio-group.tsx` matches (circle shape)
    - `grep "fill-accent" packages/app/src/components/ui/radio-group.tsx` matches (checked dot color)
    - No .css files in packages/app/src/components/ui/
  </acceptance_criteria>
  <done>
    DropdownMenu, Select, and RadioGroup components exist with correct Radix primitives and all UI-SPEC visual specs. DropdownMenu has items, checkbox items, radio items, labels, and separators. Select uses @radix-ui/react-select with trigger matching Input contract and accent checkmark. RadioGroup has 16px circle items with accent fill. No raw CSS.
  </done>
</task>

</tasks>

<verification>
- All 6 component files exist in packages/app/src/components/ui/
- No .css files in packages/app/src/components/ui/ (DESIGN-06 compliance)
- All 6 components use correct Radix primitives (not hand-rolled accessibility)
- Dialog uses z-[1300] for backdrop, z-[1400] for content
- Dropdown uses z-[1000]
- Tooltip uses z-[1600]
- Select uses z-[1500]
- TypeScript compiles without errors for all 6 files
</verification>

<success_criteria>
6 Radix-heavy components (Dialog, Tabs, Tooltip, DropdownMenu, Select, RadioGroup) are implemented with exact UI-SPEC specifications, correct Radix primitives for accessibility, and Tailwind styling via cn().
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system/03-03-SUMMARY.md`
</output>
