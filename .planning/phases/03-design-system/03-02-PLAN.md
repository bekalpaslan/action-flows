---
phase: 03-design-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/app/src/components/ui/button.tsx
  - packages/app/src/components/ui/card.tsx
  - packages/app/src/components/ui/badge.tsx
  - packages/app/src/components/ui/avatar.tsx
  - packages/app/src/components/ui/input.tsx
  - packages/app/src/components/ui/checkbox.tsx
autonomous: true
requirements:
  - DESIGN-02
  - DESIGN-03
must_haves:
  truths:
    - "Button renders with 5 variants (primary, secondary, ghost, destructive, outline) and 4 sizes (sm, md, lg, icon)"
    - "Card renders with 3 variants (flat, raised, floating) and 5 sub-components (CardHeader, CardTitle, CardDescription, CardContent, CardFooter)"
    - "Badge renders with 6 semantic variants (default, success, warning, error, info, accent) and 2 sizes (sm, md)"
    - "Avatar renders with 4 sizes (sm, md, lg, xl) and shows initials fallback when no image is provided"
    - "Input renders with 3 sizes (sm, md, lg) and shows focus ring, error state, and placeholder styling"
    - "Checkbox renders with checked, unchecked, and disabled states using Radix primitive"
  artifacts:
    - path: "packages/app/src/components/ui/button.tsx"
      provides: "Button component with CVA variants + Radix Slot"
      exports: ["Button", "buttonVariants"]
    - path: "packages/app/src/components/ui/card.tsx"
      provides: "Card + sub-components"
      exports: ["Card", "CardHeader", "CardTitle", "CardDescription", "CardContent", "CardFooter"]
    - path: "packages/app/src/components/ui/badge.tsx"
      provides: "Badge with semantic variant colors"
      exports: ["Badge", "badgeVariants"]
    - path: "packages/app/src/components/ui/avatar.tsx"
      provides: "Avatar with image + initials fallback"
      exports: ["Avatar"]
    - path: "packages/app/src/components/ui/input.tsx"
      provides: "Input with sizes and error state"
      exports: ["Input"]
    - path: "packages/app/src/components/ui/checkbox.tsx"
      provides: "Checkbox with Radix primitive"
      exports: ["Checkbox"]
  key_links:
    - from: "packages/app/src/components/ui/button.tsx"
      to: "packages/app/src/lib/utils.ts"
      via: "import"
      pattern: "import.*cn.*from.*utils"
    - from: "packages/app/src/components/ui/button.tsx"
      to: "class-variance-authority"
      via: "import"
      pattern: "import.*cva.*from.*class-variance-authority"
---

<objective>
Build 6 core components: Button, Card, Badge, Avatar, Input, Checkbox. These are the simpler components -- Button/Card/Badge/Avatar use no overlay Radix primitives (Button uses Slot only), Input is native, Checkbox uses a single Radix toggle.

Purpose: First batch of the 12-component library (per D-03). These components are building blocks for subsequent phases.
Output: 6 fully typed component files using CVA + Tailwind + cn() with exact UI-SPEC values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system/03-UI-SPEC.md
@.planning/phases/03-design-system/03-RESEARCH.md
@.planning/phases/03-design-system/03-01-SUMMARY.md

<interfaces>
From packages/app/src/lib/utils.ts (created in Plan 01):
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From packages/app/src/styles/theme.css (created in Plan 01):
- Semantic colors available as Tailwind utilities: bg-surface, bg-surface-2, bg-surface-3, bg-accent, text-text, text-text-dim, text-text-muted, border-border, border-border-strong, text-destructive, text-warning, text-success, text-info
- Palette colors: bg-prussian-{100-900}, bg-orange-{100-900}, bg-grey-{100-900}
- Radius: rounded-xs (4px), rounded-sm (6px), rounded-md (8px), rounded-lg (10px), rounded-xl (14px), rounded-2xl (18px), rounded-full
- Shadows: shadow-xs, shadow-sm, shadow-md, shadow-lg, shadow-xl, shadow-2xl, shadow-glow-default, shadow-glow-hover, shadow-glow-focus
- Typography: text-caption (13px), text-body (15px), text-heading (20px), text-display (28px)
- Font weights: font-normal (400), font-semibold (600)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Button, Card, and Badge components</name>
  <files>
    packages/app/src/components/ui/button.tsx,
    packages/app/src/components/ui/card.tsx,
    packages/app/src/components/ui/badge.tsx
  </files>
  <read_first>
    packages/app/src/lib/utils.ts,
    packages/app/src/styles/theme.css,
    .planning/phases/03-design-system/03-UI-SPEC.md,
    .planning/phases/03-design-system/03-RESEARCH.md
  </read_first>
  <action>
Create `packages/app/src/components/ui/` directory if it does not exist.

**Button (`packages/app/src/components/ui/button.tsx`):**
Per D-04, use @radix-ui/react-slot for asChild pattern. Per UI-SPEC Button spec:

```typescript
import * as React from 'react';
import { Slot } from '@radix-ui/react-slot';
import { cva, type VariantProps } from 'class-variance-authority';
import { cn } from '@/lib/utils';

const buttonVariants = cva(
  'inline-flex items-center justify-center gap-2 rounded-md font-semibold transition-colors focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent disabled:pointer-events-none disabled:opacity-50',
  {
    variants: {
      variant: {
        primary: 'bg-[rgba(62,103,191,0.95)] text-white hover:bg-accent active:bg-prussian-700 shadow-glow-default hover:shadow-glow-hover',
        secondary: 'bg-[rgba(138,138,138,0.36)] text-text hover:bg-[rgba(138,138,138,0.42)]',
        ghost: 'bg-transparent text-text hover:bg-[rgba(255,255,255,0.08)]',
        destructive: 'bg-[rgba(214,133,2,0.95)] text-white hover:bg-orange-400',
        outline: 'border border-border bg-transparent text-text hover:border-border-strong',
      },
      size: {
        sm: 'h-8 px-3 text-caption',
        md: 'h-10 px-4 text-body',
        lg: 'h-12 px-6 text-body',
        icon: 'h-10 w-10',
      },
    },
    defaultVariants: {
      variant: 'primary',
      size: 'md',
    },
  }
);

export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean;
}

const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, asChild = false, ...props }, ref) => {
    const Comp = asChild ? Slot : 'button';
    return (
      <Comp
        className={cn(buttonVariants({ variant, size, className }))}
        ref={ref}
        {...props}
      />
    );
  }
);
Button.displayName = 'Button';

export { Button, buttonVariants };
```

Transition uses `transition-colors` which is built into Tailwind. The `duration-normal` CSS variable (200ms) from theme.css applies via the base transition style. The `focus-visible:outline-accent` uses the semantic accent color.

**Card (`packages/app/src/components/ui/card.tsx`):**
Per UI-SPEC Card spec. No Radix primitive. CVA for variants (flat, raised, floating). 5 sub-components. Border radius lg (10px). Border 1px solid border. Padding p-4.

```typescript
import * as React from 'react';
import { cva, type VariantProps } from 'class-variance-authority';
import { cn } from '@/lib/utils';

const cardVariants = cva(
  'rounded-lg border border-border bg-surface-2 text-text',
  {
    variants: {
      variant: {
        flat: '',
        raised: 'shadow-md',
        floating: 'shadow-lg shadow-glow-default',
      },
      interactive: {
        true: 'cursor-pointer hover:border-border-strong transition-colors',
        false: '',
      },
    },
    defaultVariants: {
      variant: 'flat',
      interactive: false,
    },
  }
);

export interface CardProps
  extends React.HTMLAttributes<HTMLDivElement>,
    VariantProps<typeof cardVariants> {}

const Card = React.forwardRef<HTMLDivElement, CardProps>(
  ({ className, variant, interactive, ...props }, ref) => (
    <div
      ref={ref}
      className={cn(cardVariants({ variant, interactive, className }))}
      {...props}
    />
  )
);
Card.displayName = 'Card';

const CardHeader = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(
  ({ className, ...props }, ref) => (
    <div ref={ref} className={cn('flex flex-col gap-1.5 p-4', className)} {...props} />
  )
);
CardHeader.displayName = 'CardHeader';

const CardTitle = React.forwardRef<HTMLHeadingElement, React.HTMLAttributes<HTMLHeadingElement>>(
  ({ className, ...props }, ref) => (
    <h3 ref={ref} className={cn('text-heading font-semibold tracking-tight text-text', className)} {...props} />
  )
);
CardTitle.displayName = 'CardTitle';

const CardDescription = React.forwardRef<HTMLParagraphElement, React.HTMLAttributes<HTMLParagraphElement>>(
  ({ className, ...props }, ref) => (
    <p ref={ref} className={cn('text-caption text-text-dim', className)} {...props} />
  )
);
CardDescription.displayName = 'CardDescription';

const CardContent = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(
  ({ className, ...props }, ref) => (
    <div ref={ref} className={cn('p-4 pt-0', className)} {...props} />
  )
);
CardContent.displayName = 'CardContent';

const CardFooter = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(
  ({ className, ...props }, ref) => (
    <div ref={ref} className={cn('flex items-center p-4 pt-0', className)} {...props} />
  )
);
CardFooter.displayName = 'CardFooter';

export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, cardVariants };
```

**Badge (`packages/app/src/components/ui/badge.tsx`):**
Per UI-SPEC Badge spec. No Radix. 6 semantic variants with exact rgba bg colors from UI-SPEC. Font-semibold (600). rounded-sm (6px). Sizes sm (px-1.5 py-0.5) and md (px-2 py-0.5).

```typescript
import { cva, type VariantProps } from 'class-variance-authority';
import { cn } from '@/lib/utils';

const badgeVariants = cva(
  'inline-flex items-center rounded-sm font-semibold text-caption',
  {
    variants: {
      variant: {
        default: 'bg-[rgba(138,138,138,0.15)] text-text-dim',
        success: 'bg-[rgba(126,153,213,0.15)] text-success',
        warning: 'bg-[rgba(252,163,17,0.15)] text-warning',
        error: 'bg-[rgba(214,133,2,0.15)] text-destructive',
        info: 'bg-[rgba(62,103,191,0.15)] text-info',
        accent: 'bg-[rgba(62,103,191,0.15)] text-accent',
      },
      size: {
        sm: 'px-1.5 py-0.5',
        md: 'px-2 py-0.5',
      },
    },
    defaultVariants: {
      variant: 'default',
      size: 'md',
    },
  }
);

export interface BadgeProps
  extends React.HTMLAttributes<HTMLSpanElement>,
    VariantProps<typeof badgeVariants> {}

export function Badge({ className, variant, size, ...props }: BadgeProps) {
  return (
    <span className={cn(badgeVariants({ variant, size }), className)} {...props} />
  );
}

export { badgeVariants };
```
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit --pretty 2>&1 | grep -E "error TS" | grep -E "components/ui/(button|card|badge)" | head -5 || echo "No type errors in button/card/badge"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export.*Button" packages/app/src/components/ui/button.tsx` matches
    - `grep "buttonVariants" packages/app/src/components/ui/button.tsx` matches
    - `grep "primary.*secondary.*ghost.*destructive.*outline" packages/app/src/components/ui/button.tsx` matches (all 5 variants)
    - `grep "sm.*md.*lg.*icon" packages/app/src/components/ui/button.tsx` matches (all 4 sizes)
    - `grep "asChild" packages/app/src/components/ui/button.tsx` matches (Radix Slot pattern)
    - `grep "export.*Card\b" packages/app/src/components/ui/card.tsx` matches
    - `grep "CardHeader\|CardTitle\|CardDescription\|CardContent\|CardFooter" packages/app/src/components/ui/card.tsx` returns 5+ matches
    - `grep "flat.*raised.*floating" packages/app/src/components/ui/card.tsx` matches (all 3 variants)
    - `grep "export.*Badge" packages/app/src/components/ui/badge.tsx` matches
    - `grep "default.*success.*warning.*error.*info.*accent" packages/app/src/components/ui/badge.tsx` matches (all 6 variants)
    - No .css files in packages/app/src/components/ui/ (per DESIGN-06)
    - All 3 files import from '@/lib/utils' for cn()
    - All 3 files import from 'class-variance-authority' for cva
  </acceptance_criteria>
  <done>
    Button, Card, and Badge components exist with all UI-SPEC variants, sizes, and sub-components. All use CVA + cn() + Tailwind classes. No raw CSS. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Avatar, Input, and Checkbox components</name>
  <files>
    packages/app/src/components/ui/avatar.tsx,
    packages/app/src/components/ui/input.tsx,
    packages/app/src/components/ui/checkbox.tsx
  </files>
  <read_first>
    packages/app/src/lib/utils.ts,
    packages/app/src/styles/theme.css,
    .planning/phases/03-design-system/03-UI-SPEC.md
  </read_first>
  <action>
**Avatar (`packages/app/src/components/ui/avatar.tsx`):**
Per UI-SPEC Avatar spec. No Radix primitive. Sizes: sm (w-6 h-6), md (w-8 h-8), lg (w-10 h-10), xl (w-12 h-12). Border radius full (9999px). Fallback: gradient background (prussian-700 to prussian-600), white initials, font-semibold. Border: 2px solid surface-2.

```typescript
import * as React from 'react';
import { cva, type VariantProps } from 'class-variance-authority';
import { cn } from '@/lib/utils';

const avatarVariants = cva(
  'relative inline-flex shrink-0 items-center justify-center overflow-hidden rounded-full border-2 border-surface-2',
  {
    variants: {
      size: {
        sm: 'h-6 w-6 text-[10px]',
        md: 'h-8 w-8 text-caption',
        lg: 'h-10 w-10 text-body',
        xl: 'h-12 w-12 text-body',
      },
    },
    defaultVariants: {
      size: 'md',
    },
  }
);

export interface AvatarProps
  extends React.HTMLAttributes<HTMLDivElement>,
    VariantProps<typeof avatarVariants> {
  src?: string | null;
  alt?: string;
  fallback?: string;
}

const Avatar = React.forwardRef<HTMLDivElement, AvatarProps>(
  ({ className, size, src, alt, fallback, ...props }, ref) => {
    const [imgError, setImgError] = React.useState(false);
    const showImage = src && !imgError;

    const initials = fallback
      ? fallback.slice(0, 2).toUpperCase()
      : alt
        ? alt.split(' ').map(w => w[0]).slice(0, 2).join('').toUpperCase()
        : '?';

    return (
      <div
        ref={ref}
        className={cn(avatarVariants({ size, className }))}
        {...props}
      >
        {showImage ? (
          <img
            src={src}
            alt={alt || ''}
            onError={() => setImgError(true)}
            className="h-full w-full object-cover"
          />
        ) : (
          <span className="flex h-full w-full items-center justify-center bg-gradient-to-br from-prussian-700 to-prussian-600 font-semibold text-white">
            {initials}
          </span>
        )}
      </div>
    );
  }
);
Avatar.displayName = 'Avatar';

export { Avatar, avatarVariants };
```

**Input (`packages/app/src/components/ui/input.tsx`):**
Per UI-SPEC Input spec. No Radix (native input). Height h-10 (40px). Background dark: rgba(138,138,138,0.24). Border 1px solid border at rest, accent on focus with glow-focus. Border radius md (8px). Text 15px body. Placeholder text-muted. Sizes: sm (h-8 text-caption), md (h-10 text-body), lg (h-12 text-body). Error state via `error` prop.

```typescript
import * as React from 'react';
import { cva, type VariantProps } from 'class-variance-authority';
import { cn } from '@/lib/utils';

const inputVariants = cva(
  'flex w-full rounded-md border border-border bg-[rgba(138,138,138,0.24)] text-text placeholder:text-text-muted transition-colors file:border-0 file:bg-transparent file:text-body file:font-semibold focus-visible:outline-none focus-visible:border-accent focus-visible:shadow-glow-focus disabled:cursor-not-allowed disabled:opacity-50',
  {
    variants: {
      inputSize: {
        sm: 'h-8 px-3 text-caption',
        md: 'h-10 px-3 text-body',
        lg: 'h-12 px-3 text-body',
      },
      error: {
        true: 'border-destructive focus-visible:border-destructive focus-visible:shadow-[0_0_18px_rgba(214,133,2,0.25),0_0_4px_rgba(214,133,2,0.30)]',
        false: '',
      },
    },
    defaultVariants: {
      inputSize: 'md',
      error: false,
    },
  }
);

export interface InputProps
  extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'>,
    VariantProps<typeof inputVariants> {}

const Input = React.forwardRef<HTMLInputElement, InputProps>(
  ({ className, type, inputSize, error, ...props }, ref) => {
    return (
      <input
        type={type}
        className={cn(inputVariants({ inputSize, error, className }))}
        ref={ref}
        {...props}
      />
    );
  }
);
Input.displayName = 'Input';

export { Input, inputVariants };
```

Note: CVA variant name is `inputSize` instead of `size` to avoid conflict with native HTML input `size` attribute.

**Checkbox (`packages/app/src/components/ui/checkbox.tsx`):**
Per UI-SPEC Checkbox spec. Uses @radix-ui/react-checkbox. Size 16px (w-4 h-4). Border radius xs (4px). Unchecked: transparent bg, border-default. Checked: accent bg (#3e67bf), white checkmark. Disabled: opacity-50, cursor-not-allowed. Focus: 2px accent outline, 2px offset.

```typescript
import * as React from 'react';
import * as CheckboxPrimitive from '@radix-ui/react-checkbox';
import { Check } from 'lucide-react';
import { cn } from '@/lib/utils';

const Checkbox = React.forwardRef<
  React.ComponentRef<typeof CheckboxPrimitive.Root>,
  React.ComponentPropsWithoutRef<typeof CheckboxPrimitive.Root>
>(({ className, ...props }, ref) => (
  <CheckboxPrimitive.Root
    ref={ref}
    className={cn(
      'peer h-4 w-4 shrink-0 rounded-xs border border-border bg-transparent transition-colors',
      'focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent',
      'disabled:cursor-not-allowed disabled:opacity-50',
      'data-[state=checked]:bg-accent data-[state=checked]:border-accent data-[state=checked]:text-white',
      className
    )}
    {...props}
  >
    <CheckboxPrimitive.Indicator className={cn('flex items-center justify-center text-current')}>
      <Check className="h-3 w-3" />
    </CheckboxPrimitive.Indicator>
  </CheckboxPrimitive.Root>
));
Checkbox.displayName = CheckboxPrimitive.Root.displayName;

export { Checkbox };
```
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit --pretty 2>&1 | grep -E "error TS" | grep -E "components/ui/(avatar|input|checkbox)" | head -5 || echo "No type errors in avatar/input/checkbox"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export.*Avatar" packages/app/src/components/ui/avatar.tsx` matches
    - `grep "sm.*md.*lg.*xl" packages/app/src/components/ui/avatar.tsx` matches (all 4 sizes)
    - `grep "fallback\|initials" packages/app/src/components/ui/avatar.tsx` matches (fallback logic)
    - `grep "export.*Input" packages/app/src/components/ui/input.tsx` matches
    - `grep "inputSize" packages/app/src/components/ui/input.tsx` matches (size variant)
    - `grep "error" packages/app/src/components/ui/input.tsx` matches (error state)
    - `grep "export.*Checkbox" packages/app/src/components/ui/checkbox.tsx` matches
    - `grep "CheckboxPrimitive" packages/app/src/components/ui/checkbox.tsx` matches (Radix usage)
    - `grep "lucide-react" packages/app/src/components/ui/checkbox.tsx` matches (Check icon)
    - No .css files in packages/app/src/components/ui/
    - All 3 files import cn from '@/lib/utils'
  </acceptance_criteria>
  <done>
    Avatar, Input, and Checkbox components exist with all UI-SPEC variants and states. Avatar has image + initials fallback with gradient. Input has 3 sizes and error state with destructive glow. Checkbox uses Radix primitive with Check icon. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
- All 6 component files exist in packages/app/src/components/ui/
- No .css files in packages/app/src/components/ui/ (DESIGN-06 compliance)
- All components import cn from '@/lib/utils' and cva from 'class-variance-authority'
- Button has Radix Slot (asChild), Checkbox has Radix Checkbox primitive
- TypeScript compiles without errors for all 6 files
- Each component uses only Tailwind utility classes -- no raw hex values, no var(--token) references
</verification>

<success_criteria>
6 core components (Button, Card, Badge, Avatar, Input, Checkbox) are implemented with exact UI-SPEC specifications, using CVA variants, Tailwind classes, and cn() utility. No raw CSS exists in any component file.
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system/03-02-SUMMARY.md`
</output>
