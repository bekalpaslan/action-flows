# ActionFlows Backend Environment Variables

# Port for the backend API and WebSocket server
PORT=3001

# Redis Configuration (Optional - defaults to in-memory storage if not set)
# If REDIS_URL is set, the backend will use Redis for persistent storage
# Otherwise, in-memory storage is used (suitable for development only)
REDIS_URL=redis://localhost:6379

# Redis key prefix for all stored data
# This allows running multiple ActionFlows instances with the same Redis instance
REDIS_PREFIX=afw:

# Logging level (optional)
LOG_LEVEL=info
