/**
 * TaskHistoryList — Stub for Task 2 compilation. Full implementation in Task 3.
 */

import type { WorkbenchId } from '@/lib/types';

export interface TaskHistoryListProps {
  taskId: string;
  workbenchId: WorkbenchId;
}

export function TaskHistoryList(_props: TaskHistoryListProps) {
  return null;
}
